// Code generated by acp-go-generator; DO NOT EDIT.

package v2

import (
	"bytes"
	"context"
	"encoding/json"
	"errors"
	"fmt"
)

// An absolute filesystem path used by the protocol.
type AbsolutePath string

// Authentication-related extension capabilities supported by the agent.
//
// This object does not advertise support for 'auth/login' or 'auth/logout'.
// Those methods are advertised by a non-empty 'authMethods' list in the
// 'initialize' response.
type AgentAuthCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
}

// Capabilities supported by the agent.
//
// Advertised during initialization to inform the client about
// available features and content types.
//
// See protocol docs: [Agent Capabilities](https://agentclientprotocol.com/protocol/v2/initialization#agent-capabilities)
type AgentCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Authentication-related extension capabilities supported by the agent.
	//
	// Optional. Omitted or 'null' both mean the agent does not advertise any
	// authentication-related extensions. This field does not advertise support
	// for 'auth/login' or 'auth/logout'; those methods are advertised by a
	// non-empty 'authMethods' list in the 'initialize' response.
	Auth *AgentAuthCapabilities `json:"auth,omitempty"`
	// Session capabilities supported by the agent.
	//
	// Optional. Omitted or 'null' both mean the agent does not support the
	// 'session/*' method surface. Supplying '{}' means the agent supports the
	// baseline session methods: 'session/new', 'session/prompt',
	// 'session/cancel', and 'session/update'.
	Session *SessionCapabilities `json:"session,omitempty"`
}

// An agent message upsert.
//
// Only ['AgentMessage::message_id'] is required. 'content' has patch semantics:
// an omitted field leaves existing message content unchanged, 'null' clears the
// value, and a concrete array replaces the previous value. For a new
// 'messageId', omitted fields use client defaults. 'content' is replaced as a
// whole array; send '[]' or 'null' to clear it.
//
// Message updates and chunks are applied in the order they are received. When
// an 'agent_message' update includes 'content', that array replaces any
// content previously accumulated for the message, including content from
// earlier chunks. Later chunks with the same 'messageId' append to the current
// content.
type AgentMessage struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys. Omitted means no metadata update; 'null' is an explicit clear signal.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Complete replacement content for this message.
	Content []ContentBlock `json:"content,omitempty"`
	// A unique identifier for the message.
	MessageId MessageId `json:"messageId"`
}

func (v *AgentMessage) UnmarshalJSON(b []byte) error {
	*v = AgentMessage{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["messageId"]
		if !ok {
			return fmt.Errorf("messageId is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("messageId must not be null")
		}
	}
	type Alias AgentMessage
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = AgentMessage(a)
	return nil
}

// A JSON-RPC notification object.
type AgentNotification struct {
	// The notification method name.
	Method string `json:"method"`
	// Method-specific notification parameters.
	Params any `json:"params,omitempty"`
}

func (v *AgentNotification) UnmarshalJSON(b []byte) error {
	*v = AgentNotification{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["method"]
		if !ok {
			return fmt.Errorf("method is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("method must not be null")
		}
	}
	type Alias AgentNotification
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = AgentNotification(a)
	return nil
}

func (v *AgentNotification) Validate() error {
	return nil
}

// A JSON-RPC request object.
type AgentRequest struct {
	// The request id used to correlate the matching response.
	Id RequestId `json:"id"`
	// The method name to invoke.
	Method string `json:"method"`
	// Method-specific request parameters.
	Params any `json:"params,omitempty"`
}

func (v *AgentRequest) UnmarshalJSON(b []byte) error {
	*v = AgentRequest{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		_, ok := m["id"]
		if !ok {
			return fmt.Errorf("id is required")
		}
	}
	{
		raw, ok := m["method"]
		if !ok {
			return fmt.Errorf("method is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("method must not be null")
		}
	}
	type Alias AgentRequest
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = AgentRequest(a)
	return nil
}

func (v *AgentRequest) Validate() error {
	if err := v.Id.Validate(); err != nil {
		return fmt.Errorf("id: %w", err)
	}
	return nil
}

// A JSON-RPC response object.
// A successful JSON-RPC response.
type AgentResult struct {
	// The id of the request this response answers.
	Id RequestId `json:"id"`
	// Method-specific response data.
	Result any `json:"result"`
}

// A failed JSON-RPC response.
type AgentError struct {
	// Method-specific error data.
	Error Error `json:"error"`
	// The id of the request this response answers.
	Id RequestId `json:"id"`
}

type AgentResponse struct {
	// A successful JSON-RPC response.
	Result *AgentResult `json:"-"`
	// A failed JSON-RPC response.
	Error *AgentError `json:"-"`
}

func (u *AgentResponse) UnmarshalJSON(b []byte) error {
	*u = AgentResponse{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err == nil {
		{
			var v AgentResult
			var match bool = true
			{
				raw, ok := m["id"]
				if !ok {
					match = false
				}
				if ok {
					var value RequestId
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			{
				raw, ok := m["result"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value any
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			if match {
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Result = &v
				return nil
			}
		}
		{
			var v AgentError
			var match bool = true
			{
				raw, ok := m["error"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value Error
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			{
				raw, ok := m["id"]
				if !ok {
					match = false
				}
				if ok {
					var value RequestId
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			if match {
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Error = &v
				return nil
			}
		}
	} else {
		if _, ok := err.(*json.UnmarshalTypeError); !ok {
			return err
		}
	}
	var arr []map[string]json.RawMessage
	if json.Unmarshal(b, &arr) == nil {
	}
	return errors.New("no matching variant for union")
}
func (u AgentResponse) MarshalJSON() ([]byte, error) {
	if u.Result != nil {
		_b, _e := json.Marshal(*u.Result)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		return json.Marshal(m)
	}
	if u.Error != nil {
		_b, _e := json.Marshal(*u.Error)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		return json.Marshal(m)
	}
	return []byte{}, nil
}

func (u *AgentResponse) Validate() error {
	var count int
	if u.Result != nil {
		count++
	}
	if u.Error != nil {
		count++
	}
	if count < 1 {
		return errors.New("AgentResponse must have at least one variant set")
	}
	encoded, err := json.Marshal(*u)
	if err != nil {
		return err
	}
	var decoded AgentResponse
	if err := json.Unmarshal(encoded, &decoded); err != nil {
		return fmt.Errorf("AgentResponse: %w", err)
	}
	return nil
}

// An agent thought or reasoning message upsert.
//
// Only ['AgentThought::message_id'] is required. 'content' has patch semantics:
// an omitted field leaves existing thought content unchanged, 'null' clears the
// value, and a concrete array replaces the previous value. For a new
// 'messageId', omitted fields use client defaults. 'content' is replaced as a
// whole array; send '[]' or 'null' to clear it.
//
// Message updates and chunks are applied in the order they are received. When
// an 'agent_thought' update includes 'content', that array replaces any
// content previously accumulated for the thought, including content from
// earlier chunks. Later chunks with the same 'messageId' append to the current
// content.
type AgentThought struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys. Omitted means no metadata update; 'null' is an explicit clear signal.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Complete replacement content for this thought message.
	Content []ContentBlock `json:"content,omitempty"`
	// A unique identifier for the thought message.
	MessageId MessageId `json:"messageId"`
}

func (v *AgentThought) UnmarshalJSON(b []byte) error {
	*v = AgentThought{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["messageId"]
		if !ok {
			return fmt.Errorf("messageId is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("messageId must not be null")
		}
	}
	type Alias AgentThought
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = AgentThought(a)
	return nil
}

// Optional annotations for the client. The client can use annotations to inform how objects are used or displayed
type Annotations struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Intended recipients for this content, such as the user or assistant.
	Audience []Role `json:"audience,omitempty"`
	// Timestamp indicating when the underlying resource was last modified.
	//
	// Must be an RFC 3339 formatted string (e.g., "2025-01-12T15:00:58Z").
	LastModified *string `json:"lastModified,omitempty"`
	// Relative importance of this content when clients choose what to surface.
	Priority *float64 `json:"priority,omitempty"`
}

// Audio provided to or from an LLM.
type AudioContent struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Optional annotations that help clients decide how to display or route this content.
	Annotations *Annotations `json:"annotations,omitempty"`
	// Base64-encoded media payload.
	Data string `json:"data"`
	// MIME type describing the encoded media payload.
	MimeType MediaType `json:"mimeType"`
}

func (v *AudioContent) UnmarshalJSON(b []byte) error {
	*v = AudioContent{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["data"]
		if !ok {
			return fmt.Errorf("data is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("data must not be null")
		}
	}
	{
		raw, ok := m["mimeType"]
		if !ok {
			return fmt.Errorf("mimeType is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("mimeType must not be null")
		}
	}
	type Alias AudioContent
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = AudioContent(a)
	return nil
}

// Authentication capabilities supported by the client.
//
// Advertised during initialization to inform the agent which authentication
// method types the client can handle. This governs opt-in types that require
// additional client-side support.
type AuthCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Whether the client supports 'terminal' authentication methods.
	//
	// Optional. Omitted or 'null' both mean the client does not advertise support.
	// The client should supply '{}' only when it can reproduce the configured
	// agent invocation in an interactive terminal. Supplying '{}' means the
	// agent may include 'terminal' entries in its authentication methods.
	Terminal *TerminalAuthCapabilities `json:"terminal,omitempty"`
}

// Describes an available authentication method.
//
// The 'type' field acts as the discriminator in the serialized JSON form.
// Client runs the configured agent program as a separate interactive
// process, without passing this method to 'auth/login'.
type AuthMethodTerminalInline struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Additional arguments to append to the configured agent invocation for terminal auth.
	Args []string `json:"args,omitempty"`
	// Optional description providing more details about this authentication method.
	Description *string `json:"description,omitempty"`
	// Additional environment variables to set on the configured agent invocation for terminal auth.
	// Names MUST be unique. These values override same-named variables in the
	// base launch configuration.
	Env []EnvVariable `json:"env,omitempty"`
	// Unique identifier for this authentication method.
	MethodId AuthMethodId `json:"methodId"`
	// Human-readable name of the authentication method.
	Name string `json:"name"`
	Type string `json:"type"`
}

// Agent handles authentication itself through 'auth/login'.
//
// The 'type' discriminator value is 'agent'.
type AuthMethodAgentInline struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Optional description providing more details about this authentication method.
	Description *string `json:"description,omitempty"`
	// Unique identifier for this authentication method.
	MethodId AuthMethodId `json:"methodId"`
	// Human-readable name of the authentication method.
	Name string `json:"name"`
	Type string `json:"type"`
}

// Custom or future authentication method.
//
// Values beginning with '_' are reserved for implementation-specific
// extensions. Unknown values that do not begin with '_' are reserved for
// future ACP variants.
//
// Clients that do not understand this method type should preserve the raw
// payload when storing, replaying, proxying, or forwarding initialization
// data, and otherwise ignore the method or display it generically.
type AuthMethodOther = json.RawMessage

type AuthMethod struct {
	// Client runs the configured agent program as a separate interactive
	// process, without passing this method to 'auth/login'.
	Terminal *AuthMethodTerminalInline `json:"-"`
	// Agent handles authentication itself through 'auth/login'.
	//
	// The 'type' discriminator value is 'agent'.
	Agent *AuthMethodAgentInline `json:"-"`
	// Custom or future authentication method.
	//
	// Values beginning with '_' are reserved for implementation-specific
	// extensions. Unknown values that do not begin with '_' are reserved for
	// future ACP variants.
	//
	// Clients that do not understand this method type should preserve the raw
	// payload when storing, replaying, proxying, or forwarding initialization
	// data, and otherwise ignore the method or display it generically.
	Other *AuthMethodOther `json:"-"`
}

func (u *AuthMethod) UnmarshalJSON(b []byte) error {
	*u = AuthMethod{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err == nil {
		{
			var disc string
			if v, ok := m["type"]; ok {
				json.Unmarshal(v, &disc)
			}
			switch disc {
			case "terminal":
				{
					raw, ok := m["methodId"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value AuthMethodId
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["name"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["type"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v AuthMethodTerminalInline
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Terminal = &v
				return nil
			case "agent":
				{
					raw, ok := m["methodId"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value AuthMethodId
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["name"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["type"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v AuthMethodAgentInline
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Agent = &v
				return nil
			}
		}
		{
			var v AuthMethodOther
			var match bool = true
			{
				raw, ok := m["methodId"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value AuthMethodId
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			{
				raw, ok := m["name"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value string
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			{
				raw, ok := m["type"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value string
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			if match {
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Other = &v
				return nil
			}
		}
	} else {
		if _, ok := err.(*json.UnmarshalTypeError); !ok {
			return err
		}
	}
	var arr []map[string]json.RawMessage
	if json.Unmarshal(b, &arr) == nil {
	}
	return errors.New("no matching variant for union")
}
func (u AuthMethod) MarshalJSON() ([]byte, error) {
	if u.Terminal != nil {
		_b, _e := json.Marshal(*u.Terminal)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["type"] = json.RawMessage("\"terminal\"")
		return json.Marshal(m)
	}
	if u.Agent != nil {
		_b, _e := json.Marshal(*u.Agent)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["type"] = json.RawMessage("\"agent\"")
		return json.Marshal(m)
	}
	if u.Other != nil {
		_b, _e := json.Marshal(*u.Other)
		if _e != nil {
			return []byte{}, _e
		}
		return _b, nil
	}
	return []byte{}, nil
}

func (u *AuthMethod) Validate() error {
	var count int
	if u.Terminal != nil {
		count++
	}
	if u.Agent != nil {
		count++
	}
	if u.Other != nil {
		count++
	}
	if count < 1 {
		return errors.New("AuthMethod must have at least one variant set")
	}
	encoded, err := json.Marshal(*u)
	if err != nil {
		return err
	}
	var decoded AuthMethod
	if err := json.Unmarshal(encoded, &decoded); err != nil {
		return fmt.Errorf("AuthMethod: %w", err)
	}
	return nil
}

// Agent handles authentication itself through 'auth/login'.
//
// The 'type' discriminator value is 'agent'.
type AuthMethodAgent struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Optional description providing more details about this authentication method.
	Description *string `json:"description,omitempty"`
	// Unique identifier for this authentication method.
	MethodId AuthMethodId `json:"methodId"`
	// Human-readable name of the authentication method.
	Name string `json:"name"`
}

func (v *AuthMethodAgent) UnmarshalJSON(b []byte) error {
	*v = AuthMethodAgent{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["methodId"]
		if !ok {
			return fmt.Errorf("methodId is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("methodId must not be null")
		}
	}
	{
		raw, ok := m["name"]
		if !ok {
			return fmt.Errorf("name is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("name must not be null")
		}
	}
	type Alias AuthMethodAgent
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = AuthMethodAgent(a)
	return nil
}

// Typed identifier used for auth method values on the wire.
type AuthMethodId string

// Terminal-based authentication method.
//
// The client runs the configured agent program as a separate interactive
// process for the user to authenticate via a TUI. Agents MUST advertise this
// method only when the client enabled its terminal authentication capability.
// A zero exit status signals success; any other termination signals failure.
// The client MUST NOT pass this method to 'auth/login'.
type AuthMethodTerminal struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Additional arguments to append to the configured agent invocation for terminal auth.
	Args []string `json:"args,omitempty"`
	// Optional description providing more details about this authentication method.
	Description *string `json:"description,omitempty"`
	// Additional environment variables to set on the configured agent invocation for terminal auth.
	// Names MUST be unique. These values override same-named variables in the
	// base launch configuration.
	Env []EnvVariable `json:"env,omitempty"`
	// Unique identifier for this authentication method.
	MethodId AuthMethodId `json:"methodId"`
	// Human-readable name of the authentication method.
	Name string `json:"name"`
}

func (v *AuthMethodTerminal) UnmarshalJSON(b []byte) error {
	*v = AuthMethodTerminal{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["methodId"]
		if !ok {
			return fmt.Errorf("methodId is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("methodId must not be null")
		}
	}
	{
		raw, ok := m["name"]
		if !ok {
			return fmt.Errorf("name is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("name must not be null")
		}
	}
	type Alias AuthMethodTerminal
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = AuthMethodTerminal(a)
	return nil
}

// Information about a command.
type AvailableCommand struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Human-readable description of what the command does.
	Description string `json:"description"`
	// Input for the command if required
	Input *AvailableCommandInput `json:"input,omitempty"`
	// Command name (e.g., 'create_plan', 'research_codebase').
	Name string `json:"name"`
}

func (v *AvailableCommand) UnmarshalJSON(b []byte) error {
	*v = AvailableCommand{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["description"]
		if !ok {
			return fmt.Errorf("description is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("description must not be null")
		}
	}
	{
		raw, ok := m["name"]
		if !ok {
			return fmt.Errorf("name is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("name must not be null")
		}
	}
	type Alias AvailableCommand
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = AvailableCommand(a)
	return nil
}

// The input specification for a command.
// All text that was typed after the command name is provided as input.
type AvailableCommandInputText struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// A hint to display when the input hasn't been provided yet
	Hint string `json:"hint"`
	Type string `json:"type"`
}

// Custom or future command input specification.
//
// Values beginning with '_' are reserved for implementation-specific
// extensions. Unknown values that do not begin with '_' are reserved for
// future ACP variants.
//
// Clients that do not understand this input type should preserve the raw
// payload when storing, replaying, proxying, or forwarding command
// metadata, and otherwise ignore the input specification or display the
// command without structured input.
type AvailableCommandInputOther = json.RawMessage

type AvailableCommandInput struct {
	// All text that was typed after the command name is provided as input.
	Text *AvailableCommandInputText `json:"-"`
	// Custom or future command input specification.
	//
	// Values beginning with '_' are reserved for implementation-specific
	// extensions. Unknown values that do not begin with '_' are reserved for
	// future ACP variants.
	//
	// Clients that do not understand this input type should preserve the raw
	// payload when storing, replaying, proxying, or forwarding command
	// metadata, and otherwise ignore the input specification or display the
	// command without structured input.
	Other *AvailableCommandInputOther `json:"-"`
}

func (u *AvailableCommandInput) UnmarshalJSON(b []byte) error {
	*u = AvailableCommandInput{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err == nil {
		{
			var disc string
			if v, ok := m["type"]; ok {
				json.Unmarshal(v, &disc)
			}
			switch disc {
			case "text":
				{
					raw, ok := m["hint"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["type"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v AvailableCommandInputText
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Text = &v
				return nil
			}
		}
		{
			var v AvailableCommandInputOther
			var match bool = true
			{
				raw, ok := m["type"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value string
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			if match {
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Other = &v
				return nil
			}
		}
	} else {
		if _, ok := err.(*json.UnmarshalTypeError); !ok {
			return err
		}
	}
	var arr []map[string]json.RawMessage
	if json.Unmarshal(b, &arr) == nil {
	}
	return errors.New("no matching variant for union")
}
func (u AvailableCommandInput) MarshalJSON() ([]byte, error) {
	if u.Text != nil {
		_b, _e := json.Marshal(*u.Text)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["type"] = json.RawMessage("\"text\"")
		return json.Marshal(m)
	}
	if u.Other != nil {
		_b, _e := json.Marshal(*u.Other)
		if _e != nil {
			return []byte{}, _e
		}
		return _b, nil
	}
	return []byte{}, nil
}

func (u *AvailableCommandInput) Validate() error {
	var count int
	if u.Text != nil {
		count++
	}
	if u.Other != nil {
		count++
	}
	if count < 1 {
		return errors.New("AvailableCommandInput must have at least one variant set")
	}
	encoded, err := json.Marshal(*u)
	if err != nil {
		return err
	}
	var decoded AvailableCommandInput
	if err := json.Unmarshal(encoded, &decoded); err != nil {
		return fmt.Errorf("AvailableCommandInput: %w", err)
	}
	return nil
}

// Available commands are ready or have changed
type AvailableCommandsUpdate struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Commands the agent can execute.
	AvailableCommands []AvailableCommand `json:"availableCommands"`
}

func (v *AvailableCommandsUpdate) UnmarshalJSON(b []byte) error {
	*v = AvailableCommandsUpdate{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["availableCommands"]
		if !ok {
			return fmt.Errorf("availableCommands is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("availableCommands must not be null")
		}
	}
	type Alias AvailableCommandsUpdate
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = AvailableCommandsUpdate(a)
	return nil
}

// Binary resource contents.
type BlobResourceContents struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Base64-encoded bytes for a binary resource payload.
	Blob string `json:"blob"`
	// MIME type describing the encoded media payload.
	MimeType *MediaType `json:"mimeType,omitempty"`
	// URI associated with this resource or media payload.
	Uri string `json:"uri"`
}

func (v *BlobResourceContents) UnmarshalJSON(b []byte) error {
	*v = BlobResourceContents{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["blob"]
		if !ok {
			return fmt.Errorf("blob is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("blob must not be null")
		}
	}
	{
		raw, ok := m["uri"]
		if !ok {
			return fmt.Errorf("uri is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("uri must not be null")
		}
	}
	type Alias BlobResourceContents
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = BlobResourceContents(a)
	return nil
}

// Schema for boolean properties in an elicitation form.
type BooleanPropertySchema struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// Optional. Omitted and 'null' are equivalent and mean no metadata.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Default value.
	//
	// Optional. Omitted and 'null' are equivalent and mean no default value is provided.
	Default *bool `json:"default,omitempty"`
	// Human-readable description.
	//
	// Optional. Omitted and 'null' are equivalent and mean no description is provided.
	Description *string `json:"description,omitempty"`
	// Optional title for the property.
	//
	// Optional. Omitted and 'null' are equivalent and mean no title is provided.
	Title *string `json:"title,omitempty"`
}

// Notification to cancel an ongoing request.
//
// See protocol docs: [Cancellation](https://agentclientprotocol.com/protocol/v2/cancellation)
type CancelRequestNotification struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The ID of the request to cancel.
	RequestId RequestId `json:"requestId"`
}

func (v *CancelRequestNotification) UnmarshalJSON(b []byte) error {
	*v = CancelRequestNotification{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		_, ok := m["requestId"]
		if !ok {
			return fmt.Errorf("requestId is required")
		}
	}
	type Alias CancelRequestNotification
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = CancelRequestNotification(a)
	return nil
}

func (v *CancelRequestNotification) Validate() error {
	if err := v.RequestId.Validate(); err != nil {
		return fmt.Errorf("requestId: %w", err)
	}
	return nil
}

// Notification to cancel ongoing operations for a session.
//
// See protocol docs: [Cancellation](https://agentclientprotocol.com/protocol/v2/prompt-lifecycle#cancellation)
type CancelSessionNotification struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The ID of the session to cancel operations for.
	SessionId SessionId `json:"sessionId"`
}

func (v *CancelSessionNotification) UnmarshalJSON(b []byte) error {
	*v = CancelSessionNotification{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["sessionId"]
		if !ok {
			return fmt.Errorf("sessionId is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("sessionId must not be null")
		}
	}
	type Alias CancelSessionNotification
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = CancelSessionNotification(a)
	return nil
}

func (v *CancelSessionNotification) Validate() error {
	return nil
}

// Capabilities supported by the client.
//
// Advertised during initialization to inform the agent about
// available features and methods.
//
// See protocol docs: [Client Capabilities](https://agentclientprotocol.com/protocol/v2/initialization#client-capabilities)
type ClientCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Authentication capabilities supported by the client.
	// Determines which authentication method types the agent may include
	// in its 'InitializeResponse'.
	//
	// Optional. Omitted or 'null' both mean the client does not advertise any
	// authentication-method extensions.
	Auth *AuthCapabilities `json:"auth,omitempty"`
	// Elicitation capabilities supported by the client.
	// Determines which elicitation modes the agent may use.
	//
	// Optional. Omitted or 'null' both mean the client does not advertise
	// elicitation support.
	Elicitation *ElicitationCapabilities `json:"elicitation,omitempty"`
}

// A JSON-RPC notification object.
type ClientNotification struct {
	// The notification method name.
	Method string `json:"method"`
	// Method-specific notification parameters.
	Params any `json:"params,omitempty"`
}

func (v *ClientNotification) UnmarshalJSON(b []byte) error {
	*v = ClientNotification{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["method"]
		if !ok {
			return fmt.Errorf("method is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("method must not be null")
		}
	}
	type Alias ClientNotification
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = ClientNotification(a)
	return nil
}

func (v *ClientNotification) Validate() error {
	return nil
}

// A JSON-RPC request object.
type ClientRequest struct {
	// The request id used to correlate the matching response.
	Id RequestId `json:"id"`
	// The method name to invoke.
	Method string `json:"method"`
	// Method-specific request parameters.
	Params any `json:"params,omitempty"`
}

func (v *ClientRequest) UnmarshalJSON(b []byte) error {
	*v = ClientRequest{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		_, ok := m["id"]
		if !ok {
			return fmt.Errorf("id is required")
		}
	}
	{
		raw, ok := m["method"]
		if !ok {
			return fmt.Errorf("method is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("method must not be null")
		}
	}
	type Alias ClientRequest
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = ClientRequest(a)
	return nil
}

func (v *ClientRequest) Validate() error {
	if err := v.Id.Validate(); err != nil {
		return fmt.Errorf("id: %w", err)
	}
	return nil
}

// A JSON-RPC response object.
// A successful JSON-RPC response.
type ClientResult struct {
	// The id of the request this response answers.
	Id RequestId `json:"id"`
	// Method-specific response data.
	Result any `json:"result"`
}

// A failed JSON-RPC response.
type ClientError struct {
	// Method-specific error data.
	Error Error `json:"error"`
	// The id of the request this response answers.
	Id RequestId `json:"id"`
}

type ClientResponse struct {
	// A successful JSON-RPC response.
	Result *ClientResult `json:"-"`
	// A failed JSON-RPC response.
	Error *ClientError `json:"-"`
}

func (u *ClientResponse) UnmarshalJSON(b []byte) error {
	*u = ClientResponse{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err == nil {
		{
			var v ClientResult
			var match bool = true
			{
				raw, ok := m["id"]
				if !ok {
					match = false
				}
				if ok {
					var value RequestId
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			{
				raw, ok := m["result"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value any
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			if match {
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Result = &v
				return nil
			}
		}
		{
			var v ClientError
			var match bool = true
			{
				raw, ok := m["error"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value Error
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			{
				raw, ok := m["id"]
				if !ok {
					match = false
				}
				if ok {
					var value RequestId
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			if match {
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Error = &v
				return nil
			}
		}
	} else {
		if _, ok := err.(*json.UnmarshalTypeError); !ok {
			return err
		}
	}
	var arr []map[string]json.RawMessage
	if json.Unmarshal(b, &arr) == nil {
	}
	return errors.New("no matching variant for union")
}
func (u ClientResponse) MarshalJSON() ([]byte, error) {
	if u.Result != nil {
		_b, _e := json.Marshal(*u.Result)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		return json.Marshal(m)
	}
	if u.Error != nil {
		_b, _e := json.Marshal(*u.Error)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		return json.Marshal(m)
	}
	return []byte{}, nil
}

func (u *ClientResponse) Validate() error {
	var count int
	if u.Result != nil {
		count++
	}
	if u.Error != nil {
		count++
	}
	if count < 1 {
		return errors.New("ClientResponse must have at least one variant set")
	}
	encoded, err := json.Marshal(*u)
	if err != nil {
		return err
	}
	var decoded ClientResponse
	if err := json.Unmarshal(encoded, &decoded); err != nil {
		return fmt.Errorf("ClientResponse: %w", err)
	}
	return nil
}

// Request parameters for closing an active session.
//
// The agent **must** cancel any ongoing work related to the session (treat it
// as if 'session/cancel' was called) and then free up any resources associated
// with the session.
type CloseSessionRequest struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The ID of the session to close.
	SessionId SessionId `json:"sessionId"`
}

func (v *CloseSessionRequest) UnmarshalJSON(b []byte) error {
	*v = CloseSessionRequest{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["sessionId"]
		if !ok {
			return fmt.Errorf("sessionId is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("sessionId must not be null")
		}
	}
	type Alias CloseSessionRequest
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = CloseSessionRequest(a)
	return nil
}

func (v *CloseSessionRequest) Validate() error {
	return nil
}

// Response from closing a session.
type CloseSessionResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
}

func (v *CloseSessionResponse) Validate() error {
	return nil
}

// Permission request details for a command.
type CommandPermissionSubject struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys. Omitted and 'null' are equivalent and mean no subject metadata was provided.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The command that would be run if permission is granted.
	Command string `json:"command"`
	// The absolute working directory for the command.
	Cwd AbsolutePath `json:"cwd"`
	// The associated terminal, when already known. Omitted and 'null' are equivalent.
	TerminalId *TerminalId `json:"terminalId,omitempty"`
	// The associated tool call, when known. Omitted and 'null' are equivalent.
	ToolCallId *ToolCallId `json:"toolCallId,omitempty"`
}

func (v *CommandPermissionSubject) UnmarshalJSON(b []byte) error {
	*v = CommandPermissionSubject{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["command"]
		if !ok {
			return fmt.Errorf("command is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("command must not be null")
		}
	}
	{
		raw, ok := m["cwd"]
		if !ok {
			return fmt.Errorf("cwd is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("cwd must not be null")
		}
	}
	type Alias CommandPermissionSubject
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = CommandPermissionSubject(a)
	return nil
}

// Notification sent by the agent when a URL-based elicitation is complete.
type CompleteElicitationNotification struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// Optional. Omitted and 'null' are equivalent and mean no metadata.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The ID of the elicitation that completed.
	ElicitationId ElicitationId `json:"elicitationId"`
}

func (v *CompleteElicitationNotification) UnmarshalJSON(b []byte) error {
	*v = CompleteElicitationNotification{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["elicitationId"]
		if !ok {
			return fmt.Errorf("elicitationId is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("elicitationId must not be null")
		}
	}
	type Alias CompleteElicitationNotification
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = CompleteElicitationNotification(a)
	return nil
}

func (v *CompleteElicitationNotification) Validate() error {
	return nil
}

// Session configuration options have been updated.
type ConfigOptionUpdate struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The full set of configuration options and their current values.
	ConfigOptions []SessionConfigOption `json:"configOptions"`
}

func (v *ConfigOptionUpdate) UnmarshalJSON(b []byte) error {
	*v = ConfigOptionUpdate{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["configOptions"]
		if !ok {
			return fmt.Errorf("configOptions is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("configOptions must not be null")
		}
	}
	type Alias ConfigOptionUpdate
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = ConfigOptionUpdate(a)
	return nil
}

// Standard content block (text, images, resources).
type Content struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The actual content block.
	Content ContentBlock `json:"content"`
}

func (v *Content) UnmarshalJSON(b []byte) error {
	*v = Content{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["content"]
		if !ok {
			return fmt.Errorf("content is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("content must not be null")
		}
	}
	type Alias Content
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = Content(a)
	return nil
}

// Content blocks represent displayable information in the Agent Client Protocol.
//
// They provide a structured way to handle various types of user-facing content—whether
// it's text from language models, images for analysis, or embedded resources for context.
//
// Content blocks appear in:
// - User prompts sent via 'session/prompt'
// - Language model output reported through 'session/update' notifications as
// message updates or streamed chunks
// - Progress updates and results from tool calls
//
// This structure is compatible with the Model Context Protocol (MCP), enabling
// agents to seamlessly forward content from MCP tool outputs without transformation.
//
// See protocol docs: [Content](https://agentclientprotocol.com/protocol/v2/content)
// Text content. May be plain text or formatted with Markdown.
//
// All agents MUST support text content blocks in prompts.
// Clients SHOULD render this text as Markdown.
type ContentBlockText struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Optional annotations that help clients decide how to display or route this content.
	Annotations *Annotations `json:"annotations,omitempty"`
	// Text payload carried by this content block.
	Text string `json:"text"`
	Type string `json:"type"`
}

// Images for visual context or analysis.
//
// Requires the 'image' prompt capability when included in prompts.
type ContentBlockImage struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Optional annotations that help clients decide how to display or route this content.
	Annotations *Annotations `json:"annotations,omitempty"`
	// Base64-encoded media payload.
	Data string `json:"data"`
	// MIME type describing the encoded media payload.
	MimeType MediaType `json:"mimeType"`
	Type     string    `json:"type"`
	// URI associated with this resource or media payload.
	Uri *string `json:"uri,omitempty"`
}

// Audio data for transcription or analysis.
//
// Requires the 'audio' prompt capability when included in prompts.
type ContentBlockAudio struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Optional annotations that help clients decide how to display or route this content.
	Annotations *Annotations `json:"annotations,omitempty"`
	// Base64-encoded media payload.
	Data string `json:"data"`
	// MIME type describing the encoded media payload.
	MimeType MediaType `json:"mimeType"`
	Type     string    `json:"type"`
}

// References to resources that the agent can access.
//
// All agents MUST support resource links in prompts.
type ContentBlockResourceLink struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Optional annotations that help clients decide how to display or route this content.
	Annotations *Annotations `json:"annotations,omitempty"`
	// Optional human-readable details shown with this protocol object.
	Description *string `json:"description,omitempty"`
	// Optional set of sized icons that the client can display in a user interface.
	Icons []Icon `json:"icons,omitempty"`
	// MIME type describing the encoded media payload.
	MimeType *MediaType `json:"mimeType,omitempty"`
	// Human-readable name shown for this protocol object.
	Name string `json:"name"`
	// Optional size of the linked resource in bytes, if known.
	Size *int64 `json:"size,omitempty"`
	// Optional display title for end-user UI.
	Title *string `json:"title,omitempty"`
	Type  string  `json:"type"`
	// URI associated with this resource or media payload.
	Uri string `json:"uri"`
}

// Complete resource contents embedded directly in the message.
//
// Preferred for including context as it avoids extra round-trips.
//
// Requires the 'embeddedContext' prompt capability when included in prompts.
type ContentBlockResource struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Optional annotations that help clients decide how to display or route this content.
	Annotations *Annotations `json:"annotations,omitempty"`
	// Embedded resource payload, either text or binary data.
	Resource EmbeddedResourceResource `json:"resource"`
	Type     string                   `json:"type"`
}

// Custom or future content block.
//
// Values beginning with '_' are reserved for implementation-specific
// extensions. Unknown values that do not begin with '_' are reserved for
// future ACP variants.
//
// Receivers that do not understand this content block type should preserve
// the raw payload when storing, replaying, proxying, or forwarding content,
// and otherwise ignore it or display it generically.
type ContentBlockOther = json.RawMessage

type ContentBlock struct {
	// Text content. May be plain text or formatted with Markdown.
	//
	// All agents MUST support text content blocks in prompts.
	// Clients SHOULD render this text as Markdown.
	Text *ContentBlockText `json:"-"`
	// Images for visual context or analysis.
	//
	// Requires the 'image' prompt capability when included in prompts.
	Image *ContentBlockImage `json:"-"`
	// Audio data for transcription or analysis.
	//
	// Requires the 'audio' prompt capability when included in prompts.
	Audio *ContentBlockAudio `json:"-"`
	// References to resources that the agent can access.
	//
	// All agents MUST support resource links in prompts.
	ResourceLink *ContentBlockResourceLink `json:"-"`
	// Complete resource contents embedded directly in the message.
	//
	// Preferred for including context as it avoids extra round-trips.
	//
	// Requires the 'embeddedContext' prompt capability when included in prompts.
	Resource *ContentBlockResource `json:"-"`
	// Custom or future content block.
	//
	// Values beginning with '_' are reserved for implementation-specific
	// extensions. Unknown values that do not begin with '_' are reserved for
	// future ACP variants.
	//
	// Receivers that do not understand this content block type should preserve
	// the raw payload when storing, replaying, proxying, or forwarding content,
	// and otherwise ignore it or display it generically.
	Other *ContentBlockOther `json:"-"`
}

func (u *ContentBlock) UnmarshalJSON(b []byte) error {
	*u = ContentBlock{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err == nil {
		{
			var disc string
			if v, ok := m["type"]; ok {
				json.Unmarshal(v, &disc)
			}
			switch disc {
			case "text":
				{
					raw, ok := m["text"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["type"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v ContentBlockText
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Text = &v
				return nil
			case "image":
				{
					raw, ok := m["data"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["mimeType"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value MediaType
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["type"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v ContentBlockImage
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Image = &v
				return nil
			case "audio":
				{
					raw, ok := m["data"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["mimeType"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value MediaType
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["type"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v ContentBlockAudio
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Audio = &v
				return nil
			case "resource_link":
				{
					raw, ok := m["name"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["type"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["uri"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v ContentBlockResourceLink
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.ResourceLink = &v
				return nil
			case "resource":
				{
					raw, ok := m["resource"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value EmbeddedResourceResource
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["type"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v ContentBlockResource
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Resource = &v
				return nil
			}
		}
		{
			var v ContentBlockOther
			var match bool = true
			{
				raw, ok := m["type"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value string
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			if match {
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Other = &v
				return nil
			}
		}
	} else {
		if _, ok := err.(*json.UnmarshalTypeError); !ok {
			return err
		}
	}
	var arr []map[string]json.RawMessage
	if json.Unmarshal(b, &arr) == nil {
	}
	return errors.New("no matching variant for union")
}
func (u ContentBlock) MarshalJSON() ([]byte, error) {
	if u.Text != nil {
		_b, _e := json.Marshal(*u.Text)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["type"] = json.RawMessage("\"text\"")
		return json.Marshal(m)
	}
	if u.Image != nil {
		_b, _e := json.Marshal(*u.Image)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["type"] = json.RawMessage("\"image\"")
		return json.Marshal(m)
	}
	if u.Audio != nil {
		_b, _e := json.Marshal(*u.Audio)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["type"] = json.RawMessage("\"audio\"")
		return json.Marshal(m)
	}
	if u.ResourceLink != nil {
		_b, _e := json.Marshal(*u.ResourceLink)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["type"] = json.RawMessage("\"resource_link\"")
		return json.Marshal(m)
	}
	if u.Resource != nil {
		_b, _e := json.Marshal(*u.Resource)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["type"] = json.RawMessage("\"resource\"")
		return json.Marshal(m)
	}
	if u.Other != nil {
		_b, _e := json.Marshal(*u.Other)
		if _e != nil {
			return []byte{}, _e
		}
		return _b, nil
	}
	return []byte{}, nil
}

func (u *ContentBlock) Validate() error {
	var count int
	if u.Text != nil {
		count++
	}
	if u.Image != nil {
		count++
	}
	if u.Audio != nil {
		count++
	}
	if u.ResourceLink != nil {
		count++
	}
	if u.Resource != nil {
		count++
	}
	if u.Other != nil {
		count++
	}
	if count < 1 {
		return errors.New("ContentBlock must have at least one variant set")
	}
	encoded, err := json.Marshal(*u)
	if err != nil {
		return err
	}
	var decoded ContentBlock
	if err := json.Unmarshal(encoded, &decoded); err != nil {
		return fmt.Errorf("ContentBlock: %w", err)
	}
	return nil
}

// A streamed item of message content.
type ContentChunk struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys. This field is chunk-scoped.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// A single item of content
	Content ContentBlock `json:"content"`
	// A unique identifier for the message this chunk belongs to.
	//
	// All chunks belonging to the same message share the same 'messageId'.
	// A change in 'messageId' indicates a new message has started.
	MessageId MessageId `json:"messageId"`
}

func (v *ContentChunk) UnmarshalJSON(b []byte) error {
	*v = ContentChunk{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["content"]
		if !ok {
			return fmt.Errorf("content is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("content must not be null")
		}
	}
	{
		raw, ok := m["messageId"]
		if !ok {
			return fmt.Errorf("messageId is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("messageId must not be null")
		}
	}
	type Alias ContentChunk
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = ContentChunk(a)
	return nil
}

// Cost information for a session.
type Cost struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Total cumulative cost for session.
	Amount float64 `json:"amount"`
	// ISO 4217 currency code (e.g., "USD", "EUR").
	Currency string `json:"currency"`
}

func (v *Cost) UnmarshalJSON(b []byte) error {
	*v = Cost{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["amount"]
		if !ok {
			return fmt.Errorf("amount is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("amount must not be null")
		}
	}
	{
		raw, ok := m["currency"]
		if !ok {
			return fmt.Errorf("currency is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("currency must not be null")
		}
	}
	type Alias Cost
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = Cost(a)
	return nil
}

// Request from the agent to elicit structured user input.
//
// The agent sends this to the client to request information from the user,
// either via a form or by directing them to a URL.
// Elicitations are tied to a session (optionally a tool call) or a request.
// Form-based elicitation where the client renders a form from the provided schema.
type CreateElicitationForm struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// Optional. Omitted and 'null' are equivalent and mean no metadata.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// A human-readable message describing what input is needed.
	Message string `json:"message"`
	Mode    string `json:"mode"`
	// The request this elicitation is tied to.
	RequestId *RequestId `json:"requestId,omitempty"`
	// A JSON Schema describing the form fields to present to the user.
	RequestedSchema ElicitationSchema `json:"requestedSchema"`
	// The session this elicitation is tied to.
	SessionId *SessionId `json:"sessionId,omitempty"`
	// Optional tool call within the session.
	//
	// Optional. Omitted and 'null' are equivalent and mean the elicitation is scoped to the
	// session without a specific tool call.
	ToolCallId *ToolCallId `json:"toolCallId,omitempty"`
}

// URL-based elicitation where the client directs the user to a URL.
type CreateElicitationUrl struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// Optional. Omitted and 'null' are equivalent and mean no metadata.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The unique identifier for this elicitation.
	ElicitationId ElicitationId `json:"elicitationId"`
	// A human-readable message describing what input is needed.
	Message string `json:"message"`
	Mode    string `json:"mode"`
	// The request this elicitation is tied to.
	RequestId *RequestId `json:"requestId,omitempty"`
	// The session this elicitation is tied to.
	SessionId *SessionId `json:"sessionId,omitempty"`
	// Optional tool call within the session.
	//
	// Optional. Omitted and 'null' are equivalent and mean the elicitation is scoped to the
	// session without a specific tool call.
	ToolCallId *ToolCallId `json:"toolCallId,omitempty"`
	// The URL to direct the user to.
	Url string `json:"url"`
}

// Custom or future elicitation mode.
//
// Values beginning with '_' are reserved for implementation-specific
// extensions. Unknown values that do not begin with '_' are reserved for
// future ACP variants.
//
// Clients that do not understand this mode should preserve the raw payload
// when storing, replaying, proxying, or forwarding elicitation requests.
// They MUST NOT render it as a known elicitation mode.
type CreateElicitationOther = json.RawMessage

type CreateElicitationRequest struct {
	// Form-based elicitation where the client renders a form from the provided schema.
	Form *CreateElicitationForm `json:"-"`
	// URL-based elicitation where the client directs the user to a URL.
	Url *CreateElicitationUrl `json:"-"`
	// Custom or future elicitation mode.
	//
	// Values beginning with '_' are reserved for implementation-specific
	// extensions. Unknown values that do not begin with '_' are reserved for
	// future ACP variants.
	//
	// Clients that do not understand this mode should preserve the raw payload
	// when storing, replaying, proxying, or forwarding elicitation requests.
	// They MUST NOT render it as a known elicitation mode.
	Other *CreateElicitationOther `json:"-"`
}

func (u *CreateElicitationRequest) UnmarshalJSON(b []byte) error {
	*u = CreateElicitationRequest{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err == nil {
		{
			var disc string
			if v, ok := m["mode"]; ok {
				json.Unmarshal(v, &disc)
			}
			switch disc {
			case "form":
				{
					raw, ok := m["message"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["mode"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["requestedSchema"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value ElicitationSchema
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var anyOfMatch bool
				{
					match := true
					{
						raw, ok := m["sessionId"]
						if !ok {
							match = false
						}
						if ok {
							if string(raw) == "null" {
								match = false
							}
							var value SessionId
							if json.Unmarshal(raw, &value) != nil {
								match = false
							}
						}
					}
					if match {
						anyOfMatch = true
					}
				}
				{
					match := true
					{
						raw, ok := m["requestId"]
						if !ok {
							match = false
						}
						if ok {
							var value RequestId
							if json.Unmarshal(raw, &value) != nil {
								match = false
							}
						}
					}
					if match {
						anyOfMatch = true
					}
				}
				if !anyOfMatch {
					return errors.New("invalid variant payload")
				}
				var v CreateElicitationForm
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Form = &v
				return nil
			case "url":
				{
					raw, ok := m["elicitationId"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value ElicitationId
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["message"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["mode"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["url"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var anyOfMatch bool
				{
					match := true
					{
						raw, ok := m["sessionId"]
						if !ok {
							match = false
						}
						if ok {
							if string(raw) == "null" {
								match = false
							}
							var value SessionId
							if json.Unmarshal(raw, &value) != nil {
								match = false
							}
						}
					}
					if match {
						anyOfMatch = true
					}
				}
				{
					match := true
					{
						raw, ok := m["requestId"]
						if !ok {
							match = false
						}
						if ok {
							var value RequestId
							if json.Unmarshal(raw, &value) != nil {
								match = false
							}
						}
					}
					if match {
						anyOfMatch = true
					}
				}
				if !anyOfMatch {
					return errors.New("invalid variant payload")
				}
				var v CreateElicitationUrl
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Url = &v
				return nil
			}
		}
		{
			var v CreateElicitationOther
			var match bool = true
			{
				raw, ok := m["message"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value string
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			{
				raw, ok := m["mode"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value string
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			var anyOfMatch bool
			{
				candidateMatch := true
				{
					raw, ok := m["sessionId"]
					if !ok {
						candidateMatch = false
					}
					if ok {
						if string(raw) == "null" {
							candidateMatch = false
						}
						var value SessionId
						if json.Unmarshal(raw, &value) != nil {
							candidateMatch = false
						}
					}
				}
				if candidateMatch {
					anyOfMatch = true
				}
			}
			{
				candidateMatch := true
				{
					raw, ok := m["requestId"]
					if !ok {
						candidateMatch = false
					}
					if ok {
						var value RequestId
						if json.Unmarshal(raw, &value) != nil {
							candidateMatch = false
						}
					}
				}
				if candidateMatch {
					anyOfMatch = true
				}
			}
			if !anyOfMatch {
				match = false
			}
			if match {
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Other = &v
				return nil
			}
		}
	} else {
		if _, ok := err.(*json.UnmarshalTypeError); !ok {
			return err
		}
	}
	var arr []map[string]json.RawMessage
	if json.Unmarshal(b, &arr) == nil {
	}
	return errors.New("no matching variant for union")
}
func (u CreateElicitationRequest) MarshalJSON() ([]byte, error) {
	if u.Form != nil {
		_b, _e := json.Marshal(*u.Form)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["mode"] = json.RawMessage("\"form\"")
		return json.Marshal(m)
	}
	if u.Url != nil {
		_b, _e := json.Marshal(*u.Url)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["mode"] = json.RawMessage("\"url\"")
		return json.Marshal(m)
	}
	if u.Other != nil {
		_b, _e := json.Marshal(*u.Other)
		if _e != nil {
			return []byte{}, _e
		}
		return _b, nil
	}
	return []byte{}, nil
}

func (u *CreateElicitationRequest) Validate() error {
	var count int
	if u.Form != nil {
		count++
	}
	if u.Url != nil {
		count++
	}
	if u.Other != nil {
		count++
	}
	if count < 1 {
		return errors.New("CreateElicitationRequest must have at least one variant set")
	}
	encoded, err := json.Marshal(*u)
	if err != nil {
		return err
	}
	var decoded CreateElicitationRequest
	if err := json.Unmarshal(encoded, &decoded); err != nil {
		return fmt.Errorf("CreateElicitationRequest: %w", err)
	}
	return nil
}

// Response from the client to an elicitation request.
// The user accepted and provided content.
type CreateElicitationAccept struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// Optional. Omitted and 'null' are equivalent and mean no metadata.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta   map[string]json.RawMessage `json:"_meta,omitempty"`
	Action string                     `json:"action"`
	// The user-provided content, if any, as an object matching the requested schema.
	Content map[string]any `json:"content,omitempty"`
}

// The user declined the elicitation.
type CreateElicitationDecline struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// Optional. Omitted and 'null' are equivalent and mean no metadata.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta   map[string]json.RawMessage `json:"_meta,omitempty"`
	Action string                     `json:"action"`
}

// The elicitation was cancelled.
type CreateElicitationCancel struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// Optional. Omitted and 'null' are equivalent and mean no metadata.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta   map[string]json.RawMessage `json:"_meta,omitempty"`
	Action string                     `json:"action"`
}

// Custom or future elicitation action.
//
// Values beginning with '_' are reserved for implementation-specific
// extensions. Unknown values that do not begin with '_' are reserved for
// future ACP variants.
//
// Agents that do not understand this action should preserve the raw
// payload when storing, replaying, proxying, or forwarding elicitation
// responses. They MUST NOT treat it as a known elicitation action.
type CreateElicitationResponseOther = json.RawMessage

type CreateElicitationResponse struct {
	// The user accepted and provided content.
	Accept *CreateElicitationAccept `json:"-"`
	// The user declined the elicitation.
	Decline *CreateElicitationDecline `json:"-"`
	// The elicitation was cancelled.
	Cancel *CreateElicitationCancel `json:"-"`
	// Custom or future elicitation action.
	//
	// Values beginning with '_' are reserved for implementation-specific
	// extensions. Unknown values that do not begin with '_' are reserved for
	// future ACP variants.
	//
	// Agents that do not understand this action should preserve the raw
	// payload when storing, replaying, proxying, or forwarding elicitation
	// responses. They MUST NOT treat it as a known elicitation action.
	Other *CreateElicitationResponseOther `json:"-"`
}

func (u *CreateElicitationResponse) UnmarshalJSON(b []byte) error {
	*u = CreateElicitationResponse{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err == nil {
		{
			var disc string
			if v, ok := m["action"]; ok {
				json.Unmarshal(v, &disc)
			}
			switch disc {
			case "accept":
				{
					raw, ok := m["action"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v CreateElicitationAccept
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Accept = &v
				return nil
			case "decline":
				{
					raw, ok := m["action"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v CreateElicitationDecline
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Decline = &v
				return nil
			case "cancel":
				{
					raw, ok := m["action"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v CreateElicitationCancel
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Cancel = &v
				return nil
			}
		}
		{
			var v CreateElicitationResponseOther
			var match bool = true
			{
				raw, ok := m["action"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value string
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			if match {
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Other = &v
				return nil
			}
		}
	} else {
		if _, ok := err.(*json.UnmarshalTypeError); !ok {
			return err
		}
	}
	var arr []map[string]json.RawMessage
	if json.Unmarshal(b, &arr) == nil {
	}
	return errors.New("no matching variant for union")
}
func (u CreateElicitationResponse) MarshalJSON() ([]byte, error) {
	if u.Accept != nil {
		_b, _e := json.Marshal(*u.Accept)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["action"] = json.RawMessage("\"accept\"")
		return json.Marshal(m)
	}
	if u.Decline != nil {
		_b, _e := json.Marshal(*u.Decline)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["action"] = json.RawMessage("\"decline\"")
		return json.Marshal(m)
	}
	if u.Cancel != nil {
		_b, _e := json.Marshal(*u.Cancel)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["action"] = json.RawMessage("\"cancel\"")
		return json.Marshal(m)
	}
	if u.Other != nil {
		_b, _e := json.Marshal(*u.Other)
		if _e != nil {
			return []byte{}, _e
		}
		return _b, nil
	}
	return []byte{}, nil
}

func (u *CreateElicitationResponse) Validate() error {
	var count int
	if u.Accept != nil {
		count++
	}
	if u.Decline != nil {
		count++
	}
	if u.Cancel != nil {
		count++
	}
	if u.Other != nil {
		count++
	}
	if count < 1 {
		return errors.New("CreateElicitationResponse must have at least one variant set")
	}
	encoded, err := json.Marshal(*u)
	if err != nil {
		return err
	}
	var decoded CreateElicitationResponse
	if err := json.Unmarshal(encoded, &decoded); err != nil {
		return fmt.Errorf("CreateElicitationResponse: %w", err)
	}
	return nil
}

// Request parameters for deleting an existing session from 'session/list'.
//
// Only available if the Agent supports the 'session.delete' capability.
type DeleteSessionRequest struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The ID of the session to delete.
	SessionId SessionId `json:"sessionId"`
}

func (v *DeleteSessionRequest) UnmarshalJSON(b []byte) error {
	*v = DeleteSessionRequest{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["sessionId"]
		if !ok {
			return fmt.Errorf("sessionId is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("sessionId must not be null")
		}
	}
	type Alias DeleteSessionRequest
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = DeleteSessionRequest(a)
	return nil
}

func (v *DeleteSessionRequest) Validate() error {
	return nil
}

// Response from deleting a session.
type DeleteSessionResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
}

func (v *DeleteSessionResponse) Validate() error {
	return nil
}

// File changes produced by a tool call.
//
// 'changes' is authoritative for affected absolute paths and operations.
// 'patch' optionally carries renderable text for some or all of those changes
// and MUST be consistent with 'changes'. Agents SHOULD provide 'patch' whenever
// feasible. Clients MUST handle diffs where 'patch' is omitted or 'null'.
//
// See protocol docs: [Content](https://agentclientprotocol.com/protocol/v2/tool-calls#content)
type Diff struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Structured file changes described by this diff.
	//
	// Clients can use this field without parsing patch text to determine affected paths.
	Changes []DiffChange `json:"changes"`
	// Renderable patch text for some or all of the structured changes.
	//
	// Agents SHOULD provide patch text whenever feasible. Omitted or 'null'
	// means no renderable patch text was provided.
	Patch *DiffPatch `json:"patch,omitempty"`
}

func (v *Diff) UnmarshalJSON(b []byte) error {
	*v = Diff{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["changes"]
		if !ok {
			return fmt.Errorf("changes is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("changes must not be null")
		}
	}
	type Alias Diff
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = Diff(a)
	return nil
}

// One file-level change described by a ['Diff'].
//
// Structured change metadata lets clients identify affected files and
// operations without parsing the text patch.
// A file was added.
type DiffChangeAdd struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// File content kind.
	//
	// Omitted or 'null' means the content kind is unknown.
	FileType *DiffFileType `json:"fileType,omitempty"`
	// MIME type of the file contents.
	//
	// Omitted or 'null' means the MIME type is unknown.
	MimeType  *MediaType `json:"mimeType,omitempty"`
	Operation string     `json:"operation"`
	// Absolute path for the operation.
	Path AbsolutePath `json:"path"`
}

// A file was deleted.
type DiffChangeDelete struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// File content kind.
	//
	// Omitted or 'null' means the content kind is unknown.
	FileType *DiffFileType `json:"fileType,omitempty"`
	// MIME type of the file contents.
	//
	// Omitted or 'null' means the MIME type is unknown.
	MimeType  *MediaType `json:"mimeType,omitempty"`
	Operation string     `json:"operation"`
	// Absolute path for the operation.
	Path AbsolutePath `json:"path"`
}

// A file was modified in place.
type DiffChangeModify struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// File content kind.
	//
	// Omitted or 'null' means the content kind is unknown.
	FileType *DiffFileType `json:"fileType,omitempty"`
	// MIME type of the file contents.
	//
	// Omitted or 'null' means the MIME type is unknown.
	MimeType  *MediaType `json:"mimeType,omitempty"`
	Operation string     `json:"operation"`
	// Absolute path for the operation.
	Path AbsolutePath `json:"path"`
}

// A file was moved or renamed.
type DiffChangeMove struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// File content kind.
	//
	// Omitted or 'null' means the content kind is unknown.
	FileType *DiffFileType `json:"fileType,omitempty"`
	// MIME type of the file contents.
	//
	// Omitted or 'null' means the MIME type is unknown.
	MimeType *MediaType `json:"mimeType,omitempty"`
	// Absolute path before the operation.
	OldPath   AbsolutePath `json:"oldPath"`
	Operation string       `json:"operation"`
	// Absolute path after the operation.
	Path AbsolutePath `json:"path"`
}

// A file was copied.
type DiffChangeCopy struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// File content kind.
	//
	// Omitted or 'null' means the content kind is unknown.
	FileType *DiffFileType `json:"fileType,omitempty"`
	// MIME type of the file contents.
	//
	// Omitted or 'null' means the MIME type is unknown.
	MimeType *MediaType `json:"mimeType,omitempty"`
	// Absolute path before the operation.
	OldPath   AbsolutePath `json:"oldPath"`
	Operation string       `json:"operation"`
	// Absolute path after the operation.
	Path AbsolutePath `json:"path"`
}

// Custom or future file operation.
//
// Values beginning with '_' are reserved for implementation-specific
// extensions. Unknown values that do not begin with '_' are reserved for
// future ACP variants.
type DiffChangeOther = json.RawMessage

type DiffChange struct {
	// A file was added.
	Add *DiffChangeAdd `json:"-"`
	// A file was deleted.
	Delete *DiffChangeDelete `json:"-"`
	// A file was modified in place.
	Modify *DiffChangeModify `json:"-"`
	// A file was moved or renamed.
	Move *DiffChangeMove `json:"-"`
	// A file was copied.
	Copy *DiffChangeCopy `json:"-"`
	// Custom or future file operation.
	//
	// Values beginning with '_' are reserved for implementation-specific
	// extensions. Unknown values that do not begin with '_' are reserved for
	// future ACP variants.
	Other *DiffChangeOther `json:"-"`
}

func (u *DiffChange) UnmarshalJSON(b []byte) error {
	*u = DiffChange{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err == nil {
		{
			var disc string
			if v, ok := m["operation"]; ok {
				json.Unmarshal(v, &disc)
			}
			switch disc {
			case "add":
				{
					raw, ok := m["operation"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["path"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value AbsolutePath
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v DiffChangeAdd
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Add = &v
				return nil
			case "delete":
				{
					raw, ok := m["operation"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["path"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value AbsolutePath
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v DiffChangeDelete
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Delete = &v
				return nil
			case "modify":
				{
					raw, ok := m["operation"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["path"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value AbsolutePath
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v DiffChangeModify
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Modify = &v
				return nil
			case "move":
				{
					raw, ok := m["oldPath"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value AbsolutePath
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["operation"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["path"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value AbsolutePath
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v DiffChangeMove
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Move = &v
				return nil
			case "copy":
				{
					raw, ok := m["oldPath"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value AbsolutePath
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["operation"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["path"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value AbsolutePath
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v DiffChangeCopy
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Copy = &v
				return nil
			}
		}
		{
			var v DiffChangeOther
			var match bool = true
			{
				raw, ok := m["operation"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value string
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			if match {
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Other = &v
				return nil
			}
		}
	} else {
		if _, ok := err.(*json.UnmarshalTypeError); !ok {
			return err
		}
	}
	var arr []map[string]json.RawMessage
	if json.Unmarshal(b, &arr) == nil {
	}
	return errors.New("no matching variant for union")
}
func (u DiffChange) MarshalJSON() ([]byte, error) {
	if u.Add != nil {
		_b, _e := json.Marshal(*u.Add)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["operation"] = json.RawMessage("\"add\"")
		return json.Marshal(m)
	}
	if u.Delete != nil {
		_b, _e := json.Marshal(*u.Delete)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["operation"] = json.RawMessage("\"delete\"")
		return json.Marshal(m)
	}
	if u.Modify != nil {
		_b, _e := json.Marshal(*u.Modify)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["operation"] = json.RawMessage("\"modify\"")
		return json.Marshal(m)
	}
	if u.Move != nil {
		_b, _e := json.Marshal(*u.Move)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["operation"] = json.RawMessage("\"move\"")
		return json.Marshal(m)
	}
	if u.Copy != nil {
		_b, _e := json.Marshal(*u.Copy)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["operation"] = json.RawMessage("\"copy\"")
		return json.Marshal(m)
	}
	if u.Other != nil {
		_b, _e := json.Marshal(*u.Other)
		if _e != nil {
			return []byte{}, _e
		}
		return _b, nil
	}
	return []byte{}, nil
}

func (u *DiffChange) Validate() error {
	var count int
	if u.Add != nil {
		count++
	}
	if u.Delete != nil {
		count++
	}
	if u.Modify != nil {
		count++
	}
	if u.Move != nil {
		count++
	}
	if u.Copy != nil {
		count++
	}
	if u.Other != nil {
		count++
	}
	if count < 1 {
		return errors.New("DiffChange must have at least one variant set")
	}
	encoded, err := json.Marshal(*u)
	if err != nil {
		return err
	}
	var decoded DiffChange
	if err := json.Unmarshal(encoded, &decoded); err != nil {
		return fmt.Errorf("DiffChange: %w", err)
	}
	return nil
}

// Kind of file content represented by a diff change.
type DiffFileType string

const (
	DiffFileTypeText      DiffFileType = "text"
	DiffFileTypeBinary    DiffFileType = "binary"
	DiffFileTypeDirectory DiffFileType = "directory"
	DiffFileTypeSymlink   DiffFileType = "symlink"
)

// Renderable patch text and its format.
type DiffPatch struct {
	// Patch format. The only ACP-defined value is 'git_patch'.
	Format DiffPatchFormat `json:"format"`
	// Patch text in the format named by 'format'.
	Text string `json:"text"`
}

func (v *DiffPatch) UnmarshalJSON(b []byte) error {
	*v = DiffPatch{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["format"]
		if !ok {
			return fmt.Errorf("format is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("format must not be null")
		}
	}
	{
		raw, ok := m["text"]
		if !ok {
			return fmt.Errorf("text is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("text must not be null")
		}
	}
	type Alias DiffPatch
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = DiffPatch(a)
	return nil
}

// Text patch format used by ['DiffPatch'].
type DiffPatchFormat string

const (
	DiffPatchFormatGitPatch DiffPatchFormat = "git_patch"
)

// Operation metadata for add, delete, and modify changes.
type DiffPathChange struct {
	// Absolute path for the operation.
	Path AbsolutePath `json:"path"`
}

func (v *DiffPathChange) UnmarshalJSON(b []byte) error {
	*v = DiffPathChange{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["path"]
		if !ok {
			return fmt.Errorf("path is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("path must not be null")
		}
	}
	type Alias DiffPathChange
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = DiffPathChange(a)
	return nil
}

// Operation metadata for move and copy changes.
type DiffPathPairChange struct {
	// Absolute path before the operation.
	OldPath AbsolutePath `json:"oldPath"`
	// Absolute path after the operation.
	Path AbsolutePath `json:"path"`
}

func (v *DiffPathPairChange) UnmarshalJSON(b []byte) error {
	*v = DiffPathPairChange{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["oldPath"]
		if !ok {
			return fmt.Errorf("oldPath is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("oldPath must not be null")
		}
	}
	{
		raw, ok := m["path"]
		if !ok {
			return fmt.Errorf("path is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("path must not be null")
		}
	}
	type Alias DiffPathPairChange
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = DiffPathPairChange(a)
	return nil
}

// The user accepted the elicitation and provided content.
type ElicitationAcceptAction struct {
	// The user-provided content, if any, as an object matching the requested schema.
	Content map[string]any `json:"content,omitempty"`
}

// Elicitation capabilities supported by the client.
type ElicitationCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// Optional. Omitted and 'null' are equivalent and mean no metadata.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Whether the client supports form-based elicitation.
	//
	// Optional. Omitted and 'null' are equivalent and mean form support is not advertised.
	// Supplying '{}' explicitly advertises form support.
	Form *ElicitationFormCapabilities `json:"form,omitempty"`
	// Whether the client supports URL-based elicitation.
	//
	// Optional. Omitted or 'null' both mean the client does not advertise support.
	// Supplying '{}' means the client supports URL-based elicitation.
	Url *ElicitationUrlCapabilities `json:"url,omitempty"`
}

// Allowed wire representations for ['ElicitationContentValue'].
// String value accepted in elicitation response content.
type ElicitationContentValueString string

// Integer value accepted in elicitation response content.
type ElicitationContentValueInteger int64

// Number value accepted in elicitation response content.
type ElicitationContentValueNumber float64

// Boolean value accepted in elicitation response content.
type ElicitationContentValueBoolean bool

// String array value accepted in elicitation response content.
type ElicitationContentValueStringArray []string

type ElicitationContentValue struct {
	// String value accepted in elicitation response content.
	String *ElicitationContentValueString `json:"-"`
	// Integer value accepted in elicitation response content.
	Integer *ElicitationContentValueInteger `json:"-"`
	// Number value accepted in elicitation response content.
	Number *ElicitationContentValueNumber `json:"-"`
	// Boolean value accepted in elicitation response content.
	Boolean *ElicitationContentValueBoolean `json:"-"`
	// String array value accepted in elicitation response content.
	StringArray *ElicitationContentValueStringArray `json:"-"`
}

func (u *ElicitationContentValue) UnmarshalJSON(b []byte) error {
	*u = ElicitationContentValue{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err == nil {
	} else {
		if _, ok := err.(*json.UnmarshalTypeError); !ok {
			return err
		}
	}
	var arr []map[string]json.RawMessage
	if json.Unmarshal(b, &arr) == nil {
	}
	{
		var v ElicitationContentValueString
		if json.Unmarshal(b, &v) == nil {
			u.String = &v
			return nil
		}
	}
	{
		var v ElicitationContentValueInteger
		if json.Unmarshal(b, &v) == nil {
			u.Integer = &v
			return nil
		}
	}
	{
		var v ElicitationContentValueNumber
		if json.Unmarshal(b, &v) == nil {
			u.Number = &v
			return nil
		}
	}
	{
		var v ElicitationContentValueBoolean
		if json.Unmarshal(b, &v) == nil {
			u.Boolean = &v
			return nil
		}
	}
	{
		var v ElicitationContentValueStringArray
		if json.Unmarshal(b, &v) == nil {
			u.StringArray = &v
			return nil
		}
	}
	return errors.New("no matching variant for union")
}
func (u ElicitationContentValue) MarshalJSON() ([]byte, error) {
	if u.String != nil {
		_b, _e := json.Marshal(*u.String)
		if _e != nil {
			return []byte{}, _e
		}
		return _b, nil
	}
	if u.Integer != nil {
		_b, _e := json.Marshal(*u.Integer)
		if _e != nil {
			return []byte{}, _e
		}
		return _b, nil
	}
	if u.Number != nil {
		_b, _e := json.Marshal(*u.Number)
		if _e != nil {
			return []byte{}, _e
		}
		return _b, nil
	}
	if u.Boolean != nil {
		_b, _e := json.Marshal(*u.Boolean)
		if _e != nil {
			return []byte{}, _e
		}
		return _b, nil
	}
	if u.StringArray != nil {
		_b, _e := json.Marshal(*u.StringArray)
		if _e != nil {
			return []byte{}, _e
		}
		return _b, nil
	}
	return []byte{}, nil
}

func (u *ElicitationContentValue) Validate() error {
	var count int
	if u.String != nil {
		count++
	}
	if u.Integer != nil {
		count++
	}
	if u.Number != nil {
		count++
	}
	if u.Boolean != nil {
		count++
	}
	if u.StringArray != nil {
		count++
	}
	if count < 1 {
		return errors.New("ElicitationContentValue must have at least one variant set")
	}
	encoded, err := json.Marshal(*u)
	if err != nil {
		return err
	}
	var decoded ElicitationContentValue
	if err := json.Unmarshal(encoded, &decoded); err != nil {
		return fmt.Errorf("ElicitationContentValue: %w", err)
	}
	return nil
}

// Form-based elicitation capabilities.
//
// Supplying '{}' means the client supports form-based elicitation.
type ElicitationFormCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// Optional. Omitted and 'null' are equivalent and mean no metadata.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
}

// Form-based elicitation mode where the client renders a form from the provided schema.
// Tied to a session, optionally to a specific tool call within that session.
type ElicitationFormModeSession struct {
	// A JSON Schema describing the form fields to present to the user.
	RequestedSchema ElicitationSchema `json:"requestedSchema"`
	// The session this elicitation is tied to.
	SessionId SessionId `json:"sessionId"`
	// Optional tool call within the session.
	//
	// Optional. Omitted and 'null' are equivalent and mean the elicitation is scoped to the
	// session without a specific tool call.
	ToolCallId *ToolCallId `json:"toolCallId,omitempty"`
}

// Tied to a specific JSON-RPC request outside of a session
// (e.g., during auth/configuration phases before any session is started).
type ElicitationFormModeRequest struct {
	// The request this elicitation is tied to.
	RequestId RequestId `json:"requestId"`
	// A JSON Schema describing the form fields to present to the user.
	RequestedSchema ElicitationSchema `json:"requestedSchema"`
}

type ElicitationFormMode struct {
	// Tied to a session, optionally to a specific tool call within that session.
	Session *ElicitationFormModeSession `json:"-"`
	// Tied to a specific JSON-RPC request outside of a session
	// (e.g., during auth/configuration phases before any session is started).
	Request *ElicitationFormModeRequest `json:"-"`
}

func (u *ElicitationFormMode) UnmarshalJSON(b []byte) error {
	*u = ElicitationFormMode{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err == nil {
		{
			var v ElicitationFormModeSession
			var match bool = true
			{
				raw, ok := m["requestedSchema"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value ElicitationSchema
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			{
				raw, ok := m["sessionId"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value SessionId
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			if match {
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Session = &v
				return nil
			}
		}
		{
			var v ElicitationFormModeRequest
			var match bool = true
			{
				raw, ok := m["requestId"]
				if !ok {
					match = false
				}
				if ok {
					var value RequestId
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			{
				raw, ok := m["requestedSchema"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value ElicitationSchema
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			if match {
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Request = &v
				return nil
			}
		}
	} else {
		if _, ok := err.(*json.UnmarshalTypeError); !ok {
			return err
		}
	}
	var arr []map[string]json.RawMessage
	if json.Unmarshal(b, &arr) == nil {
	}
	return errors.New("no matching variant for union")
}
func (u ElicitationFormMode) MarshalJSON() ([]byte, error) {
	if u.Session != nil {
		_b, _e := json.Marshal(*u.Session)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		return json.Marshal(m)
	}
	if u.Request != nil {
		_b, _e := json.Marshal(*u.Request)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		return json.Marshal(m)
	}
	return []byte{}, nil
}

func (u *ElicitationFormMode) Validate() error {
	var count int
	if u.Session != nil {
		count++
	}
	if u.Request != nil {
		count++
	}
	if count < 1 {
		return errors.New("ElicitationFormMode must have at least one variant set")
	}
	encoded, err := json.Marshal(*u)
	if err != nil {
		return err
	}
	var decoded ElicitationFormMode
	if err := json.Unmarshal(encoded, &decoded); err != nil {
		return fmt.Errorf("ElicitationFormMode: %w", err)
	}
	return nil
}

// Unique identifier for an elicitation.
type ElicitationId string

// Property schema for elicitation form fields.
//
// Each variant corresponds to a JSON Schema '"type"' value.
// Single-select enums use the 'String' variant with 'enum' or 'oneOf' set.
// Multi-select enums use the 'Array' variant.
// String property (or single-select enum when 'enum'/'oneOf' is set).
type ElicitationPropertySchemaString struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// Optional. Omitted and 'null' are equivalent and mean no metadata.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Default value.
	//
	// Optional. Omitted and 'null' are equivalent and mean no default value is provided.
	Default *string `json:"default,omitempty"`
	// Human-readable description.
	//
	// Optional. Omitted and 'null' are equivalent and mean no description is provided.
	Description *string `json:"description,omitempty"`
	// Enum values for untitled single-select enums.
	// Must contain at least one value when present.
	// Optional. Omitted and 'null' are equivalent and mean no untitled single-select choices are
	// declared by 'enum'.
	Enum []string `json:"enum,omitempty"`
	// String format.
	//
	// Optional. Omitted and 'null' are equivalent and mean there is no format constraint.
	Format *StringFormat `json:"format,omitempty"`
	// Maximum string length.
	//
	// Optional. Omitted and 'null' are equivalent and mean there is no maximum length constraint.
	MaxLength *uint32 `json:"maxLength,omitempty"`
	// Minimum string length.
	//
	// Optional. Omitted and 'null' are equivalent and mean there is no minimum length constraint.
	MinLength *uint32 `json:"minLength,omitempty"`
	// Titled enum options for titled single-select enums.
	// Must contain at least one option when present.
	// Optional. Omitted and 'null' are equivalent and mean no titled single-select choices are
	// declared by 'oneOf'.
	OneOf []EnumOption `json:"oneOf,omitempty"`
	// Pattern the string must match.
	//
	// Optional. Omitted and 'null' are equivalent and mean there is no pattern constraint.
	Pattern *string `json:"pattern,omitempty"`
	// Optional title for the property.
	//
	// Optional. Omitted and 'null' are equivalent and mean no title is provided.
	Title *string `json:"title,omitempty"`
	Type  string  `json:"type"`
}

// Number (floating-point) property.
type ElicitationPropertySchemaNumber struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// Optional. Omitted and 'null' are equivalent and mean no metadata.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Default value.
	//
	// Optional. Omitted and 'null' are equivalent and mean no default value is provided.
	Default *float64 `json:"default,omitempty"`
	// Human-readable description.
	//
	// Optional. Omitted and 'null' are equivalent and mean no description is provided.
	Description *string `json:"description,omitempty"`
	// Maximum value (inclusive).
	//
	// Optional. Omitted and 'null' are equivalent and mean there is no inclusive upper bound.
	Maximum *float64 `json:"maximum,omitempty"`
	// Minimum value (inclusive).
	//
	// Optional. Omitted and 'null' are equivalent and mean there is no inclusive lower bound.
	Minimum *float64 `json:"minimum,omitempty"`
	// Optional title for the property.
	//
	// Optional. Omitted and 'null' are equivalent and mean no title is provided.
	Title *string `json:"title,omitempty"`
	Type  string  `json:"type"`
}

// Integer property.
type ElicitationPropertySchemaInteger struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// Optional. Omitted and 'null' are equivalent and mean no metadata.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Default value.
	//
	// Optional. Omitted and 'null' are equivalent and mean no default value is provided.
	Default *int64 `json:"default,omitempty"`
	// Human-readable description.
	//
	// Optional. Omitted and 'null' are equivalent and mean no description is provided.
	Description *string `json:"description,omitempty"`
	// Maximum value (inclusive).
	//
	// Optional. Omitted and 'null' are equivalent and mean there is no inclusive upper bound.
	Maximum *int64 `json:"maximum,omitempty"`
	// Minimum value (inclusive).
	//
	// Optional. Omitted and 'null' are equivalent and mean there is no inclusive lower bound.
	Minimum *int64 `json:"minimum,omitempty"`
	// Optional title for the property.
	//
	// Optional. Omitted and 'null' are equivalent and mean no title is provided.
	Title *string `json:"title,omitempty"`
	Type  string  `json:"type"`
}

// Boolean property.
type ElicitationPropertySchemaBoolean struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// Optional. Omitted and 'null' are equivalent and mean no metadata.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Default value.
	//
	// Optional. Omitted and 'null' are equivalent and mean no default value is provided.
	Default *bool `json:"default,omitempty"`
	// Human-readable description.
	//
	// Optional. Omitted and 'null' are equivalent and mean no description is provided.
	Description *string `json:"description,omitempty"`
	// Optional title for the property.
	//
	// Optional. Omitted and 'null' are equivalent and mean no title is provided.
	Title *string `json:"title,omitempty"`
	Type  string  `json:"type"`
}

// Multi-select array property.
type ElicitationPropertySchemaArray struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// Optional. Omitted and 'null' are equivalent and mean no metadata.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Default selected values.
	//
	// Optional. Omitted and 'null' are equivalent and mean no default selections are provided.
	Default []string `json:"default,omitempty"`
	// Human-readable description.
	//
	// Optional. Omitted and 'null' are equivalent and mean no description is provided.
	Description *string `json:"description,omitempty"`
	// The items definition describing allowed values.
	Items MultiSelectItems `json:"items"`
	// Maximum number of items to select.
	//
	// Optional. Omitted and 'null' are equivalent and mean there is no maximum selection count.
	MaxItems *uint64 `json:"maxItems,omitempty"`
	// Minimum number of items to select.
	//
	// Optional. Omitted and 'null' are equivalent and mean there is no minimum selection count.
	MinItems *uint64 `json:"minItems,omitempty"`
	// Optional title for the property.
	//
	// Optional. Omitted and 'null' are equivalent and mean no title is provided.
	Title *string `json:"title,omitempty"`
	Type  string  `json:"type"`
}

// Custom or future elicitation property schema.
//
// Values beginning with '_' are reserved for implementation-specific
// extensions. Unknown values that do not begin with '_' are reserved for
// future ACP variants.
//
// Clients that do not understand this property schema type should preserve
// the raw schema when storing, replaying, proxying, or forwarding
// elicitation requests. They MUST NOT render it as a known input control.
type ElicitationPropertySchemaOther = json.RawMessage

type ElicitationPropertySchema struct {
	// String property (or single-select enum when 'enum'/'oneOf' is set).
	String *ElicitationPropertySchemaString `json:"-"`
	// Number (floating-point) property.
	Number *ElicitationPropertySchemaNumber `json:"-"`
	// Integer property.
	Integer *ElicitationPropertySchemaInteger `json:"-"`
	// Boolean property.
	Boolean *ElicitationPropertySchemaBoolean `json:"-"`
	// Multi-select array property.
	Array *ElicitationPropertySchemaArray `json:"-"`
	// Custom or future elicitation property schema.
	//
	// Values beginning with '_' are reserved for implementation-specific
	// extensions. Unknown values that do not begin with '_' are reserved for
	// future ACP variants.
	//
	// Clients that do not understand this property schema type should preserve
	// the raw schema when storing, replaying, proxying, or forwarding
	// elicitation requests. They MUST NOT render it as a known input control.
	Other *ElicitationPropertySchemaOther `json:"-"`
}

func (u *ElicitationPropertySchema) UnmarshalJSON(b []byte) error {
	*u = ElicitationPropertySchema{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err == nil {
		{
			var disc string
			if v, ok := m["type"]; ok {
				json.Unmarshal(v, &disc)
			}
			switch disc {
			case "string":
				{
					raw, ok := m["type"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v ElicitationPropertySchemaString
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.String = &v
				return nil
			case "number":
				{
					raw, ok := m["type"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v ElicitationPropertySchemaNumber
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Number = &v
				return nil
			case "integer":
				{
					raw, ok := m["type"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v ElicitationPropertySchemaInteger
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Integer = &v
				return nil
			case "boolean":
				{
					raw, ok := m["type"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v ElicitationPropertySchemaBoolean
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Boolean = &v
				return nil
			case "array":
				{
					raw, ok := m["items"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value MultiSelectItems
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["type"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v ElicitationPropertySchemaArray
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Array = &v
				return nil
			}
		}
		{
			var v ElicitationPropertySchemaOther
			var match bool = true
			{
				raw, ok := m["type"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value string
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			if match {
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Other = &v
				return nil
			}
		}
	} else {
		if _, ok := err.(*json.UnmarshalTypeError); !ok {
			return err
		}
	}
	var arr []map[string]json.RawMessage
	if json.Unmarshal(b, &arr) == nil {
	}
	return errors.New("no matching variant for union")
}
func (u ElicitationPropertySchema) MarshalJSON() ([]byte, error) {
	if u.String != nil {
		_b, _e := json.Marshal(*u.String)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["type"] = json.RawMessage("\"string\"")
		return json.Marshal(m)
	}
	if u.Number != nil {
		_b, _e := json.Marshal(*u.Number)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["type"] = json.RawMessage("\"number\"")
		return json.Marshal(m)
	}
	if u.Integer != nil {
		_b, _e := json.Marshal(*u.Integer)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["type"] = json.RawMessage("\"integer\"")
		return json.Marshal(m)
	}
	if u.Boolean != nil {
		_b, _e := json.Marshal(*u.Boolean)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["type"] = json.RawMessage("\"boolean\"")
		return json.Marshal(m)
	}
	if u.Array != nil {
		_b, _e := json.Marshal(*u.Array)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["type"] = json.RawMessage("\"array\"")
		return json.Marshal(m)
	}
	if u.Other != nil {
		_b, _e := json.Marshal(*u.Other)
		if _e != nil {
			return []byte{}, _e
		}
		return _b, nil
	}
	return []byte{}, nil
}

func (u *ElicitationPropertySchema) Validate() error {
	var count int
	if u.String != nil {
		count++
	}
	if u.Number != nil {
		count++
	}
	if u.Integer != nil {
		count++
	}
	if u.Boolean != nil {
		count++
	}
	if u.Array != nil {
		count++
	}
	if u.Other != nil {
		count++
	}
	if count < 1 {
		return errors.New("ElicitationPropertySchema must have at least one variant set")
	}
	encoded, err := json.Marshal(*u)
	if err != nil {
		return err
	}
	var decoded ElicitationPropertySchema
	if err := json.Unmarshal(encoded, &decoded); err != nil {
		return fmt.Errorf("ElicitationPropertySchema: %w", err)
	}
	return nil
}

// Request-scoped elicitation, tied to a specific JSON-RPC request outside of a session
// (e.g., during auth/configuration phases before any session is started).
type ElicitationRequestScope struct {
	// The request this elicitation is tied to.
	RequestId RequestId `json:"requestId"`
}

func (v *ElicitationRequestScope) UnmarshalJSON(b []byte) error {
	*v = ElicitationRequestScope{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		_, ok := m["requestId"]
		if !ok {
			return fmt.Errorf("requestId is required")
		}
	}
	type Alias ElicitationRequestScope
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = ElicitationRequestScope(a)
	return nil
}

// Type-safe elicitation schema for requesting structured user input.
//
// This represents a JSON Schema object with primitive-typed properties,
// as required by the elicitation specification.
type ElicitationSchema struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// Optional. Omitted and 'null' are equivalent and mean no metadata.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Optional description of what this schema represents.
	//
	// Optional. Omitted and 'null' are equivalent and mean no schema description is provided.
	Description *string `json:"description,omitempty"`
	// Property definitions (must be primitive types).
	//
	// Defaults to {} if unset.
	Properties map[string]any `json:"properties"`
	// List of required property names.
	//
	// Optional. Omitted and 'null' are equivalent and mean no property names are required.
	Required []string `json:"required,omitempty"`
	// Optional title for the schema.
	//
	// Optional. Omitted and 'null' are equivalent and mean no title is provided.
	Title *string `json:"title,omitempty"`
	// Type discriminator. Always '"object"'.
	//
	// Defaults to "object" if unset.
	Type ElicitationSchemaType `json:"type,omitempty"`
}

func (v ElicitationSchema) MarshalJSON() ([]byte, error) {
	type Alias ElicitationSchema
	var a Alias
	a = Alias(v)
	if a.Properties == nil {
		json.Unmarshal([]byte("{}"), &a.Properties)
	}
	return json.Marshal(a)
}

func (v *ElicitationSchema) UnmarshalJSON(b []byte) error {
	*v = ElicitationSchema{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	type Alias ElicitationSchema
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	{
		_rm, _ok := m["properties"]
		if !_ok || (string(_rm) == "null") {
			json.Unmarshal([]byte("{}"), &a.Properties)
		}
	}
	{
		_rm, _ok := m["type"]
		if !_ok || (string(_rm) == "null") {
			json.Unmarshal([]byte("\"object\""), &a.Type)
		}
	}
	*v = ElicitationSchema(a)
	return nil
}

// Type discriminator for elicitation schemas.
type ElicitationSchemaType string

const (
	ElicitationSchemaTypeObject ElicitationSchemaType = "object"
)

// Session-scoped elicitation, optionally tied to a specific tool call.
//
// When 'tool_call_id' is set, the elicitation is tied to a specific tool call.
// This is useful when an agent receives an elicitation from an MCP server
// during a tool call and needs to redirect it to the user.
type ElicitationSessionScope struct {
	// The session this elicitation is tied to.
	SessionId SessionId `json:"sessionId"`
	// Optional tool call within the session.
	//
	// Optional. Omitted and 'null' are equivalent and mean the elicitation is scoped to the
	// session without a specific tool call.
	ToolCallId *ToolCallId `json:"toolCallId,omitempty"`
}

func (v *ElicitationSessionScope) UnmarshalJSON(b []byte) error {
	*v = ElicitationSessionScope{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["sessionId"]
		if !ok {
			return fmt.Errorf("sessionId is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("sessionId must not be null")
		}
	}
	type Alias ElicitationSessionScope
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = ElicitationSessionScope(a)
	return nil
}

// URL-based elicitation capabilities.
//
// Supplying '{}' means the client supports URL-based elicitation.
type ElicitationUrlCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// Optional. Omitted and 'null' are equivalent and mean no metadata.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
}

// URL-based elicitation mode where the client directs the user to a URL.
// Tied to a session, optionally to a specific tool call within that session.
type ElicitationUrlModeSession struct {
	// The unique identifier for this elicitation.
	ElicitationId ElicitationId `json:"elicitationId"`
	// The session this elicitation is tied to.
	SessionId SessionId `json:"sessionId"`
	// Optional tool call within the session.
	//
	// Optional. Omitted and 'null' are equivalent and mean the elicitation is scoped to the
	// session without a specific tool call.
	ToolCallId *ToolCallId `json:"toolCallId,omitempty"`
	// The URL to direct the user to.
	Url string `json:"url"`
}

// Tied to a specific JSON-RPC request outside of a session
// (e.g., during auth/configuration phases before any session is started).
type ElicitationUrlModeRequest struct {
	// The unique identifier for this elicitation.
	ElicitationId ElicitationId `json:"elicitationId"`
	// The request this elicitation is tied to.
	RequestId RequestId `json:"requestId"`
	// The URL to direct the user to.
	Url string `json:"url"`
}

type ElicitationUrlMode struct {
	// Tied to a session, optionally to a specific tool call within that session.
	Session *ElicitationUrlModeSession `json:"-"`
	// Tied to a specific JSON-RPC request outside of a session
	// (e.g., during auth/configuration phases before any session is started).
	Request *ElicitationUrlModeRequest `json:"-"`
}

func (u *ElicitationUrlMode) UnmarshalJSON(b []byte) error {
	*u = ElicitationUrlMode{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err == nil {
		{
			var v ElicitationUrlModeSession
			var match bool = true
			{
				raw, ok := m["elicitationId"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value ElicitationId
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			{
				raw, ok := m["sessionId"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value SessionId
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			{
				raw, ok := m["url"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value string
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			if match {
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Session = &v
				return nil
			}
		}
		{
			var v ElicitationUrlModeRequest
			var match bool = true
			{
				raw, ok := m["elicitationId"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value ElicitationId
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			{
				raw, ok := m["requestId"]
				if !ok {
					match = false
				}
				if ok {
					var value RequestId
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			{
				raw, ok := m["url"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value string
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			if match {
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Request = &v
				return nil
			}
		}
	} else {
		if _, ok := err.(*json.UnmarshalTypeError); !ok {
			return err
		}
	}
	var arr []map[string]json.RawMessage
	if json.Unmarshal(b, &arr) == nil {
	}
	return errors.New("no matching variant for union")
}
func (u ElicitationUrlMode) MarshalJSON() ([]byte, error) {
	if u.Session != nil {
		_b, _e := json.Marshal(*u.Session)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		return json.Marshal(m)
	}
	if u.Request != nil {
		_b, _e := json.Marshal(*u.Request)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		return json.Marshal(m)
	}
	return []byte{}, nil
}

func (u *ElicitationUrlMode) Validate() error {
	var count int
	if u.Session != nil {
		count++
	}
	if u.Request != nil {
		count++
	}
	if count < 1 {
		return errors.New("ElicitationUrlMode must have at least one variant set")
	}
	encoded, err := json.Marshal(*u)
	if err != nil {
		return err
	}
	var decoded ElicitationUrlMode
	if err := json.Unmarshal(encoded, &decoded); err != nil {
		return fmt.Errorf("ElicitationUrlMode: %w", err)
	}
	return nil
}

// The contents of a resource, embedded into a prompt or tool call result.
type EmbeddedResource struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Optional annotations that help clients decide how to display or route this content.
	Annotations *Annotations `json:"annotations,omitempty"`
	// Embedded resource payload, either text or binary data.
	Resource EmbeddedResourceResource `json:"resource"`
}

func (v *EmbeddedResource) UnmarshalJSON(b []byte) error {
	*v = EmbeddedResource{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["resource"]
		if !ok {
			return fmt.Errorf("resource is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("resource must not be null")
		}
	}
	type Alias EmbeddedResource
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = EmbeddedResource(a)
	return nil
}

// Resource content that can be embedded in a message.
type EmbeddedResourceResource struct {
	// Text resource contents embedded directly in the message.
	TextResourceContents *TextResourceContents `json:"-"`
	// Binary resource contents embedded directly in the message.
	BlobResourceContents *BlobResourceContents `json:"-"`
}

func (u *EmbeddedResourceResource) UnmarshalJSON(b []byte) error {
	*u = EmbeddedResourceResource{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err == nil {
		{
			var v TextResourceContents
			var match bool = true
			{
				raw, ok := m["text"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value string
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			{
				raw, ok := m["uri"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value string
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			if match {
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.TextResourceContents = &v
				return nil
			}
		}
		{
			var v BlobResourceContents
			var match bool = true
			{
				raw, ok := m["blob"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value string
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			{
				raw, ok := m["uri"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value string
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			if match {
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.BlobResourceContents = &v
				return nil
			}
		}
	} else {
		if _, ok := err.(*json.UnmarshalTypeError); !ok {
			return err
		}
	}
	var arr []map[string]json.RawMessage
	if json.Unmarshal(b, &arr) == nil {
	}
	return errors.New("no matching variant for union")
}
func (u EmbeddedResourceResource) MarshalJSON() ([]byte, error) {
	if u.TextResourceContents != nil {
		_b, _e := json.Marshal(*u.TextResourceContents)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		return json.Marshal(m)
	}
	if u.BlobResourceContents != nil {
		_b, _e := json.Marshal(*u.BlobResourceContents)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		return json.Marshal(m)
	}
	return []byte{}, nil
}

func (u *EmbeddedResourceResource) Validate() error {
	var count int
	if u.TextResourceContents != nil {
		count++
	}
	if u.BlobResourceContents != nil {
		count++
	}
	if count < 1 {
		return errors.New("EmbeddedResourceResource must have at least one variant set")
	}
	encoded, err := json.Marshal(*u)
	if err != nil {
		return err
	}
	var decoded EmbeddedResourceResource
	if err := json.Unmarshal(encoded, &decoded); err != nil {
		return fmt.Errorf("EmbeddedResourceResource: %w", err)
	}
	return nil
}

// A titled enum option with a const value, human-readable title, and optional description.
type EnumOption struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// Optional. Omitted and 'null' are equivalent and mean no metadata.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The constant value for this option.
	Const string `json:"const"`
	// Human-readable description.
	//
	// Optional. Omitted and 'null' are equivalent and mean no description is provided.
	Description *string `json:"description,omitempty"`
	// Human-readable title for this option.
	Title string `json:"title"`
}

func (v *EnumOption) UnmarshalJSON(b []byte) error {
	*v = EnumOption{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["const"]
		if !ok {
			return fmt.Errorf("const is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("const must not be null")
		}
	}
	{
		raw, ok := m["title"]
		if !ok {
			return fmt.Errorf("title is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("title must not be null")
		}
	}
	type Alias EnumOption
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = EnumOption(a)
	return nil
}

// An environment variable to set when launching a process.
type EnvVariable struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The name of the environment variable.
	Name string `json:"name"`
	// The value to set for the environment variable.
	Value string `json:"value"`
}

func (v *EnvVariable) UnmarshalJSON(b []byte) error {
	*v = EnvVariable{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["name"]
		if !ok {
			return fmt.Errorf("name is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("name must not be null")
		}
	}
	{
		raw, ok := m["value"]
		if !ok {
			return fmt.Errorf("value is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("value must not be null")
		}
	}
	type Alias EnvVariable
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = EnvVariable(a)
	return nil
}

// JSON-RPC error object.
//
// Represents an error that occurred during method execution, following the
// JSON-RPC 2.0 error object specification with optional additional data.
//
// See protocol docs: [JSON-RPC Error Object](https://www.jsonrpc.org/specification#error_object)
type Error struct {
	// A number indicating the error type that occurred.
	// This must be an integer as defined in the JSON-RPC specification.
	Code ErrorCode `json:"code"`
	// Optional primitive or structured value that contains additional information about the error.
	// This may include debugging information or context-specific details.
	Data any `json:"data,omitempty"`
	// A string providing a short description of the error.
	// The message should be limited to a concise single sentence.
	Message string `json:"message"`
}

func (v *Error) UnmarshalJSON(b []byte) error {
	*v = Error{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["code"]
		if !ok {
			return fmt.Errorf("code is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("code must not be null")
		}
	}
	{
		raw, ok := m["message"]
		if !ok {
			return fmt.Errorf("message is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("message must not be null")
		}
	}
	type Alias Error
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = Error(a)
	return nil
}

// Predefined error codes for common JSON-RPC and ACP-specific errors.
//
// These codes follow the JSON-RPC 2.0 specification for standard errors
// and use the reserved range (-32000 to -32099) for protocol-specific errors.
// **Parse error**: Invalid JSON was received by the server.
// An error occurred on the server while parsing the JSON text.
type ErrorCodeParseError int32

// **Invalid request**: The JSON sent is not a valid Request object.
type ErrorCodeInvalidRequest int32

// **Method not found**: The method does not exist or is not available.
type ErrorCodeMethodNotFound int32

// **Invalid params**: Invalid method parameter(s).
type ErrorCodeInvalidParams int32

// **Internal error**: Internal JSON-RPC error.
// Reserved for implementation-defined server errors.
type ErrorCodeInternalError int32

// **Request cancelled**: Execution of the method was aborted either due to a cancellation request from the caller or
// because of resource constraints or shutdown.
type ErrorCodeRequestCancelled int32

// **Authentication required**: Authentication is required before this operation can be performed.
type ErrorCodeAuthenticationRequired int32

// **Resource not found**: A given resource, such as a file, was not found.
type ErrorCodeResourceNotFound int32

// Other undefined error code.
type ErrorCodeOther int32

type ErrorCode struct {
	// **Parse error**: Invalid JSON was received by the server.
	// An error occurred on the server while parsing the JSON text.
	ParseError *ErrorCodeParseError `json:"-"`
	// **Invalid request**: The JSON sent is not a valid Request object.
	InvalidRequest *ErrorCodeInvalidRequest `json:"-"`
	// **Method not found**: The method does not exist or is not available.
	MethodNotFound *ErrorCodeMethodNotFound `json:"-"`
	// **Invalid params**: Invalid method parameter(s).
	InvalidParams *ErrorCodeInvalidParams `json:"-"`
	// **Internal error**: Internal JSON-RPC error.
	// Reserved for implementation-defined server errors.
	InternalError *ErrorCodeInternalError `json:"-"`
	// **Request cancelled**: Execution of the method was aborted either due to a cancellation request from the caller or
	// because of resource constraints or shutdown.
	RequestCancelled *ErrorCodeRequestCancelled `json:"-"`
	// **Authentication required**: Authentication is required before this operation can be performed.
	AuthenticationRequired *ErrorCodeAuthenticationRequired `json:"-"`
	// **Resource not found**: A given resource, such as a file, was not found.
	ResourceNotFound *ErrorCodeResourceNotFound `json:"-"`
	// Other undefined error code.
	Other *ErrorCodeOther `json:"-"`
}

func (u *ErrorCode) UnmarshalJSON(b []byte) error {
	*u = ErrorCode{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err == nil {
	} else {
		if _, ok := err.(*json.UnmarshalTypeError); !ok {
			return err
		}
	}
	var arr []map[string]json.RawMessage
	if json.Unmarshal(b, &arr) == nil {
	}
	{
		var v ErrorCodeParseError
		if json.Unmarshal(b, &v) == nil && v == ErrorCodeParseError(-32700.0) {
			u.ParseError = &v
			return nil
		}
	}
	{
		var v ErrorCodeInvalidRequest
		if json.Unmarshal(b, &v) == nil && v == ErrorCodeInvalidRequest(-32600.0) {
			u.InvalidRequest = &v
			return nil
		}
	}
	{
		var v ErrorCodeMethodNotFound
		if json.Unmarshal(b, &v) == nil && v == ErrorCodeMethodNotFound(-32601.0) {
			u.MethodNotFound = &v
			return nil
		}
	}
	{
		var v ErrorCodeInvalidParams
		if json.Unmarshal(b, &v) == nil && v == ErrorCodeInvalidParams(-32602.0) {
			u.InvalidParams = &v
			return nil
		}
	}
	{
		var v ErrorCodeInternalError
		if json.Unmarshal(b, &v) == nil && v == ErrorCodeInternalError(-32603.0) {
			u.InternalError = &v
			return nil
		}
	}
	{
		var v ErrorCodeRequestCancelled
		if json.Unmarshal(b, &v) == nil && v == ErrorCodeRequestCancelled(-32800.0) {
			u.RequestCancelled = &v
			return nil
		}
	}
	{
		var v ErrorCodeAuthenticationRequired
		if json.Unmarshal(b, &v) == nil && v == ErrorCodeAuthenticationRequired(-32000.0) {
			u.AuthenticationRequired = &v
			return nil
		}
	}
	{
		var v ErrorCodeResourceNotFound
		if json.Unmarshal(b, &v) == nil && v == ErrorCodeResourceNotFound(-32002.0) {
			u.ResourceNotFound = &v
			return nil
		}
	}
	{
		var v ErrorCodeOther
		if json.Unmarshal(b, &v) == nil {
			u.Other = &v
			return nil
		}
	}
	return errors.New("no matching variant for union")
}
func (u ErrorCode) MarshalJSON() ([]byte, error) {
	if u.ParseError != nil {
		return json.Marshal(ErrorCodeParseError(-32700.0))
	}
	if u.InvalidRequest != nil {
		return json.Marshal(ErrorCodeInvalidRequest(-32600.0))
	}
	if u.MethodNotFound != nil {
		return json.Marshal(ErrorCodeMethodNotFound(-32601.0))
	}
	if u.InvalidParams != nil {
		return json.Marshal(ErrorCodeInvalidParams(-32602.0))
	}
	if u.InternalError != nil {
		return json.Marshal(ErrorCodeInternalError(-32603.0))
	}
	if u.RequestCancelled != nil {
		return json.Marshal(ErrorCodeRequestCancelled(-32800.0))
	}
	if u.AuthenticationRequired != nil {
		return json.Marshal(ErrorCodeAuthenticationRequired(-32000.0))
	}
	if u.ResourceNotFound != nil {
		return json.Marshal(ErrorCodeResourceNotFound(-32002.0))
	}
	if u.Other != nil {
		_b, _e := json.Marshal(*u.Other)
		if _e != nil {
			return []byte{}, _e
		}
		return _b, nil
	}
	return []byte{}, nil
}

func (u *ErrorCode) Validate() error {
	var count int
	if u.ParseError != nil {
		count++
	}
	if u.InvalidRequest != nil {
		count++
	}
	if u.MethodNotFound != nil {
		count++
	}
	if u.InvalidParams != nil {
		count++
	}
	if u.InternalError != nil {
		count++
	}
	if u.RequestCancelled != nil {
		count++
	}
	if u.AuthenticationRequired != nil {
		count++
	}
	if u.ResourceNotFound != nil {
		count++
	}
	if u.Other != nil {
		count++
	}
	if count < 1 {
		return errors.New("ErrorCode must have at least one variant set")
	}
	encoded, err := json.Marshal(*u)
	if err != nil {
		return err
	}
	var decoded ErrorCode
	if err := json.Unmarshal(encoded, &decoded); err != nil {
		return fmt.Errorf("ErrorCode: %w", err)
	}
	return nil
}

// Allows the Agent to send an arbitrary notification that is not part of the ACP spec.
// Extension notifications provide a way to send one-way messages for custom functionality
// while maintaining protocol compatibility.
//
// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
// ExtNotification is a union or complex schema; represented generically.
type ExtNotification any

// Allows for sending an arbitrary request that is not part of the ACP spec.
// Extension methods provide a way to add custom functionality while maintaining
// protocol compatibility.
//
// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
// ExtRequest is a union or complex schema; represented generically.
type ExtRequest any

// Allows for sending an arbitrary response to an ['ExtRequest'] that is not part of the ACP spec.
// Extension methods provide a way to add custom functionality while maintaining
// protocol compatibility.
//
// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
// ExtResponse is a union or complex schema; represented generically.
type ExtResponse any

// An HTTP header to set when making requests to the MCP server.
type HttpHeader struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The name of the HTTP header.
	Name string `json:"name"`
	// The value to set for the HTTP header.
	Value string `json:"value"`
}

func (v *HttpHeader) UnmarshalJSON(b []byte) error {
	*v = HttpHeader{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["name"]
		if !ok {
			return fmt.Errorf("name is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("name must not be null")
		}
	}
	{
		raw, ok := m["value"]
		if !ok {
			return fmt.Errorf("value is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("value must not be null")
		}
	}
	type Alias HttpHeader
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = HttpHeader(a)
	return nil
}

// An optionally-sized icon that can be displayed in a user interface.
type Icon struct {
	// Optional MIME type override if the source MIME type is missing or generic.
	MimeType *MediaType `json:"mimeType,omitempty"`
	// Optional array of strings that specify sizes at which the icon can be used.
	// Each string should be in 'WxH' format (e.g., '"48x48"', '"96x96"') or
	// '"any"' for scalable formats like SVG.
	//
	// If not provided, the client should assume that the icon can be used at any size.
	Sizes []string `json:"sizes,omitempty"`
	// A standard URI pointing to an icon resource.
	Src string `json:"src"`
	// Optional theme this icon is designed for.
	Theme *IconTheme `json:"theme,omitempty"`
}

func (v *Icon) UnmarshalJSON(b []byte) error {
	*v = Icon{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["src"]
		if !ok {
			return fmt.Errorf("src is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("src must not be null")
		}
	}
	type Alias Icon
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = Icon(a)
	return nil
}

// Theme an icon is designed for.
type IconTheme string

const (
	IconThemeLight IconTheme = "light"
	IconThemeDark  IconTheme = "dark"
)

// The agent is ready to process a new prompt.
type IdleStateUpdate struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Indicates why foreground work stopped.
	//
	// Optional. Omitted or 'null' both mean the agent is not reporting a stop reason.
	// Agents SHOULD include this when the idle transition ends foreground work.
	StopReason *StopReason `json:"stopReason,omitempty"`
}

// An image provided to or from an LLM.
type ImageContent struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Optional annotations that help clients decide how to display or route this content.
	Annotations *Annotations `json:"annotations,omitempty"`
	// Base64-encoded media payload.
	Data string `json:"data"`
	// MIME type describing the encoded media payload.
	MimeType MediaType `json:"mimeType"`
	// URI associated with this resource or media payload.
	Uri *string `json:"uri,omitempty"`
}

func (v *ImageContent) UnmarshalJSON(b []byte) error {
	*v = ImageContent{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["data"]
		if !ok {
			return fmt.Errorf("data is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("data must not be null")
		}
	}
	{
		raw, ok := m["mimeType"]
		if !ok {
			return fmt.Errorf("mimeType is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("mimeType must not be null")
		}
	}
	type Alias ImageContent
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = ImageContent(a)
	return nil
}

// Metadata about the implementation of the client or agent.
// Describes the name and version of an ACP implementation, with an optional
// title for UI representation.
type Implementation struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Intended for programmatic or logical use, but can be used as a display
	// name fallback if title isn’t present.
	Name string `json:"name"`
	// Intended for UI and end-user contexts — optimized to be human-readable
	// and easily understood.
	//
	// If not provided, the name should be used for display.
	Title *string `json:"title,omitempty"`
	// Version of the implementation. Can be displayed to the user or used
	// for debugging or metrics purposes. (e.g. "1.0.0").
	Version string `json:"version"`
}

func (v *Implementation) UnmarshalJSON(b []byte) error {
	*v = Implementation{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["name"]
		if !ok {
			return fmt.Errorf("name is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("name must not be null")
		}
	}
	{
		raw, ok := m["version"]
		if !ok {
			return fmt.Errorf("version is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("version must not be null")
		}
	}
	type Alias Implementation
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = Implementation(a)
	return nil
}

// Request parameters for the initialize method.
//
// Sent by the client to establish connection and negotiate capabilities.
//
// See protocol docs: [Initialization](https://agentclientprotocol.com/protocol/v2/initialization)
type InitializeRequest struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Capabilities supported by the client.
	//
	// Defaults to {} if unset.
	Capabilities ClientCapabilities `json:"capabilities,omitempty"`
	// Information about the implementation sending this initialize request.
	Info Implementation `json:"info"`
	// The latest protocol version supported by the client.
	ProtocolVersion ProtocolVersion `json:"protocolVersion"`
}

func (v InitializeRequest) MarshalJSON() ([]byte, error) {
	type Alias InitializeRequest
	var a Alias
	a = Alias(v)
	return json.Marshal(a)
}

func (v *InitializeRequest) UnmarshalJSON(b []byte) error {
	*v = InitializeRequest{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["info"]
		if !ok {
			return fmt.Errorf("info is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("info must not be null")
		}
	}
	{
		raw, ok := m["protocolVersion"]
		if !ok {
			return fmt.Errorf("protocolVersion is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("protocolVersion must not be null")
		}
	}
	type Alias InitializeRequest
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	{
		_rm, _ok := m["capabilities"]
		if !_ok || (string(_rm) == "null") {
			json.Unmarshal([]byte("{}"), &a.Capabilities)
		}
	}
	*v = InitializeRequest(a)
	return nil
}

func (v *InitializeRequest) Validate() error {
	return nil
}

// Response to the 'initialize' method.
//
// Contains the negotiated protocol version and agent capabilities.
//
// See protocol docs: [Initialization](https://agentclientprotocol.com/protocol/v2/initialization)
type InitializeResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Authentication methods supported by the agent.
	//
	// Optional. Omitted or empty means the agent does not advertise the
	// authentication method surface. Supplying one or more valid methods means
	// the agent MUST support both 'auth/login' and 'auth/logout'.
	AuthMethods []AuthMethod `json:"authMethods,omitempty"`
	// Capabilities supported by the agent.
	//
	// Defaults to {} if unset.
	Capabilities AgentCapabilities `json:"capabilities,omitempty"`
	// Information about the implementation sending this initialize response.
	Info Implementation `json:"info"`
	// The protocol version the client specified if supported by the agent,
	// or the latest protocol version supported by the agent.
	//
	// The client should disconnect, if it doesn't support this version.
	ProtocolVersion ProtocolVersion `json:"protocolVersion"`
}

func (v InitializeResponse) MarshalJSON() ([]byte, error) {
	type Alias InitializeResponse
	var a Alias
	a = Alias(v)
	return json.Marshal(a)
}

func (v *InitializeResponse) UnmarshalJSON(b []byte) error {
	*v = InitializeResponse{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["info"]
		if !ok {
			return fmt.Errorf("info is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("info must not be null")
		}
	}
	{
		raw, ok := m["protocolVersion"]
		if !ok {
			return fmt.Errorf("protocolVersion is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("protocolVersion must not be null")
		}
	}
	type Alias InitializeResponse
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	{
		_rm, _ok := m["capabilities"]
		if !_ok || (string(_rm) == "null") {
			json.Unmarshal([]byte("{}"), &a.Capabilities)
		}
	}
	*v = InitializeResponse(a)
	return nil
}

func (v *InitializeResponse) Validate() error {
	return nil
}

// Schema for integer properties in an elicitation form.
type IntegerPropertySchema struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// Optional. Omitted and 'null' are equivalent and mean no metadata.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Default value.
	//
	// Optional. Omitted and 'null' are equivalent and mean no default value is provided.
	Default *int64 `json:"default,omitempty"`
	// Human-readable description.
	//
	// Optional. Omitted and 'null' are equivalent and mean no description is provided.
	Description *string `json:"description,omitempty"`
	// Maximum value (inclusive).
	//
	// Optional. Omitted and 'null' are equivalent and mean there is no inclusive upper bound.
	Maximum *int64 `json:"maximum,omitempty"`
	// Minimum value (inclusive).
	//
	// Optional. Omitted and 'null' are equivalent and mean there is no inclusive lower bound.
	Minimum *int64 `json:"minimum,omitempty"`
	// Optional title for the property.
	//
	// Optional. Omitted and 'null' are equivalent and mean no title is provided.
	Title *string `json:"title,omitempty"`
}

// Request parameters for listing existing sessions.
type ListSessionsRequest struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Opaque cursor token from a previous response's nextCursor field for cursor-based pagination
	Cursor *SessionListCursor `json:"cursor,omitempty"`
	// Filter sessions by working directory. Must be an absolute path.
	Cwd *AbsolutePath `json:"cwd,omitempty"`
}

func (v *ListSessionsRequest) Validate() error {
	return nil
}

// Response from listing sessions.
type ListSessionsResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Opaque cursor token. If present, pass this in the next request's cursor parameter
	// to fetch the next page. If absent, there are no more results.
	NextCursor *SessionListCursor `json:"nextCursor,omitempty"`
	// Array of session information objects.
	Sessions []SessionInfo `json:"sessions"`
}

func (v *ListSessionsResponse) UnmarshalJSON(b []byte) error {
	*v = ListSessionsResponse{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["sessions"]
		if !ok {
			return fmt.Errorf("sessions is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("sessions must not be null")
		}
	}
	type Alias ListSessionsResponse
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = ListSessionsResponse(a)
	return nil
}

func (v *ListSessionsResponse) Validate() error {
	if v.Sessions == nil {
		return fmt.Errorf("sessions is required")
	}
	return nil
}

// Request parameters for the 'auth/login' method.
//
// Specifies which authentication method to use.
//
// Agents MUST support this method when their 'initialize' response advertised
// at least one valid authentication method. Clients MUST NOT call this method
// when 'authMethods' was omitted or empty.
type LoginAuthRequest struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The ID of the authentication method to use.
	// Must be one of the methods advertised in the initialize response.
	MethodId AuthMethodId `json:"methodId"`
}

func (v *LoginAuthRequest) UnmarshalJSON(b []byte) error {
	*v = LoginAuthRequest{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["methodId"]
		if !ok {
			return fmt.Errorf("methodId is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("methodId must not be null")
		}
	}
	type Alias LoginAuthRequest
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = LoginAuthRequest(a)
	return nil
}

func (v *LoginAuthRequest) Validate() error {
	return nil
}

// Response to the 'auth/login' method.
type LoginAuthResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
}

func (v *LoginAuthResponse) Validate() error {
	return nil
}

// Request parameters for the 'auth/logout' method.
//
// Terminates the current authenticated session.
//
// Agents MUST support this method when their 'initialize' response advertised
// at least one valid authentication method. Clients MUST NOT call this method
// when 'authMethods' was omitted or empty.
type LogoutAuthRequest struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
}

func (v *LogoutAuthRequest) Validate() error {
	return nil
}

// Response to the 'auth/logout' method.
type LogoutAuthResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
}

func (v *LogoutAuthResponse) Validate() error {
	return nil
}

// MCP capabilities supported by the agent for session lifecycle requests.
type McpCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Agent supports ['McpServer::Http'].
	//
	// Optional. Omitted or 'null' both mean the agent does not advertise support.
	// Supplying '{}' means the agent supports HTTP MCP server transports.
	Http *McpHttpCapabilities `json:"http,omitempty"`
	// Agent supports ['McpServer::Stdio'].
	//
	// Optional. Omitted or 'null' both mean the agent does not advertise support.
	// Supplying '{}' means the agent supports stdio MCP server transports.
	Stdio *McpStdioCapabilities `json:"stdio,omitempty"`
}

// Capabilities for HTTP MCP server transports.
//
// Supplying '{}' means the agent supports HTTP MCP server transports.
type McpHttpCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
}

// Configuration for connecting to an MCP (Model Context Protocol) server.
//
// MCP servers provide tools and context that the agent can use when
// processing prompts.
//
// See protocol docs: [MCP Servers](https://agentclientprotocol.com/protocol/v2/session-setup#mcp-servers)
// HTTP transport configuration
//
// Only available when the Agent capabilities include 'session.mcp.http'.
type McpServerHttpInline struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// HTTP headers to set when making requests to the MCP server.
	Headers []HttpHeader `json:"headers,omitempty"`
	// Human-readable name identifying this MCP server.
	Name string `json:"name"`
	Type string `json:"type"`
	// URL to the MCP server.
	Url string `json:"url"`
}

// Stdio transport configuration
//
// Only available when the Agent capabilities include 'session.mcp.stdio'.
type McpServerStdioInline struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Command-line arguments to pass to the MCP server.
	Args []string `json:"args,omitempty"`
	// Absolute path to the MCP server executable.
	Command AbsolutePath `json:"command"`
	// Environment variables to set when launching the MCP server.
	Env []EnvVariable `json:"env,omitempty"`
	// Human-readable name identifying this MCP server.
	Name string `json:"name"`
	Type string `json:"type"`
}

// Custom or future MCP server transport configuration.
//
// Values beginning with '_' are reserved for implementation-specific
// extensions. Unknown values that do not begin with '_' are reserved for
// future ACP variants.
//
// Receivers that do not understand this transport should preserve the raw
// payload when storing, replaying, proxying, or forwarding session setup
// data, and otherwise ignore it or reject the server configuration.
type McpServerOther = json.RawMessage

type McpServer struct {
	// HTTP transport configuration
	//
	// Only available when the Agent capabilities include 'session.mcp.http'.
	Http *McpServerHttpInline `json:"-"`
	// Stdio transport configuration
	//
	// Only available when the Agent capabilities include 'session.mcp.stdio'.
	Stdio *McpServerStdioInline `json:"-"`
	// Custom or future MCP server transport configuration.
	//
	// Values beginning with '_' are reserved for implementation-specific
	// extensions. Unknown values that do not begin with '_' are reserved for
	// future ACP variants.
	//
	// Receivers that do not understand this transport should preserve the raw
	// payload when storing, replaying, proxying, or forwarding session setup
	// data, and otherwise ignore it or reject the server configuration.
	Other *McpServerOther `json:"-"`
}

func (u *McpServer) UnmarshalJSON(b []byte) error {
	*u = McpServer{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err == nil {
		{
			var disc string
			if v, ok := m["type"]; ok {
				json.Unmarshal(v, &disc)
			}
			switch disc {
			case "http":
				{
					raw, ok := m["name"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["type"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["url"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v McpServerHttpInline
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Http = &v
				return nil
			case "stdio":
				{
					raw, ok := m["command"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value AbsolutePath
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["name"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["type"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v McpServerStdioInline
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Stdio = &v
				return nil
			}
		}
		{
			var v McpServerOther
			var match bool = true
			{
				raw, ok := m["type"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value string
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			if match {
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Other = &v
				return nil
			}
		}
	} else {
		if _, ok := err.(*json.UnmarshalTypeError); !ok {
			return err
		}
	}
	var arr []map[string]json.RawMessage
	if json.Unmarshal(b, &arr) == nil {
	}
	return errors.New("no matching variant for union")
}
func (u McpServer) MarshalJSON() ([]byte, error) {
	if u.Http != nil {
		_b, _e := json.Marshal(*u.Http)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["type"] = json.RawMessage("\"http\"")
		return json.Marshal(m)
	}
	if u.Stdio != nil {
		_b, _e := json.Marshal(*u.Stdio)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["type"] = json.RawMessage("\"stdio\"")
		return json.Marshal(m)
	}
	if u.Other != nil {
		_b, _e := json.Marshal(*u.Other)
		if _e != nil {
			return []byte{}, _e
		}
		return _b, nil
	}
	return []byte{}, nil
}

func (u *McpServer) Validate() error {
	var count int
	if u.Http != nil {
		count++
	}
	if u.Stdio != nil {
		count++
	}
	if u.Other != nil {
		count++
	}
	if count < 1 {
		return errors.New("McpServer must have at least one variant set")
	}
	encoded, err := json.Marshal(*u)
	if err != nil {
		return err
	}
	var decoded McpServer
	if err := json.Unmarshal(encoded, &decoded); err != nil {
		return fmt.Errorf("McpServer: %w", err)
	}
	return nil
}

// HTTP transport configuration for MCP.
type McpServerHttp struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// HTTP headers to set when making requests to the MCP server.
	Headers []HttpHeader `json:"headers,omitempty"`
	// Human-readable name identifying this MCP server.
	Name string `json:"name"`
	// URL to the MCP server.
	Url string `json:"url"`
}

func (v *McpServerHttp) UnmarshalJSON(b []byte) error {
	*v = McpServerHttp{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["name"]
		if !ok {
			return fmt.Errorf("name is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("name must not be null")
		}
	}
	{
		raw, ok := m["url"]
		if !ok {
			return fmt.Errorf("url is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("url must not be null")
		}
	}
	type Alias McpServerHttp
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = McpServerHttp(a)
	return nil
}

// Stdio transport configuration for MCP.
type McpServerStdio struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Command-line arguments to pass to the MCP server.
	Args []string `json:"args,omitempty"`
	// Absolute path to the MCP server executable.
	Command AbsolutePath `json:"command"`
	// Environment variables to set when launching the MCP server.
	Env []EnvVariable `json:"env,omitempty"`
	// Human-readable name identifying this MCP server.
	Name string `json:"name"`
}

func (v *McpServerStdio) UnmarshalJSON(b []byte) error {
	*v = McpServerStdio{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["command"]
		if !ok {
			return fmt.Errorf("command is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("command must not be null")
		}
	}
	{
		raw, ok := m["name"]
		if !ok {
			return fmt.Errorf("name is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("name must not be null")
		}
	}
	type Alias McpServerStdio
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = McpServerStdio(a)
	return nil
}

// Capabilities for stdio MCP server transports.
//
// Supplying '{}' means the agent supports stdio MCP server transports.
type McpStdioCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
}

// An Internet media type identifying the format of protocol content.
type MediaType string

// Unique identifier for a message within a session.
type MessageId string

// Items for a multi-select (array) property schema.
// Multi-select string items with plain string values.
type MultiSelectItemsString struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// Optional. Omitted and 'null' are equivalent and mean no metadata.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Allowed enum values. Must contain at least one value.
	Enum []string `json:"enum"`
	Type string   `json:"type"`
}

// Custom or future typed multi-select items.
type MultiSelectItemsOther = json.RawMessage

type MultiSelectItems struct {
	// Multi-select string items with plain string values.
	String *MultiSelectItemsString `json:"-"`
	// Custom or future typed multi-select items.
	Other *MultiSelectItemsOther `json:"-"`
	// Titled multi-select items with human-readable labels.
	Titled *TitledMultiSelectItems `json:"-"`
}

func (u *MultiSelectItems) UnmarshalJSON(b []byte) error {
	*u = MultiSelectItems{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err == nil {
		{
			var disc string
			if v, ok := m["type"]; ok {
				json.Unmarshal(v, &disc)
			}
			switch disc {
			case "string":
				{
					raw, ok := m["enum"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value []string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["type"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v MultiSelectItemsString
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.String = &v
				return nil
			}
		}
		{
			var v MultiSelectItemsOther
			var match bool = true
			{
				raw, ok := m["type"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value string
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			if match {
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Other = &v
				return nil
			}
		}
		{
			var v TitledMultiSelectItems
			var match bool = true
			{
				raw, ok := m["anyOf"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value []EnumOption
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			if match {
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Titled = &v
				return nil
			}
		}
	} else {
		if _, ok := err.(*json.UnmarshalTypeError); !ok {
			return err
		}
	}
	var arr []map[string]json.RawMessage
	if json.Unmarshal(b, &arr) == nil {
	}
	return errors.New("no matching variant for union")
}
func (u MultiSelectItems) MarshalJSON() ([]byte, error) {
	if u.String != nil {
		_b, _e := json.Marshal(*u.String)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["type"] = json.RawMessage("\"string\"")
		return json.Marshal(m)
	}
	if u.Other != nil {
		_b, _e := json.Marshal(*u.Other)
		if _e != nil {
			return []byte{}, _e
		}
		return _b, nil
	}
	if u.Titled != nil {
		_b, _e := json.Marshal(*u.Titled)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		return json.Marshal(m)
	}
	return []byte{}, nil
}

func (u *MultiSelectItems) Validate() error {
	var count int
	if u.String != nil {
		count++
	}
	if u.Other != nil {
		count++
	}
	if u.Titled != nil {
		count++
	}
	if count < 1 {
		return errors.New("MultiSelectItems must have at least one variant set")
	}
	encoded, err := json.Marshal(*u)
	if err != nil {
		return err
	}
	var decoded MultiSelectItems
	if err := json.Unmarshal(encoded, &decoded); err != nil {
		return fmt.Errorf("MultiSelectItems: %w", err)
	}
	return nil
}

// Schema for multi-select (array) properties in an elicitation form.
type MultiSelectPropertySchema struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// Optional. Omitted and 'null' are equivalent and mean no metadata.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Default selected values.
	//
	// Optional. Omitted and 'null' are equivalent and mean no default selections are provided.
	Default []string `json:"default,omitempty"`
	// Human-readable description.
	//
	// Optional. Omitted and 'null' are equivalent and mean no description is provided.
	Description *string `json:"description,omitempty"`
	// The items definition describing allowed values.
	Items MultiSelectItems `json:"items"`
	// Maximum number of items to select.
	//
	// Optional. Omitted and 'null' are equivalent and mean there is no maximum selection count.
	MaxItems *uint64 `json:"maxItems,omitempty"`
	// Minimum number of items to select.
	//
	// Optional. Omitted and 'null' are equivalent and mean there is no minimum selection count.
	MinItems *uint64 `json:"minItems,omitempty"`
	// Optional title for the property.
	//
	// Optional. Omitted and 'null' are equivalent and mean no title is provided.
	Title *string `json:"title,omitempty"`
}

func (v *MultiSelectPropertySchema) UnmarshalJSON(b []byte) error {
	*v = MultiSelectPropertySchema{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["items"]
		if !ok {
			return fmt.Errorf("items is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("items must not be null")
		}
	}
	type Alias MultiSelectPropertySchema
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = MultiSelectPropertySchema(a)
	return nil
}

// Request parameters for creating a new session.
//
// See protocol docs: [Creating a Session](https://agentclientprotocol.com/protocol/v2/session-setup#creating-a-session)
type NewSessionRequest struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Additional workspace roots for this session. Each path must be absolute.
	//
	// These expand the session's workspace scope without changing 'cwd', which
	// remains the base for relative paths. When omitted or empty, no
	// additional roots are activated for the new session.
	AdditionalDirectories []AbsolutePath `json:"additionalDirectories,omitempty"`
	// The working directory for this session. Must be an absolute path.
	Cwd AbsolutePath `json:"cwd"`
	// List of MCP (Model Context Protocol) servers the agent should connect to.
	McpServers []McpServer `json:"mcpServers,omitempty"`
}

func (v *NewSessionRequest) UnmarshalJSON(b []byte) error {
	*v = NewSessionRequest{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["cwd"]
		if !ok {
			return fmt.Errorf("cwd is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("cwd must not be null")
		}
	}
	type Alias NewSessionRequest
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = NewSessionRequest(a)
	return nil
}

func (v *NewSessionRequest) Validate() error {
	return nil
}

// Response from creating a new session.
//
// See protocol docs: [Creating a Session](https://agentclientprotocol.com/protocol/v2/session-setup#creating-a-session)
type NewSessionResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Initial session configuration options.
	ConfigOptions []SessionConfigOption `json:"configOptions,omitempty"`
	// Unique identifier for the created session.
	//
	// Used in all subsequent requests for this conversation.
	SessionId SessionId `json:"sessionId"`
}

func (v *NewSessionResponse) UnmarshalJSON(b []byte) error {
	*v = NewSessionResponse{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["sessionId"]
		if !ok {
			return fmt.Errorf("sessionId is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("sessionId must not be null")
		}
	}
	type Alias NewSessionResponse
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = NewSessionResponse(a)
	return nil
}

func (v *NewSessionResponse) Validate() error {
	return nil
}

// Schema for number (floating-point) properties in an elicitation form.
type NumberPropertySchema struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// Optional. Omitted and 'null' are equivalent and mean no metadata.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Default value.
	//
	// Optional. Omitted and 'null' are equivalent and mean no default value is provided.
	Default *float64 `json:"default,omitempty"`
	// Human-readable description.
	//
	// Optional. Omitted and 'null' are equivalent and mean no description is provided.
	Description *string `json:"description,omitempty"`
	// Maximum value (inclusive).
	//
	// Optional. Omitted and 'null' are equivalent and mean there is no inclusive upper bound.
	Maximum *float64 `json:"maximum,omitempty"`
	// Minimum value (inclusive).
	//
	// Optional. Omitted and 'null' are equivalent and mean there is no inclusive lower bound.
	Minimum *float64 `json:"minimum,omitempty"`
	// Optional title for the property.
	//
	// Optional. Omitted and 'null' are equivalent and mean no title is provided.
	Title *string `json:"title,omitempty"`
}

// An option presented to the user when requesting permission.
type PermissionOption struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Hint about the nature of this permission option.
	Kind PermissionOptionKind `json:"kind"`
	// Human-readable label to display to the user.
	Name string `json:"name"`
	// Unique identifier for this permission option.
	OptionId PermissionOptionId `json:"optionId"`
}

func (v *PermissionOption) UnmarshalJSON(b []byte) error {
	*v = PermissionOption{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["kind"]
		if !ok {
			return fmt.Errorf("kind is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("kind must not be null")
		}
	}
	{
		raw, ok := m["name"]
		if !ok {
			return fmt.Errorf("name is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("name must not be null")
		}
	}
	{
		raw, ok := m["optionId"]
		if !ok {
			return fmt.Errorf("optionId is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("optionId must not be null")
		}
	}
	type Alias PermissionOption
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = PermissionOption(a)
	return nil
}

// Unique identifier for a permission option.
type PermissionOptionId string

// The type of permission option being presented to the user.
//
// Helps clients choose appropriate icons and UI treatment.
type PermissionOptionKind string

const (
	PermissionOptionKindAllowOnce    PermissionOptionKind = "allow_once"
	PermissionOptionKindAllowAlways  PermissionOptionKind = "allow_always"
	PermissionOptionKindRejectOnce   PermissionOptionKind = "reject_once"
	PermissionOptionKindRejectAlways PermissionOptionKind = "reject_always"
)

// A single entry in the execution plan.
//
// Represents a task or goal that the assistant intends to accomplish
// as part of fulfilling the user's request.
// See protocol docs: [Plan Entries](https://agentclientprotocol.com/protocol/v2/agent-plan#plan-entries)
type PlanEntry struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Human-readable description of what this task aims to accomplish.
	Content string `json:"content"`
	// The relative importance of this task.
	// Used to indicate which tasks are most critical to the overall goal.
	Priority PlanEntryPriority `json:"priority"`
	// Current execution status of this task.
	Status PlanEntryStatus `json:"status"`
}

func (v *PlanEntry) UnmarshalJSON(b []byte) error {
	*v = PlanEntry{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["content"]
		if !ok {
			return fmt.Errorf("content is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("content must not be null")
		}
	}
	{
		raw, ok := m["priority"]
		if !ok {
			return fmt.Errorf("priority is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("priority must not be null")
		}
	}
	{
		raw, ok := m["status"]
		if !ok {
			return fmt.Errorf("status is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("status must not be null")
		}
	}
	type Alias PlanEntry
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = PlanEntry(a)
	return nil
}

// Priority levels for plan entries.
//
// Used to indicate the relative importance or urgency of different
// tasks in the execution plan.
// See protocol docs: [Plan Entries](https://agentclientprotocol.com/protocol/v2/agent-plan#plan-entries)
type PlanEntryPriority string

const (
	PlanEntryPriorityHigh   PlanEntryPriority = "high"
	PlanEntryPriorityMedium PlanEntryPriority = "medium"
	PlanEntryPriorityLow    PlanEntryPriority = "low"
)

// Status of a plan entry in the execution flow.
//
// Tracks the lifecycle of each task from planning through completion.
// See protocol docs: [Plan Entries](https://agentclientprotocol.com/protocol/v2/agent-plan#plan-entries)
type PlanEntryStatus string

const (
	PlanEntryStatusPending    PlanEntryStatus = "pending"
	PlanEntryStatusInProgress PlanEntryStatus = "in_progress"
	PlanEntryStatusCompleted  PlanEntryStatus = "completed"
	PlanEntryStatusCancelled  PlanEntryStatus = "cancelled"
)

// Unique identifier for a plan within a session.
type PlanId string

// A plan represented as structured entries.
type PlanItems struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The list of tasks to be accomplished.
	//
	// When updating an item-based plan, the agent must send a complete list of all entries
	// with their current status. The client replaces that plan with each update.
	Entries []PlanEntry `json:"entries"`
	// The plan ID to update.
	PlanId PlanId `json:"planId"`
}

func (v *PlanItems) UnmarshalJSON(b []byte) error {
	*v = PlanItems{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["entries"]
		if !ok {
			return fmt.Errorf("entries is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("entries must not be null")
		}
	}
	{
		raw, ok := m["planId"]
		if !ok {
			return fmt.Errorf("planId is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("planId must not be null")
		}
	}
	type Alias PlanItems
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = PlanItems(a)
	return nil
}

// A content update for a plan identified by ID.
type PlanUpdate struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The updated plan content.
	Plan PlanUpdateContent `json:"plan"`
}

func (v *PlanUpdate) UnmarshalJSON(b []byte) error {
	*v = PlanUpdate{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["plan"]
		if !ok {
			return fmt.Errorf("plan is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("plan must not be null")
		}
	}
	type Alias PlanUpdate
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = PlanUpdate(a)
	return nil
}

// Updated content for a plan.
// Structured plan entries.
type PlanUpdateContentItems struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The list of tasks to be accomplished.
	//
	// When updating an item-based plan, the agent must send a complete list of all entries
	// with their current status. The client replaces that plan with each update.
	Entries []PlanEntry `json:"entries"`
	// The plan ID to update.
	PlanId PlanId `json:"planId"`
	Type   string `json:"type"`
}

// Custom or future plan update content.
//
// Values beginning with '_' are reserved for implementation-specific
// extensions. Unknown values that do not begin with '_' are reserved for
// future ACP variants.
//
// Receivers that do not understand this content type should preserve the
// raw payload when storing, replaying, proxying, or forwarding plans, and
// otherwise ignore it or display it generically.
type PlanUpdateContentOther = json.RawMessage

type PlanUpdateContent struct {
	// Structured plan entries.
	Items *PlanUpdateContentItems `json:"-"`
	// Custom or future plan update content.
	//
	// Values beginning with '_' are reserved for implementation-specific
	// extensions. Unknown values that do not begin with '_' are reserved for
	// future ACP variants.
	//
	// Receivers that do not understand this content type should preserve the
	// raw payload when storing, replaying, proxying, or forwarding plans, and
	// otherwise ignore it or display it generically.
	Other *PlanUpdateContentOther `json:"-"`
}

func (u *PlanUpdateContent) UnmarshalJSON(b []byte) error {
	*u = PlanUpdateContent{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err == nil {
		{
			var disc string
			if v, ok := m["type"]; ok {
				json.Unmarshal(v, &disc)
			}
			switch disc {
			case "items":
				{
					raw, ok := m["entries"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value []PlanEntry
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["planId"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value PlanId
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["type"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v PlanUpdateContentItems
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Items = &v
				return nil
			}
		}
		{
			var v PlanUpdateContentOther
			var match bool = true
			{
				raw, ok := m["planId"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value PlanId
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			{
				raw, ok := m["type"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value string
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			if match {
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Other = &v
				return nil
			}
		}
	} else {
		if _, ok := err.(*json.UnmarshalTypeError); !ok {
			return err
		}
	}
	var arr []map[string]json.RawMessage
	if json.Unmarshal(b, &arr) == nil {
	}
	return errors.New("no matching variant for union")
}
func (u PlanUpdateContent) MarshalJSON() ([]byte, error) {
	if u.Items != nil {
		_b, _e := json.Marshal(*u.Items)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["type"] = json.RawMessage("\"items\"")
		return json.Marshal(m)
	}
	if u.Other != nil {
		_b, _e := json.Marshal(*u.Other)
		if _e != nil {
			return []byte{}, _e
		}
		return _b, nil
	}
	return []byte{}, nil
}

func (u *PlanUpdateContent) Validate() error {
	var count int
	if u.Items != nil {
		count++
	}
	if u.Other != nil {
		count++
	}
	if count < 1 {
		return errors.New("PlanUpdateContent must have at least one variant set")
	}
	encoded, err := json.Marshal(*u)
	if err != nil {
		return err
	}
	var decoded PlanUpdateContent
	if err := json.Unmarshal(encoded, &decoded); err != nil {
		return fmt.Errorf("PlanUpdateContent: %w", err)
	}
	return nil
}

// Capabilities for audio content in prompt requests.
//
// Supplying '{}' means the agent supports audio content in prompts.
type PromptAudioCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
}

// Prompt capabilities supported by the agent in 'session/prompt' requests.
//
// Baseline agent functionality requires support for ['ContentBlock::Text']
// and ['ContentBlock::ResourceLink'] in prompt requests.
//
// Other variants must be explicitly opted in to.
// Capabilities for different types of content in prompt requests.
//
// Indicates which content types beyond the baseline (text and resource links)
// the agent can process.
//
// See protocol docs: [Prompt Capabilities](https://agentclientprotocol.com/protocol/v2/initialization#prompt-capabilities)
type PromptCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Agent supports ['ContentBlock::Audio'].
	//
	// Optional. Omitted or 'null' both mean the agent does not advertise support.
	// Supplying '{}' means the agent supports audio content in prompts.
	Audio *PromptAudioCapabilities `json:"audio,omitempty"`
	// Agent supports embedded context in 'session/prompt' requests.
	//
	// When enabled, the Client is allowed to include ['ContentBlock::Resource']
	// in prompt requests for pieces of context that are referenced in the message.
	//
	// Optional. Omitted or 'null' both mean the agent does not advertise support.
	// Supplying '{}' means the agent supports embedded context in prompts.
	EmbeddedContext *PromptEmbeddedContextCapabilities `json:"embeddedContext,omitempty"`
	// Agent supports ['ContentBlock::Image'].
	//
	// Optional. Omitted or 'null' both mean the agent does not advertise support.
	// Supplying '{}' means the agent supports image content in prompts.
	Image *PromptImageCapabilities `json:"image,omitempty"`
}

// Capabilities for embedded context in prompt requests.
//
// Supplying '{}' means the agent supports embedded context in prompts.
type PromptEmbeddedContextCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
}

// Capabilities for image content in prompt requests.
//
// Supplying '{}' means the agent supports image content in prompts.
type PromptImageCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
}

// Request parameters for sending a user prompt to the agent.
//
// Contains the user's message and any additional context.
//
// See protocol docs: [User Message](https://agentclientprotocol.com/protocol/v2/prompt-lifecycle#1-user-message)
type PromptRequest struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The blocks of content that compose the user's message.
	//
	// As a baseline, the Agent MUST support ['ContentBlock::Text'] and ['ContentBlock::ResourceLink'],
	// while other variants are optionally enabled via ['PromptCapabilities'].
	//
	// The Client MUST adapt its interface according to ['PromptCapabilities'].
	//
	// The client MAY include referenced pieces of context as either
	// ['ContentBlock::Resource'] or ['ContentBlock::ResourceLink'].
	//
	// When available, ['ContentBlock::Resource'] is preferred
	// as it avoids extra round-trips and allows the message to include
	// pieces of context from sources the agent may not have access to.
	Prompt []ContentBlock `json:"prompt"`
	// The ID of the session to send this user message to
	SessionId SessionId `json:"sessionId"`
}

func (v *PromptRequest) UnmarshalJSON(b []byte) error {
	*v = PromptRequest{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["prompt"]
		if !ok {
			return fmt.Errorf("prompt is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("prompt must not be null")
		}
	}
	{
		raw, ok := m["sessionId"]
		if !ok {
			return fmt.Errorf("sessionId is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("sessionId must not be null")
		}
	}
	type Alias PromptRequest
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = PromptRequest(a)
	return nil
}

func (v *PromptRequest) Validate() error {
	if v.Prompt == nil {
		return fmt.Errorf("prompt is required")
	}
	for i := range v.Prompt {
		if err := v.Prompt[i].Validate(); err != nil {
			return fmt.Errorf("prompt[%d]: %w", i, err)
		}
	}
	return nil
}

// Response acknowledging that a user prompt was accepted.
//
// This response does not indicate that the agent has finished processing.
// Processing and completion are reported through 'state_update' session updates.
//
// See protocol docs: [Prompt Accepted](https://agentclientprotocol.com/protocol/v2/prompt-lifecycle#2-prompt-accepted)
type PromptResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
}

func (v *PromptResponse) Validate() error {
	return nil
}

// A JSON-RPC notification object.
type ProtocolLevelNotification struct {
	// The notification method name.
	Method string `json:"method"`
	// Method-specific notification parameters.
	Params any `json:"params,omitempty"`
}

func (v *ProtocolLevelNotification) UnmarshalJSON(b []byte) error {
	*v = ProtocolLevelNotification{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["method"]
		if !ok {
			return fmt.Errorf("method is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("method must not be null")
		}
	}
	type Alias ProtocolLevelNotification
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = ProtocolLevelNotification(a)
	return nil
}

func (v *ProtocolLevelNotification) Validate() error {
	return nil
}

// Protocol version identifier.
//
// This version is only bumped for breaking changes.
// Non-breaking changes should be introduced via capabilities.
type ProtocolVersion uint16

// Inclusive cursor describing where replayed session history should begin.
//
// Replay includes the position identified by the cursor.
// Replay the whole conversation from its first replayable entry.
type ReplayFromStartInline struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	Type string                     `json:"type"`
}

// Custom or future replay cursor.
//
// Values beginning with '_' are reserved for implementation-specific
// extensions. Unknown values that do not begin with '_' are reserved for
// future ACP variants.
//
// Receivers that do not understand this cursor should preserve the raw
// payload when storing, replaying, proxying, or forwarding requests, and
// otherwise reject the request rather than guessing where to replay from.
type ReplayFromOther = json.RawMessage

type ReplayFrom struct {
	// Replay the whole conversation from its first replayable entry.
	Start *ReplayFromStartInline `json:"-"`
	// Custom or future replay cursor.
	//
	// Values beginning with '_' are reserved for implementation-specific
	// extensions. Unknown values that do not begin with '_' are reserved for
	// future ACP variants.
	//
	// Receivers that do not understand this cursor should preserve the raw
	// payload when storing, replaying, proxying, or forwarding requests, and
	// otherwise reject the request rather than guessing where to replay from.
	Other *ReplayFromOther `json:"-"`
}

func (u *ReplayFrom) UnmarshalJSON(b []byte) error {
	*u = ReplayFrom{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err == nil {
		{
			var disc string
			if v, ok := m["type"]; ok {
				json.Unmarshal(v, &disc)
			}
			switch disc {
			case "start":
				{
					raw, ok := m["type"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v ReplayFromStartInline
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Start = &v
				return nil
			}
		}
		{
			var v ReplayFromOther
			var match bool = true
			{
				raw, ok := m["type"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value string
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			if match {
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Other = &v
				return nil
			}
		}
	} else {
		if _, ok := err.(*json.UnmarshalTypeError); !ok {
			return err
		}
	}
	var arr []map[string]json.RawMessage
	if json.Unmarshal(b, &arr) == nil {
	}
	return errors.New("no matching variant for union")
}
func (u ReplayFrom) MarshalJSON() ([]byte, error) {
	if u.Start != nil {
		_b, _e := json.Marshal(*u.Start)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["type"] = json.RawMessage("\"start\"")
		return json.Marshal(m)
	}
	if u.Other != nil {
		_b, _e := json.Marshal(*u.Other)
		if _e != nil {
			return []byte{}, _e
		}
		return _b, nil
	}
	return []byte{}, nil
}

func (u *ReplayFrom) Validate() error {
	var count int
	if u.Start != nil {
		count++
	}
	if u.Other != nil {
		count++
	}
	if count < 1 {
		return errors.New("ReplayFrom must have at least one variant set")
	}
	encoded, err := json.Marshal(*u)
	if err != nil {
		return err
	}
	var decoded ReplayFrom
	if err := json.Unmarshal(encoded, &decoded); err != nil {
		return fmt.Errorf("ReplayFrom: %w", err)
	}
	return nil
}

// Inclusive replay cursor requesting replay from the start of the conversation.
type ReplayFromStart struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
}

// JSON RPC Request Id
//
// An identifier established by the Client that MUST contain a String, Number, or NULL value if included. If it is not included it is assumed to be a notification. The value SHOULD normally not be Null \[1\] and Numbers SHOULD NOT contain fractional parts \[2\]
//
// The Server MUST reply with the same value in the Response object if included. This member is used to correlate the context between the two objects.
//
// \[1\] The use of Null as a value for the id member in a Request object is discouraged, because this specification uses a value of Null for Responses with an unknown id. Also, because JSON-RPC 1.0 uses an id value of Null for Notifications this could cause confusion in handling.
//
// \[2\] Fractional parts may be problematic, since many decimal fractions cannot be represented exactly as binary fractions.
type RequestIdNull struct{}

// A numeric JSON-RPC request id.
type RequestIdNumber int64

// A string JSON-RPC request id.
type RequestIdStr string

type RequestId struct {
	// The JSON-RPC 'null' request id.
	Null *RequestIdNull `json:"-"`
	// A numeric JSON-RPC request id.
	Number *RequestIdNumber `json:"-"`
	// A string JSON-RPC request id.
	Str *RequestIdStr `json:"-"`
}

func (u *RequestId) UnmarshalJSON(b []byte) error {
	*u = RequestId{}
	if string(b) == "null" {
		var v RequestIdNull
		u.Null = &v
		return nil
	}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err == nil {
	} else {
		if _, ok := err.(*json.UnmarshalTypeError); !ok {
			return err
		}
	}
	var arr []map[string]json.RawMessage
	if json.Unmarshal(b, &arr) == nil {
	}
	{
		var v RequestIdNull
		if json.Unmarshal(b, &v) == nil {
			u.Null = &v
			return nil
		}
	}
	{
		var v RequestIdNumber
		if json.Unmarshal(b, &v) == nil {
			u.Number = &v
			return nil
		}
	}
	{
		var v RequestIdStr
		if json.Unmarshal(b, &v) == nil {
			u.Str = &v
			return nil
		}
	}
	return errors.New("no matching variant for union")
}
func (u RequestId) MarshalJSON() ([]byte, error) {
	if u.Null != nil {
		return json.Marshal(nil)
	}
	if u.Number != nil {
		_b, _e := json.Marshal(*u.Number)
		if _e != nil {
			return []byte{}, _e
		}
		return _b, nil
	}
	if u.Str != nil {
		_b, _e := json.Marshal(*u.Str)
		if _e != nil {
			return []byte{}, _e
		}
		return _b, nil
	}
	return []byte{}, nil
}

func (u *RequestId) Validate() error {
	var count int
	if u.Null != nil {
		count++
	}
	if u.Number != nil {
		count++
	}
	if u.Str != nil {
		count++
	}
	if count < 1 {
		return errors.New("RequestId must have at least one variant set")
	}
	encoded, err := json.Marshal(*u)
	if err != nil {
		return err
	}
	var decoded RequestId
	if err := json.Unmarshal(encoded, &decoded); err != nil {
		return fmt.Errorf("RequestId: %w", err)
	}
	return nil
}

// The outcome of a permission request.
// Active session work was cancelled before the user responded.
//
// When a client sends a 'session/cancel' notification to cancel active
// session work, it MUST respond to all pending 'session/request_permission'
// requests with this 'Cancelled' outcome.
//
// See protocol docs: [Cancellation](https://agentclientprotocol.com/protocol/v2/prompt-lifecycle#cancellation)
type RequestPermissionOutcomeCancelled struct {
	Outcome string `json:"outcome"`
}

// The user selected one of the provided options.
type RequestPermissionOutcomeSelected struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The ID of the option the user selected.
	OptionId PermissionOptionId `json:"optionId"`
	Outcome  string             `json:"outcome"`
}

// Custom or future permission outcome.
//
// Values beginning with '_' are reserved for implementation-specific
// extensions. Unknown values that do not begin with '_' are reserved for
// future ACP variants.
//
// Agents that do not understand this outcome MUST NOT treat it as approval.
// They should preserve the raw payload when storing, replaying, proxying, or
// forwarding permission responses, and otherwise fail or decline the
// permission request according to policy.
type RequestPermissionOutcomeOther = json.RawMessage

type RequestPermissionOutcome struct {
	// Active session work was cancelled before the user responded.
	//
	// When a client sends a 'session/cancel' notification to cancel active
	// session work, it MUST respond to all pending 'session/request_permission'
	// requests with this 'Cancelled' outcome.
	//
	// See protocol docs: [Cancellation](https://agentclientprotocol.com/protocol/v2/prompt-lifecycle#cancellation)
	Cancelled *RequestPermissionOutcomeCancelled `json:"-"`
	// The user selected one of the provided options.
	Selected *RequestPermissionOutcomeSelected `json:"-"`
	// Custom or future permission outcome.
	//
	// Values beginning with '_' are reserved for implementation-specific
	// extensions. Unknown values that do not begin with '_' are reserved for
	// future ACP variants.
	//
	// Agents that do not understand this outcome MUST NOT treat it as approval.
	// They should preserve the raw payload when storing, replaying, proxying, or
	// forwarding permission responses, and otherwise fail or decline the
	// permission request according to policy.
	Other *RequestPermissionOutcomeOther `json:"-"`
}

func (u *RequestPermissionOutcome) UnmarshalJSON(b []byte) error {
	*u = RequestPermissionOutcome{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err == nil {
		{
			var disc string
			if v, ok := m["outcome"]; ok {
				json.Unmarshal(v, &disc)
			}
			switch disc {
			case "cancelled":
				{
					raw, ok := m["outcome"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v RequestPermissionOutcomeCancelled
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Cancelled = &v
				return nil
			case "selected":
				{
					raw, ok := m["optionId"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value PermissionOptionId
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["outcome"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v RequestPermissionOutcomeSelected
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Selected = &v
				return nil
			}
		}
		{
			var v RequestPermissionOutcomeOther
			var match bool = true
			{
				raw, ok := m["outcome"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value string
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			if match {
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Other = &v
				return nil
			}
		}
	} else {
		if _, ok := err.(*json.UnmarshalTypeError); !ok {
			return err
		}
	}
	var arr []map[string]json.RawMessage
	if json.Unmarshal(b, &arr) == nil {
	}
	return errors.New("no matching variant for union")
}
func (u RequestPermissionOutcome) MarshalJSON() ([]byte, error) {
	if u.Cancelled != nil {
		_b, _e := json.Marshal(*u.Cancelled)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["outcome"] = json.RawMessage("\"cancelled\"")
		return json.Marshal(m)
	}
	if u.Selected != nil {
		_b, _e := json.Marshal(*u.Selected)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["outcome"] = json.RawMessage("\"selected\"")
		return json.Marshal(m)
	}
	if u.Other != nil {
		_b, _e := json.Marshal(*u.Other)
		if _e != nil {
			return []byte{}, _e
		}
		return _b, nil
	}
	return []byte{}, nil
}

func (u *RequestPermissionOutcome) Validate() error {
	var count int
	if u.Cancelled != nil {
		count++
	}
	if u.Selected != nil {
		count++
	}
	if u.Other != nil {
		count++
	}
	if count < 1 {
		return errors.New("RequestPermissionOutcome must have at least one variant set")
	}
	encoded, err := json.Marshal(*u)
	if err != nil {
		return err
	}
	var decoded RequestPermissionOutcome
	if err := json.Unmarshal(encoded, &decoded); err != nil {
		return fmt.Errorf("RequestPermissionOutcome: %w", err)
	}
	return nil
}

// Request for user permission to proceed with an operation.
//
// Sent when the agent needs authorization before performing a sensitive operation.
//
// See protocol docs: [Requesting Permission](https://agentclientprotocol.com/protocol/v2/tool-calls#requesting-permission)
type RequestPermissionRequest struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Optional human-readable explanation of why permission is needed.
	//
	// This text is specific to the permission prompt and does not update any
	// subject's displayed content. Omitted or 'null' both mean no separate
	// permission description was provided.
	Description *string `json:"description,omitempty"`
	// Available permission options for the user to choose from.
	// Must contain at least one option.
	Options []PermissionOption `json:"options"`
	// The session ID for this request.
	SessionId SessionId `json:"sessionId"`
	// Optional structured context about the operation requiring permission.
	//
	// Omitted or 'null' both mean no structured subject was provided.
	Subject *RequestPermissionSubject `json:"subject,omitempty"`
	// Human-readable title for the permission prompt.
	//
	// This title is specific to the permission prompt and does not update any
	// subject's displayed title.
	Title string `json:"title"`
}

func (v *RequestPermissionRequest) UnmarshalJSON(b []byte) error {
	*v = RequestPermissionRequest{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["options"]
		if !ok {
			return fmt.Errorf("options is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("options must not be null")
		}
	}
	{
		raw, ok := m["sessionId"]
		if !ok {
			return fmt.Errorf("sessionId is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("sessionId must not be null")
		}
	}
	{
		raw, ok := m["title"]
		if !ok {
			return fmt.Errorf("title is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("title must not be null")
		}
	}
	type Alias RequestPermissionRequest
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = RequestPermissionRequest(a)
	return nil
}

func (v *RequestPermissionRequest) Validate() error {
	if v.Options == nil {
		return fmt.Errorf("options is required")
	}
	return nil
}

// Response to a permission request.
type RequestPermissionResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The user's decision on the permission request.
	Outcome RequestPermissionOutcome `json:"outcome"`
}

func (v *RequestPermissionResponse) UnmarshalJSON(b []byte) error {
	*v = RequestPermissionResponse{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["outcome"]
		if !ok {
			return fmt.Errorf("outcome is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("outcome must not be null")
		}
	}
	type Alias RequestPermissionResponse
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = RequestPermissionResponse(a)
	return nil
}

func (v *RequestPermissionResponse) Validate() error {
	if err := v.Outcome.Validate(); err != nil {
		return fmt.Errorf("outcome: %w", err)
	}
	return nil
}

// The operation requiring permission.
// Permission is requested before executing a tool call.
type RequestPermissionSubjectToolCall struct {
	// Details about the tool call requiring permission.
	ToolCall ToolCallUpdate `json:"toolCall"`
	Type     string         `json:"type"`
}

// Permission is requested before running a command.
type RequestPermissionSubjectCommand struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys. Omitted and 'null' are equivalent and mean no subject metadata was provided.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The command that would be run if permission is granted.
	Command string `json:"command"`
	// The absolute working directory for the command.
	Cwd AbsolutePath `json:"cwd"`
	// The associated terminal, when already known. Omitted and 'null' are equivalent.
	TerminalId *TerminalId `json:"terminalId,omitempty"`
	// The associated tool call, when known. Omitted and 'null' are equivalent.
	ToolCallId *ToolCallId `json:"toolCallId,omitempty"`
	Type       string      `json:"type"`
}

// Custom or future permission subject.
//
// Values beginning with '_' are reserved for implementation-specific
// extensions. Unknown values that do not begin with '_' are reserved for
// future ACP variants.
//
// Clients that do not understand this subject type should preserve the raw
// payload when storing, replaying, proxying, or forwarding permission
// requests, and otherwise display a generic permission prompt or decline it
// according to policy.
type RequestPermissionSubjectOther = json.RawMessage

type RequestPermissionSubject struct {
	// Permission is requested before executing a tool call.
	ToolCall *RequestPermissionSubjectToolCall `json:"-"`
	// Permission is requested before running a command.
	Command *RequestPermissionSubjectCommand `json:"-"`
	// Custom or future permission subject.
	//
	// Values beginning with '_' are reserved for implementation-specific
	// extensions. Unknown values that do not begin with '_' are reserved for
	// future ACP variants.
	//
	// Clients that do not understand this subject type should preserve the raw
	// payload when storing, replaying, proxying, or forwarding permission
	// requests, and otherwise display a generic permission prompt or decline it
	// according to policy.
	Other *RequestPermissionSubjectOther `json:"-"`
}

func (u *RequestPermissionSubject) UnmarshalJSON(b []byte) error {
	*u = RequestPermissionSubject{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err == nil {
		{
			var disc string
			if v, ok := m["type"]; ok {
				json.Unmarshal(v, &disc)
			}
			switch disc {
			case "tool_call":
				{
					raw, ok := m["toolCall"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value ToolCallUpdate
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["type"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v RequestPermissionSubjectToolCall
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.ToolCall = &v
				return nil
			case "command":
				{
					raw, ok := m["command"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["cwd"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value AbsolutePath
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["type"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v RequestPermissionSubjectCommand
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Command = &v
				return nil
			}
		}
		{
			var v RequestPermissionSubjectOther
			var match bool = true
			{
				raw, ok := m["type"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value string
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			if match {
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Other = &v
				return nil
			}
		}
	} else {
		if _, ok := err.(*json.UnmarshalTypeError); !ok {
			return err
		}
	}
	var arr []map[string]json.RawMessage
	if json.Unmarshal(b, &arr) == nil {
	}
	return errors.New("no matching variant for union")
}
func (u RequestPermissionSubject) MarshalJSON() ([]byte, error) {
	if u.ToolCall != nil {
		_b, _e := json.Marshal(*u.ToolCall)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["type"] = json.RawMessage("\"tool_call\"")
		return json.Marshal(m)
	}
	if u.Command != nil {
		_b, _e := json.Marshal(*u.Command)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["type"] = json.RawMessage("\"command\"")
		return json.Marshal(m)
	}
	if u.Other != nil {
		_b, _e := json.Marshal(*u.Other)
		if _e != nil {
			return []byte{}, _e
		}
		return _b, nil
	}
	return []byte{}, nil
}

func (u *RequestPermissionSubject) Validate() error {
	var count int
	if u.ToolCall != nil {
		count++
	}
	if u.Command != nil {
		count++
	}
	if u.Other != nil {
		count++
	}
	if count < 1 {
		return errors.New("RequestPermissionSubject must have at least one variant set")
	}
	encoded, err := json.Marshal(*u)
	if err != nil {
		return err
	}
	var decoded RequestPermissionSubject
	if err := json.Unmarshal(encoded, &decoded); err != nil {
		return fmt.Errorf("RequestPermissionSubject: %w", err)
	}
	return nil
}

// Foreground work is blocked on user action.
type RequiresActionStateUpdate struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
}

// A resource that the server is capable of reading, included in a prompt or tool call result.
type ResourceLink struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Optional annotations that help clients decide how to display or route this content.
	Annotations *Annotations `json:"annotations,omitempty"`
	// Optional human-readable details shown with this protocol object.
	Description *string `json:"description,omitempty"`
	// Optional set of sized icons that the client can display in a user interface.
	Icons []Icon `json:"icons,omitempty"`
	// MIME type describing the encoded media payload.
	MimeType *MediaType `json:"mimeType,omitempty"`
	// Human-readable name shown for this protocol object.
	Name string `json:"name"`
	// Optional size of the linked resource in bytes, if known.
	Size *int64 `json:"size,omitempty"`
	// Optional display title for end-user UI.
	Title *string `json:"title,omitempty"`
	// URI associated with this resource or media payload.
	Uri string `json:"uri"`
}

func (v *ResourceLink) UnmarshalJSON(b []byte) error {
	*v = ResourceLink{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["name"]
		if !ok {
			return fmt.Errorf("name is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("name must not be null")
		}
	}
	{
		raw, ok := m["uri"]
		if !ok {
			return fmt.Errorf("uri is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("uri must not be null")
		}
	}
	type Alias ResourceLink
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = ResourceLink(a)
	return nil
}

// Request parameters for resuming an existing session.
//
// Resumes an existing session and optionally replays prior conversation
// history according to 'replayFrom'.
type ResumeSessionRequest struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Additional workspace roots to activate for this session. Each path must be absolute.
	//
	// When omitted or empty, no additional roots are activated. When non-empty,
	// this is the complete resulting additional-root list for the resumed
	// session. It may differ from any previously used or reported list as long as
	// the request 'cwd' matches the session's 'cwd'.
	AdditionalDirectories []AbsolutePath `json:"additionalDirectories,omitempty"`
	// The working directory for this session. Must be an absolute path.
	Cwd AbsolutePath `json:"cwd"`
	// List of MCP servers to connect to for this session.
	McpServers []McpServer `json:"mcpServers,omitempty"`
	// Inclusive cursor describing where conversation replay should begin.
	//
	// Optional. Omitted or 'null' both mean the Agent should resume without
	// replaying previous conversation history. Replay cursors are inclusive:
	// replay includes the position identified by the cursor. Supplying
	// '{ "type": "start" }' means the Agent should replay the whole
	// conversation before responding.
	ReplayFrom *ReplayFrom `json:"replayFrom,omitempty"`
	// The ID of the session to resume.
	SessionId SessionId `json:"sessionId"`
}

func (v *ResumeSessionRequest) UnmarshalJSON(b []byte) error {
	*v = ResumeSessionRequest{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["cwd"]
		if !ok {
			return fmt.Errorf("cwd is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("cwd must not be null")
		}
	}
	{
		raw, ok := m["sessionId"]
		if !ok {
			return fmt.Errorf("sessionId is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("sessionId must not be null")
		}
	}
	type Alias ResumeSessionRequest
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = ResumeSessionRequest(a)
	return nil
}

func (v *ResumeSessionRequest) Validate() error {
	return nil
}

// Response from resuming an existing session.
type ResumeSessionResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Initial session configuration options.
	ConfigOptions []SessionConfigOption `json:"configOptions,omitempty"`
}

func (v *ResumeSessionResponse) Validate() error {
	return nil
}

// The sender or recipient of messages and data in a conversation.
type Role string

const (
	RoleAssistant Role = "assistant"
	RoleUser      Role = "user"
)

// Foreground work is in progress.
type RunningStateUpdate struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
}

// The user selected one of the provided options.
type SelectedPermissionOutcome struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The ID of the option the user selected.
	OptionId PermissionOptionId `json:"optionId"`
}

func (v *SelectedPermissionOutcome) UnmarshalJSON(b []byte) error {
	*v = SelectedPermissionOutcome{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["optionId"]
		if !ok {
			return fmt.Errorf("optionId is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("optionId must not be null")
		}
	}
	type Alias SelectedPermissionOutcome
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = SelectedPermissionOutcome(a)
	return nil
}

// Capabilities for additional session directories support.
//
// Supplying '{}' means the agent supports the 'additionalDirectories' field on
// supported session lifecycle requests. Agents that also support
// 'session/list' may return 'SessionInfo.additionalDirectories' to report the
// complete ordered additional-root list associated with a listed session.
type SessionAdditionalDirectoriesCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
}

// Session capabilities supported by the agent.
//
// Supplying '{}' means the agent supports the baseline session methods:
// 'session/new', 'session/list', 'session/resume', 'session/close',
// 'session/prompt', 'session/cancel', and 'session/update'.
//
// Agents that support sessions **MAY** support additional session methods,
// prompt content types, and MCP transports by specifying additional
// capabilities.
//
// See protocol docs: [Session Capabilities](https://agentclientprotocol.com/protocol/v2/initialization#session-capabilities)
type SessionCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Whether the agent supports 'additionalDirectories' on supported session lifecycle requests.
	//
	// Optional. Omitted or 'null' both mean the agent does not advertise support.
	// Supplying '{}' means the agent supports 'additionalDirectories' on
	// supported session lifecycle requests.
	//
	// Agents may return 'SessionInfo.additionalDirectories' to report the
	// complete ordered additional-root list associated with a listed session.
	AdditionalDirectories *SessionAdditionalDirectoriesCapabilities `json:"additionalDirectories,omitempty"`
	// Whether the agent supports 'session/delete'.
	//
	// Optional. Omitted or 'null' both mean the agent does not advertise support.
	// Supplying '{}' means the agent supports deleting sessions from 'session/list'.
	Delete *SessionDeleteCapabilities `json:"delete,omitempty"`
	// MCP capabilities supported by the agent for session lifecycle requests.
	//
	// Optional. Omitted or 'null' both mean the agent does not advertise MCP
	// server transport support for sessions.
	Mcp *McpCapabilities `json:"mcp,omitempty"`
	// Prompt capabilities supported by the agent in 'session/prompt' requests.
	//
	// Optional. Omitted or 'null' both mean the agent does not advertise any
	// prompt extensions beyond the baseline text and resource-link content
	// required by 'session/prompt'.
	Prompt *PromptCapabilities `json:"prompt,omitempty"`
}

// A boolean on/off toggle session configuration option payload.
type SessionConfigBoolean struct {
	// The current value of the boolean option.
	CurrentValue bool `json:"currentValue"`
}

func (v *SessionConfigBoolean) UnmarshalJSON(b []byte) error {
	*v = SessionConfigBoolean{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["currentValue"]
		if !ok {
			return fmt.Errorf("currentValue is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("currentValue must not be null")
		}
	}
	type Alias SessionConfigBoolean
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = SessionConfigBoolean(a)
	return nil
}

// Unique identifier for a session configuration option value group.
type SessionConfigGroupId string

// Unique identifier for a session configuration option.
type SessionConfigId string

// A session configuration option selector and its current state.
// Single-value selector (dropdown).
type SessionConfigOptionSelect struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Optional semantic category for this option (UX only).
	Category *SessionConfigOptionCategory `json:"category,omitempty"`
	// Unique identifier for the configuration option.
	ConfigId SessionConfigId `json:"configId"`
	// The currently selected value.
	CurrentValue SessionConfigValueId `json:"currentValue"`
	// Optional description for the Client to display to the user.
	Description *string `json:"description,omitempty"`
	// Human-readable label for the option.
	Name string `json:"name"`
	// The set of selectable options.
	Options SessionConfigSelectOptions `json:"options"`
	Type    string                     `json:"type"`
}

// Boolean on/off toggle.
type SessionConfigOptionBoolean struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Optional semantic category for this option (UX only).
	Category *SessionConfigOptionCategory `json:"category,omitempty"`
	// Unique identifier for the configuration option.
	ConfigId SessionConfigId `json:"configId"`
	// The current value of the boolean option.
	CurrentValue bool `json:"currentValue"`
	// Optional description for the Client to display to the user.
	Description *string `json:"description,omitempty"`
	// Human-readable label for the option.
	Name string `json:"name"`
	Type string `json:"type"`
}

// Custom or future session configuration option payload.
//
// Values beginning with '_' are reserved for implementation-specific
// extensions. Unknown values that do not begin with '_' are reserved for
// future ACP variants.
//
// Clients that do not understand this option type should preserve the raw
// payload when storing, replaying, proxying, or forwarding configuration
// data, and otherwise ignore the option or display it generically.
type SessionConfigOptionOther = json.RawMessage

type SessionConfigOption struct {
	// Single-value selector (dropdown).
	Select *SessionConfigOptionSelect `json:"-"`
	// Boolean on/off toggle.
	Boolean *SessionConfigOptionBoolean `json:"-"`
	// Custom or future session configuration option payload.
	//
	// Values beginning with '_' are reserved for implementation-specific
	// extensions. Unknown values that do not begin with '_' are reserved for
	// future ACP variants.
	//
	// Clients that do not understand this option type should preserve the raw
	// payload when storing, replaying, proxying, or forwarding configuration
	// data, and otherwise ignore the option or display it generically.
	Other *SessionConfigOptionOther `json:"-"`
}

func (u *SessionConfigOption) UnmarshalJSON(b []byte) error {
	*u = SessionConfigOption{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err == nil {
		{
			var disc string
			if v, ok := m["type"]; ok {
				json.Unmarshal(v, &disc)
			}
			switch disc {
			case "select":
				{
					raw, ok := m["configId"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value SessionConfigId
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["currentValue"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value SessionConfigValueId
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["name"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["options"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value SessionConfigSelectOptions
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["type"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v SessionConfigOptionSelect
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Select = &v
				return nil
			case "boolean":
				{
					raw, ok := m["configId"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value SessionConfigId
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["currentValue"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value bool
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["name"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["type"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v SessionConfigOptionBoolean
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Boolean = &v
				return nil
			}
		}
		{
			var v SessionConfigOptionOther
			var match bool = true
			{
				raw, ok := m["configId"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value SessionConfigId
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			{
				raw, ok := m["name"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value string
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			{
				raw, ok := m["type"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value string
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			if match {
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Other = &v
				return nil
			}
		}
	} else {
		if _, ok := err.(*json.UnmarshalTypeError); !ok {
			return err
		}
	}
	var arr []map[string]json.RawMessage
	if json.Unmarshal(b, &arr) == nil {
	}
	return errors.New("no matching variant for union")
}
func (u SessionConfigOption) MarshalJSON() ([]byte, error) {
	if u.Select != nil {
		_b, _e := json.Marshal(*u.Select)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["type"] = json.RawMessage("\"select\"")
		return json.Marshal(m)
	}
	if u.Boolean != nil {
		_b, _e := json.Marshal(*u.Boolean)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["type"] = json.RawMessage("\"boolean\"")
		return json.Marshal(m)
	}
	if u.Other != nil {
		_b, _e := json.Marshal(*u.Other)
		if _e != nil {
			return []byte{}, _e
		}
		return _b, nil
	}
	return []byte{}, nil
}

func (u *SessionConfigOption) Validate() error {
	var count int
	if u.Select != nil {
		count++
	}
	if u.Boolean != nil {
		count++
	}
	if u.Other != nil {
		count++
	}
	if count < 1 {
		return errors.New("SessionConfigOption must have at least one variant set")
	}
	encoded, err := json.Marshal(*u)
	if err != nil {
		return err
	}
	var decoded SessionConfigOption
	if err := json.Unmarshal(encoded, &decoded); err != nil {
		return fmt.Errorf("SessionConfigOption: %w", err)
	}
	return nil
}

// Semantic category for a session configuration option.
//
// This is intended to help Clients distinguish broadly common selectors (e.g. model selector vs
// session mode selector vs thought/reasoning level) for UX purposes (keyboard shortcuts, icons,
// placement). It MUST NOT be required for correctness. Clients MUST handle missing or unknown
// categories gracefully.
//
// Category names beginning with '_' are free for custom use, like other ACP extension methods.
// Category names that do not begin with '_' are reserved for the ACP spec.
type SessionConfigOptionCategory string

const (
	SessionConfigOptionCategoryMode         SessionConfigOptionCategory = "mode"
	SessionConfigOptionCategoryModel        SessionConfigOptionCategory = "model"
	SessionConfigOptionCategoryModelConfig  SessionConfigOptionCategory = "model_config"
	SessionConfigOptionCategoryThoughtLevel SessionConfigOptionCategory = "thought_level"
)

// A single-value selector (dropdown) session configuration option payload.
type SessionConfigSelect struct {
	// The currently selected value.
	CurrentValue SessionConfigValueId `json:"currentValue"`
	// The set of selectable options.
	Options SessionConfigSelectOptions `json:"options"`
}

func (v *SessionConfigSelect) UnmarshalJSON(b []byte) error {
	*v = SessionConfigSelect{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["currentValue"]
		if !ok {
			return fmt.Errorf("currentValue is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("currentValue must not be null")
		}
	}
	{
		raw, ok := m["options"]
		if !ok {
			return fmt.Errorf("options is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("options must not be null")
		}
	}
	type Alias SessionConfigSelect
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = SessionConfigSelect(a)
	return nil
}

// A group of possible values for a session configuration option.
type SessionConfigSelectGroup struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Unique identifier for this group.
	GroupId SessionConfigGroupId `json:"groupId"`
	// Human-readable label for this group.
	Name string `json:"name"`
	// The set of option values in this group.
	Options []SessionConfigSelectOption `json:"options"`
}

func (v *SessionConfigSelectGroup) UnmarshalJSON(b []byte) error {
	*v = SessionConfigSelectGroup{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["groupId"]
		if !ok {
			return fmt.Errorf("groupId is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("groupId must not be null")
		}
	}
	{
		raw, ok := m["name"]
		if !ok {
			return fmt.Errorf("name is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("name must not be null")
		}
	}
	{
		raw, ok := m["options"]
		if !ok {
			return fmt.Errorf("options is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("options must not be null")
		}
	}
	type Alias SessionConfigSelectGroup
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = SessionConfigSelectGroup(a)
	return nil
}

// A possible value for a session configuration option.
type SessionConfigSelectOption struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Optional description for this option value.
	Description *string `json:"description,omitempty"`
	// Human-readable label for this option value.
	Name string `json:"name"`
	// Unique identifier for this option value.
	Value SessionConfigValueId `json:"value"`
}

func (v *SessionConfigSelectOption) UnmarshalJSON(b []byte) error {
	*v = SessionConfigSelectOption{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["name"]
		if !ok {
			return fmt.Errorf("name is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("name must not be null")
		}
	}
	{
		raw, ok := m["value"]
		if !ok {
			return fmt.Errorf("value is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("value must not be null")
		}
	}
	type Alias SessionConfigSelectOption
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = SessionConfigSelectOption(a)
	return nil
}

// Possible values for a session configuration option.
// A flat list of options with no grouping.
type SessionConfigSelectOptionsUngrouped []SessionConfigSelectOption

// A list of options grouped under headers.
type SessionConfigSelectOptionsGrouped []SessionConfigSelectGroup

type SessionConfigSelectOptions struct {
	// A flat list of options with no grouping.
	Ungrouped *SessionConfigSelectOptionsUngrouped `json:"-"`
	// A list of options grouped under headers.
	Grouped *SessionConfigSelectOptionsGrouped `json:"-"`
}

func (u *SessionConfigSelectOptions) UnmarshalJSON(b []byte) error {
	*u = SessionConfigSelectOptions{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err == nil {
	} else {
		if _, ok := err.(*json.UnmarshalTypeError); !ok {
			return err
		}
	}
	var arr []map[string]json.RawMessage
	if json.Unmarshal(b, &arr) == nil {
		{
			var v SessionConfigSelectOptionsUngrouped
			var match bool = true
			for _, elem := range arr {
				if _, ok := elem["name"]; !ok {
					match = false
				}
				if _, ok := elem["value"]; !ok {
					match = false
				}
			}
			if match {
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Ungrouped = &v
				return nil
			}
		}
		{
			var v SessionConfigSelectOptionsGrouped
			var match bool = true
			for _, elem := range arr {
				if _, ok := elem["groupId"]; !ok {
					match = false
				}
				if _, ok := elem["name"]; !ok {
					match = false
				}
				if _, ok := elem["options"]; !ok {
					match = false
				}
			}
			if match {
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Grouped = &v
				return nil
			}
		}
	}
	{
		var v SessionConfigSelectOptionsUngrouped
		if json.Unmarshal(b, &v) == nil {
			u.Ungrouped = &v
			return nil
		}
	}
	{
		var v SessionConfigSelectOptionsGrouped
		if json.Unmarshal(b, &v) == nil {
			u.Grouped = &v
			return nil
		}
	}
	return errors.New("no matching variant for union")
}
func (u SessionConfigSelectOptions) MarshalJSON() ([]byte, error) {
	if u.Ungrouped != nil {
		_b, _e := json.Marshal(*u.Ungrouped)
		if _e != nil {
			return []byte{}, _e
		}
		return _b, nil
	}
	if u.Grouped != nil {
		_b, _e := json.Marshal(*u.Grouped)
		if _e != nil {
			return []byte{}, _e
		}
		return _b, nil
	}
	return []byte{}, nil
}

func (u *SessionConfigSelectOptions) Validate() error {
	var count int
	if u.Ungrouped != nil {
		count++
	}
	if u.Grouped != nil {
		count++
	}
	if count < 1 {
		return errors.New("SessionConfigSelectOptions must have at least one variant set")
	}
	encoded, err := json.Marshal(*u)
	if err != nil {
		return err
	}
	var decoded SessionConfigSelectOptions
	if err := json.Unmarshal(encoded, &decoded); err != nil {
		return fmt.Errorf("SessionConfigSelectOptions: %w", err)
	}
	return nil
}

// Unique identifier for a session configuration option value.
type SessionConfigValueId string

// Capabilities for the 'session/delete' method.
//
// Supplying '{}' means the agent supports deleting sessions from 'session/list'.
type SessionDeleteCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
}

// A unique identifier for a conversation session between a client and agent.
//
// Sessions maintain their own context, conversation history, and state,
// allowing multiple independent interactions with the same agent.
//
// See protocol docs: [Session ID](https://agentclientprotocol.com/protocol/v2/session-setup#session-id)
type SessionId string

// Information about a session returned by session/list
type SessionInfo struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Additional workspace roots reported for this session. Each path must be absolute.
	//
	// When present, this is the complete ordered additional-root list reported
	// by the Agent. Omitted and empty values are equivalent: the response
	// reports no additional roots.
	AdditionalDirectories []AbsolutePath `json:"additionalDirectories,omitempty"`
	// The working directory for this session. Must be an absolute path.
	Cwd AbsolutePath `json:"cwd"`
	// Unique identifier for the session
	SessionId SessionId `json:"sessionId"`
	// Human-readable title for the session
	Title *string `json:"title,omitempty"`
	// RFC 3339 timestamp of last activity.
	UpdatedAt *string `json:"updatedAt,omitempty"`
}

func (v *SessionInfo) UnmarshalJSON(b []byte) error {
	*v = SessionInfo{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["cwd"]
		if !ok {
			return fmt.Errorf("cwd is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("cwd must not be null")
		}
	}
	{
		raw, ok := m["sessionId"]
		if !ok {
			return fmt.Errorf("sessionId is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("sessionId must not be null")
		}
	}
	type Alias SessionInfo
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = SessionInfo(a)
	return nil
}

// Update to session metadata. All fields are optional to support partial updates.
//
// Agents send this notification to update session information like title or custom metadata.
// This allows clients to display dynamic session names and track session state changes.
//
// Omitted fields leave the existing session info unchanged. 'null' clears the
// corresponding value.
type SessionInfoUpdate struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Omitted means no metadata update; 'null' is an
	// explicit clear signal. Implementations MUST NOT make assumptions about values at these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Human-readable title for the session. Set to null to clear.
	Title *string `json:"title,omitempty"`
	// RFC 3339 timestamp of last activity. Set to null to clear.
	UpdatedAt    *string `json:"updatedAt,omitempty"`
	hasTitle     bool    `json:"-"`
	hasUpdatedAt bool    `json:"-"`
}

func (v SessionInfoUpdate) MarshalJSON() ([]byte, error) {
	type Alias SessionInfoUpdate
	var a Alias
	a = Alias(v)
	var _titleJSON json.RawMessage
	if a.Title != nil {
		encoded, err := json.Marshal(*a.Title)
		if err != nil {
			return nil, err
		}
		_titleJSON = encoded
	} else if a.hasTitle {
		_titleJSON = json.RawMessage("null")
	}
	var _updatedAtJSON json.RawMessage
	if a.UpdatedAt != nil {
		encoded, err := json.Marshal(*a.UpdatedAt)
		if err != nil {
			return nil, err
		}
		_updatedAtJSON = encoded
	} else if a.hasUpdatedAt {
		_updatedAtJSON = json.RawMessage("null")
	}
	return json.Marshal(struct {
		Alias
		Title     json.RawMessage `json:"title,omitempty"`
		UpdatedAt json.RawMessage `json:"updatedAt,omitempty"`
	}{
		Alias:     a,
		Title:     _titleJSON,
		UpdatedAt: _updatedAtJSON,
	})
}

func (v *SessionInfoUpdate) UnmarshalJSON(b []byte) error {
	*v = SessionInfoUpdate{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	type Alias SessionInfoUpdate
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	{
		_, present := m["title"]
		a.hasTitle = present
	}
	{
		_, present := m["updatedAt"]
		a.hasUpdatedAt = present
	}
	*v = SessionInfoUpdate(a)
	return nil
}

func (v SessionInfoUpdate) TitleState() NullableFieldState {
	if v.Title != nil {
		return NullableFieldValue
	}
	if v.hasTitle {
		return NullableFieldNull
	}
	return NullableFieldAbsent
}

func (v *SessionInfoUpdate) SetTitle(value string) {
	v.Title = &value
	v.hasTitle = true
}

func (v *SessionInfoUpdate) ClearTitle() {
	v.Title = nil
	v.hasTitle = true
}

func (v *SessionInfoUpdate) UnsetTitle() {
	v.Title = nil
	v.hasTitle = false
}

func (v SessionInfoUpdate) UpdatedAtState() NullableFieldState {
	if v.UpdatedAt != nil {
		return NullableFieldValue
	}
	if v.hasUpdatedAt {
		return NullableFieldNull
	}
	return NullableFieldAbsent
}

func (v *SessionInfoUpdate) SetUpdatedAt(value string) {
	v.UpdatedAt = &value
	v.hasUpdatedAt = true
}

func (v *SessionInfoUpdate) ClearUpdatedAt() {
	v.UpdatedAt = nil
	v.hasUpdatedAt = true
}

func (v *SessionInfoUpdate) UnsetUpdatedAt() {
	v.UpdatedAt = nil
	v.hasUpdatedAt = false
}

// An opaque cursor used to paginate 'session/list' results.
type SessionListCursor string

// Different types of updates that can be sent while a session exists.
//
// These updates report messages, progress, and other session activity.
//
// See protocol docs: [Agent Reports Output](https://agentclientprotocol.com/protocol/v2/prompt-lifecycle#3-agent-reports-output)
// A chunk of the user's message being streamed.
type SessionUpdateUserMessageChunk struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys. This field is chunk-scoped.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// A single item of content
	Content ContentBlock `json:"content"`
	// A unique identifier for the message this chunk belongs to.
	//
	// All chunks belonging to the same message share the same 'messageId'.
	// A change in 'messageId' indicates a new message has started.
	MessageId     MessageId `json:"messageId"`
	SessionUpdate string    `json:"sessionUpdate"`
}

// A user message has been created or updated.
//
// Agents can send this when they accept or replay a user message. When a
// client receives another 'user_message' update with the same 'messageId',
// fields in the new update patch the previous fields for that message.
type SessionUpdateUserMessage struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys. Omitted means no metadata update; 'null' is an explicit clear signal.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Complete replacement content for this message.
	Content []ContentBlock `json:"content,omitempty"`
	// A unique identifier for the message.
	MessageId     MessageId `json:"messageId"`
	SessionUpdate string    `json:"sessionUpdate"`
}

// A chunk of the agent's response being streamed.
type SessionUpdateAgentMessageChunk struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys. This field is chunk-scoped.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// A single item of content
	Content ContentBlock `json:"content"`
	// A unique identifier for the message this chunk belongs to.
	//
	// All chunks belonging to the same message share the same 'messageId'.
	// A change in 'messageId' indicates a new message has started.
	MessageId     MessageId `json:"messageId"`
	SessionUpdate string    `json:"sessionUpdate"`
}

// An agent message has been created or updated.
//
// Agents can send this in addition to streamed chunks. When a client
// receives another 'agent_message' update with the same 'messageId',
// fields in the new update patch the previous fields for that message.
type SessionUpdateAgentMessage struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys. Omitted means no metadata update; 'null' is an explicit clear signal.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Complete replacement content for this message.
	Content []ContentBlock `json:"content,omitempty"`
	// A unique identifier for the message.
	MessageId     MessageId `json:"messageId"`
	SessionUpdate string    `json:"sessionUpdate"`
}

// A chunk of the agent's internal reasoning being streamed.
type SessionUpdateAgentThoughtChunk struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys. This field is chunk-scoped.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// A single item of content
	Content ContentBlock `json:"content"`
	// A unique identifier for the message this chunk belongs to.
	//
	// All chunks belonging to the same message share the same 'messageId'.
	// A change in 'messageId' indicates a new message has started.
	MessageId     MessageId `json:"messageId"`
	SessionUpdate string    `json:"sessionUpdate"`
}

// An agent thought or reasoning message has been created or updated.
//
// Agents can send this in addition to streamed chunks. When a client
// receives another 'agent_thought' update with the same 'messageId',
// fields in the new update patch the previous fields for that message.
type SessionUpdateAgentThought struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys. Omitted means no metadata update; 'null' is an explicit clear signal.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Complete replacement content for this thought message.
	Content []ContentBlock `json:"content,omitempty"`
	// A unique identifier for the thought message.
	MessageId     MessageId `json:"messageId"`
	SessionUpdate string    `json:"sessionUpdate"`
}

// The state of the agent's foreground work has changed.
type SessionStateUpdate struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta          map[string]json.RawMessage `json:"_meta,omitempty"`
	SessionUpdate string                     `json:"sessionUpdate"`
	// Custom or future session state.
	//
	// Values beginning with '_' are reserved for implementation-specific
	// extensions. Unknown values that do not begin with '_' are reserved for
	// future ACP variants.
	State *string `json:"state,omitempty"`
	// Indicates why foreground work stopped.
	//
	// Optional. Omitted or 'null' both mean the agent is not reporting a stop reason.
	// Agents SHOULD include this when the idle transition ends foreground work.
	StopReason *StopReason `json:"stopReason,omitempty"`
}

// A chunk of tool-call content being streamed.
type SessionUpdateToolCallContentChunk struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys. This field is chunk-scoped.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// A single item of content produced by the tool call.
	Content       ToolCallContent `json:"content"`
	SessionUpdate string          `json:"sessionUpdate"`
	// The ID of the tool call this content belongs to.
	ToolCallId ToolCallId `json:"toolCallId"`
}

// A tool call has been created or updated.
type SessionToolCallUpdate struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Omitted means no metadata update; 'null' is an
	// explicit clear signal. Implementations MUST NOT make assumptions about values at these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Content produced by the tool call.
	Content []ToolCallContent `json:"content,omitempty"`
	// The category of tool being invoked.
	// Helps clients choose appropriate icons and UI treatment.
	Kind *ToolKind `json:"kind,omitempty"`
	// File locations affected by this tool call.
	// Enables "follow-along" features in clients.
	Locations []ToolCallLocation `json:"locations,omitempty"`
	// Raw input parameters sent to the tool.
	RawInput any `json:"rawInput,omitempty"`
	// Raw output returned by the tool.
	RawOutput     any    `json:"rawOutput,omitempty"`
	SessionUpdate string `json:"sessionUpdate"`
	// Current execution status of the tool call.
	Status *ToolCallStatus `json:"status,omitempty"`
	// Human-readable title describing what the tool is doing.
	Title *string `json:"title,omitempty"`
	// Unique identifier for this tool call within the session.
	ToolCallId ToolCallId `json:"toolCallId"`
}

// An agent-owned terminal has been created or updated.
type SessionTerminalUpdate struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Omitted means no metadata update; 'null' is an
	// explicit clear signal. Implementations MUST NOT make assumptions about values at these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The command being run.
	Command *string `json:"command,omitempty"`
	// The absolute working directory of the command.
	Cwd *AbsolutePath `json:"cwd,omitempty"`
	// Exit information. A concrete object marks the terminal as exited.
	ExitStatus *TerminalExitStatus `json:"exitStatus,omitempty"`
	// An authoritative replacement snapshot of terminal output bytes.
	Output        *TerminalOutput `json:"output,omitempty"`
	SessionUpdate string          `json:"sessionUpdate"`
	// Unique identifier for this terminal within the session.
	TerminalId TerminalId `json:"terminalId"`
}

// A chunk of bytes appended to an agent-owned terminal's output.
type SessionUpdateTerminalOutputChunk struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys. This field is chunk-scoped. Omitted and 'null' are
	// equivalent and mean no chunk metadata was provided.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Independently base64-encoded terminal output bytes.
	Data          string `json:"data"`
	SessionUpdate string `json:"sessionUpdate"`
	// The terminal receiving these bytes.
	TerminalId TerminalId `json:"terminalId"`
}

// A content update for a plan identified by ID.
// See protocol docs: [Agent Plan](https://agentclientprotocol.com/protocol/v2/agent-plan)
type SessionPlanUpdate struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The updated plan content.
	Plan          PlanUpdateContent `json:"plan"`
	SessionUpdate string            `json:"sessionUpdate"`
}

// Available commands are ready or have changed
type SessionAvailableCommandsUpdate struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Commands the agent can execute.
	AvailableCommands []AvailableCommand `json:"availableCommands"`
	SessionUpdate     string             `json:"sessionUpdate"`
}

// Session configuration options have been updated.
type SessionConfigOptionUpdate struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The full set of configuration options and their current values.
	ConfigOptions []SessionConfigOption `json:"configOptions"`
	SessionUpdate string                `json:"sessionUpdate"`
}

// Session metadata has been updated (title, timestamps, custom metadata)
type SessionSessionInfoUpdate struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Omitted means no metadata update; 'null' is an
	// explicit clear signal. Implementations MUST NOT make assumptions about values at these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta          map[string]json.RawMessage `json:"_meta,omitempty"`
	SessionUpdate string                     `json:"sessionUpdate"`
	// Human-readable title for the session. Set to null to clear.
	Title *string `json:"title,omitempty"`
	// RFC 3339 timestamp of last activity. Set to null to clear.
	UpdatedAt    *string `json:"updatedAt,omitempty"`
	hasTitle     bool    `json:"-"`
	hasUpdatedAt bool    `json:"-"`
}

func (v SessionSessionInfoUpdate) MarshalJSON() ([]byte, error) {
	type Alias SessionSessionInfoUpdate
	a := Alias(v)
	var _titleJSON json.RawMessage
	if a.Title != nil {
		encoded, err := json.Marshal(*a.Title)
		if err != nil {
			return nil, err
		}
		_titleJSON = encoded
	} else if a.hasTitle {
		_titleJSON = json.RawMessage("null")
	}
	var _updatedAtJSON json.RawMessage
	if a.UpdatedAt != nil {
		encoded, err := json.Marshal(*a.UpdatedAt)
		if err != nil {
			return nil, err
		}
		_updatedAtJSON = encoded
	} else if a.hasUpdatedAt {
		_updatedAtJSON = json.RawMessage("null")
	}
	return json.Marshal(struct {
		Alias
		Title     json.RawMessage `json:"title,omitempty"`
		UpdatedAt json.RawMessage `json:"updatedAt,omitempty"`
	}{
		Alias:     a,
		Title:     _titleJSON,
		UpdatedAt: _updatedAtJSON,
	})
}

func (v *SessionSessionInfoUpdate) UnmarshalJSON(b []byte) error {
	*v = SessionSessionInfoUpdate{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["sessionUpdate"]
		if !ok {
			return fmt.Errorf("sessionUpdate is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("sessionUpdate must not be null")
		}
	}
	type Alias SessionSessionInfoUpdate
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	{
		_, present := m["title"]
		a.hasTitle = present
	}
	{
		_, present := m["updatedAt"]
		a.hasUpdatedAt = present
	}
	*v = SessionSessionInfoUpdate(a)
	return nil
}

func (v SessionSessionInfoUpdate) TitleState() NullableFieldState {
	if v.Title != nil {
		return NullableFieldValue
	}
	if v.hasTitle {
		return NullableFieldNull
	}
	return NullableFieldAbsent
}

func (v *SessionSessionInfoUpdate) SetTitle(value string) {
	v.Title = &value
	v.hasTitle = true
}

func (v *SessionSessionInfoUpdate) ClearTitle() {
	v.Title = nil
	v.hasTitle = true
}

func (v *SessionSessionInfoUpdate) UnsetTitle() {
	v.Title = nil
	v.hasTitle = false
}

func (v SessionSessionInfoUpdate) UpdatedAtState() NullableFieldState {
	if v.UpdatedAt != nil {
		return NullableFieldValue
	}
	if v.hasUpdatedAt {
		return NullableFieldNull
	}
	return NullableFieldAbsent
}

func (v *SessionSessionInfoUpdate) SetUpdatedAt(value string) {
	v.UpdatedAt = &value
	v.hasUpdatedAt = true
}

func (v *SessionSessionInfoUpdate) ClearUpdatedAt() {
	v.UpdatedAt = nil
	v.hasUpdatedAt = true
}

func (v *SessionSessionInfoUpdate) UnsetUpdatedAt() {
	v.UpdatedAt = nil
	v.hasUpdatedAt = false
}

// Context window and cost update for the session.
type SessionUsageUpdate struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Cumulative session cost (optional).
	Cost          *Cost  `json:"cost,omitempty"`
	SessionUpdate string `json:"sessionUpdate"`
	// Total context window size in tokens.
	Size uint64 `json:"size"`
	// Tokens currently in context.
	Used uint64 `json:"used"`
}

// Custom or future session update.
//
// Values beginning with '_' are reserved for implementation-specific
// extensions. Unknown values that do not begin with '_' are reserved for
// future ACP variants.
//
// Receivers that do not understand this update type should preserve the
// raw payload when storing, replaying, proxying, or forwarding session
// history, and otherwise ignore it or display it generically.
type SessionUpdateOther = json.RawMessage

type SessionUpdate struct {
	// A chunk of the user's message being streamed.
	UserMessageChunk *SessionUpdateUserMessageChunk `json:"-"`
	// A user message has been created or updated.
	//
	// Agents can send this when they accept or replay a user message. When a
	// client receives another 'user_message' update with the same 'messageId',
	// fields in the new update patch the previous fields for that message.
	UserMessage *SessionUpdateUserMessage `json:"-"`
	// A chunk of the agent's response being streamed.
	AgentMessageChunk *SessionUpdateAgentMessageChunk `json:"-"`
	// An agent message has been created or updated.
	//
	// Agents can send this in addition to streamed chunks. When a client
	// receives another 'agent_message' update with the same 'messageId',
	// fields in the new update patch the previous fields for that message.
	AgentMessage *SessionUpdateAgentMessage `json:"-"`
	// A chunk of the agent's internal reasoning being streamed.
	AgentThoughtChunk *SessionUpdateAgentThoughtChunk `json:"-"`
	// An agent thought or reasoning message has been created or updated.
	//
	// Agents can send this in addition to streamed chunks. When a client
	// receives another 'agent_thought' update with the same 'messageId',
	// fields in the new update patch the previous fields for that message.
	AgentThought *SessionUpdateAgentThought `json:"-"`
	// The state of the agent's foreground work has changed.
	StateUpdate *SessionStateUpdate `json:"-"`
	// A chunk of tool-call content being streamed.
	ToolCallContentChunk *SessionUpdateToolCallContentChunk `json:"-"`
	// A tool call has been created or updated.
	ToolCallUpdate *SessionToolCallUpdate `json:"-"`
	// An agent-owned terminal has been created or updated.
	TerminalUpdate *SessionTerminalUpdate `json:"-"`
	// A chunk of bytes appended to an agent-owned terminal's output.
	TerminalOutputChunk *SessionUpdateTerminalOutputChunk `json:"-"`
	// A content update for a plan identified by ID.
	// See protocol docs: [Agent Plan](https://agentclientprotocol.com/protocol/v2/agent-plan)
	PlanUpdate *SessionPlanUpdate `json:"-"`
	// Available commands are ready or have changed
	AvailableCommandsUpdate *SessionAvailableCommandsUpdate `json:"-"`
	// Session configuration options have been updated.
	ConfigOptionUpdate *SessionConfigOptionUpdate `json:"-"`
	// Session metadata has been updated (title, timestamps, custom metadata)
	SessionInfoUpdate *SessionSessionInfoUpdate `json:"-"`
	// Context window and cost update for the session.
	UsageUpdate *SessionUsageUpdate `json:"-"`
	// Custom or future session update.
	//
	// Values beginning with '_' are reserved for implementation-specific
	// extensions. Unknown values that do not begin with '_' are reserved for
	// future ACP variants.
	//
	// Receivers that do not understand this update type should preserve the
	// raw payload when storing, replaying, proxying, or forwarding session
	// history, and otherwise ignore it or display it generically.
	Other *SessionUpdateOther `json:"-"`
}

func (u *SessionUpdate) UnmarshalJSON(b []byte) error {
	*u = SessionUpdate{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err == nil {
		{
			var disc string
			if v, ok := m["sessionUpdate"]; ok {
				json.Unmarshal(v, &disc)
			}
			switch disc {
			case "user_message_chunk":
				{
					raw, ok := m["content"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value ContentBlock
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["messageId"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value MessageId
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["sessionUpdate"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v SessionUpdateUserMessageChunk
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.UserMessageChunk = &v
				return nil
			case "user_message":
				{
					raw, ok := m["messageId"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value MessageId
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["sessionUpdate"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v SessionUpdateUserMessage
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.UserMessage = &v
				return nil
			case "agent_message_chunk":
				{
					raw, ok := m["content"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value ContentBlock
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["messageId"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value MessageId
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["sessionUpdate"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v SessionUpdateAgentMessageChunk
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.AgentMessageChunk = &v
				return nil
			case "agent_message":
				{
					raw, ok := m["messageId"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value MessageId
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["sessionUpdate"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v SessionUpdateAgentMessage
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.AgentMessage = &v
				return nil
			case "agent_thought_chunk":
				{
					raw, ok := m["content"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value ContentBlock
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["messageId"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value MessageId
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["sessionUpdate"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v SessionUpdateAgentThoughtChunk
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.AgentThoughtChunk = &v
				return nil
			case "agent_thought":
				{
					raw, ok := m["messageId"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value MessageId
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["sessionUpdate"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v SessionUpdateAgentThought
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.AgentThought = &v
				return nil
			case "state_update":
				{
					raw, ok := m["sessionUpdate"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var anyOfMatch bool
				{
					match := true
					{
						raw, ok := m["state"]
						if !ok {
							match = false
						}
						if ok {
							if string(raw) == "null" {
								match = false
							}
							var value string
							if json.Unmarshal(raw, &value) != nil {
								match = false
							}
						}
					}
					if match {
						anyOfMatch = true
					}
				}
				{
					match := true
					{
						raw, ok := m["state"]
						if !ok {
							match = false
						}
						if ok {
							if string(raw) == "null" {
								match = false
							}
							var value string
							if json.Unmarshal(raw, &value) != nil {
								match = false
							}
						}
					}
					if match {
						anyOfMatch = true
					}
				}
				{
					match := true
					{
						raw, ok := m["state"]
						if !ok {
							match = false
						}
						if ok {
							if string(raw) == "null" {
								match = false
							}
							var value string
							if json.Unmarshal(raw, &value) != nil {
								match = false
							}
						}
					}
					if match {
						anyOfMatch = true
					}
				}
				{
					match := true
					{
						raw, ok := m["state"]
						if !ok {
							match = false
						}
						if ok {
							if string(raw) == "null" {
								match = false
							}
							var value string
							if json.Unmarshal(raw, &value) != nil {
								match = false
							}
						}
					}
					if match {
						anyOfMatch = true
					}
				}
				if !anyOfMatch {
					return errors.New("invalid variant payload")
				}
				var v SessionStateUpdate
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.StateUpdate = &v
				return nil
			case "tool_call_content_chunk":
				{
					raw, ok := m["content"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value ToolCallContent
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["sessionUpdate"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["toolCallId"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value ToolCallId
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v SessionUpdateToolCallContentChunk
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.ToolCallContentChunk = &v
				return nil
			case "tool_call_update":
				{
					raw, ok := m["sessionUpdate"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["toolCallId"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value ToolCallId
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v SessionToolCallUpdate
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.ToolCallUpdate = &v
				return nil
			case "terminal_update":
				{
					raw, ok := m["sessionUpdate"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["terminalId"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value TerminalId
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v SessionTerminalUpdate
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.TerminalUpdate = &v
				return nil
			case "terminal_output_chunk":
				{
					raw, ok := m["data"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["sessionUpdate"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["terminalId"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value TerminalId
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v SessionUpdateTerminalOutputChunk
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.TerminalOutputChunk = &v
				return nil
			case "plan_update":
				{
					raw, ok := m["plan"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value PlanUpdateContent
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["sessionUpdate"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v SessionPlanUpdate
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.PlanUpdate = &v
				return nil
			case "available_commands_update":
				{
					raw, ok := m["availableCommands"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value []AvailableCommand
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["sessionUpdate"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v SessionAvailableCommandsUpdate
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.AvailableCommandsUpdate = &v
				return nil
			case "config_option_update":
				{
					raw, ok := m["configOptions"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value []SessionConfigOption
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["sessionUpdate"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v SessionConfigOptionUpdate
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.ConfigOptionUpdate = &v
				return nil
			case "session_info_update":
				{
					raw, ok := m["sessionUpdate"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v SessionSessionInfoUpdate
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.SessionInfoUpdate = &v
				return nil
			case "usage_update":
				{
					raw, ok := m["sessionUpdate"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["size"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value uint64
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["used"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value uint64
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v SessionUsageUpdate
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.UsageUpdate = &v
				return nil
			}
		}
		{
			var v SessionUpdateOther
			var match bool = true
			{
				raw, ok := m["sessionUpdate"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value string
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			if match {
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Other = &v
				return nil
			}
		}
	} else {
		if _, ok := err.(*json.UnmarshalTypeError); !ok {
			return err
		}
	}
	var arr []map[string]json.RawMessage
	if json.Unmarshal(b, &arr) == nil {
	}
	return errors.New("no matching variant for union")
}
func (u SessionUpdate) MarshalJSON() ([]byte, error) {
	if u.UserMessageChunk != nil {
		_b, _e := json.Marshal(*u.UserMessageChunk)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["sessionUpdate"] = json.RawMessage("\"user_message_chunk\"")
		return json.Marshal(m)
	}
	if u.UserMessage != nil {
		_b, _e := json.Marshal(*u.UserMessage)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["sessionUpdate"] = json.RawMessage("\"user_message\"")
		return json.Marshal(m)
	}
	if u.AgentMessageChunk != nil {
		_b, _e := json.Marshal(*u.AgentMessageChunk)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["sessionUpdate"] = json.RawMessage("\"agent_message_chunk\"")
		return json.Marshal(m)
	}
	if u.AgentMessage != nil {
		_b, _e := json.Marshal(*u.AgentMessage)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["sessionUpdate"] = json.RawMessage("\"agent_message\"")
		return json.Marshal(m)
	}
	if u.AgentThoughtChunk != nil {
		_b, _e := json.Marshal(*u.AgentThoughtChunk)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["sessionUpdate"] = json.RawMessage("\"agent_thought_chunk\"")
		return json.Marshal(m)
	}
	if u.AgentThought != nil {
		_b, _e := json.Marshal(*u.AgentThought)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["sessionUpdate"] = json.RawMessage("\"agent_thought\"")
		return json.Marshal(m)
	}
	if u.StateUpdate != nil {
		_b, _e := json.Marshal(*u.StateUpdate)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["sessionUpdate"] = json.RawMessage("\"state_update\"")
		return json.Marshal(m)
	}
	if u.ToolCallContentChunk != nil {
		_b, _e := json.Marshal(*u.ToolCallContentChunk)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["sessionUpdate"] = json.RawMessage("\"tool_call_content_chunk\"")
		return json.Marshal(m)
	}
	if u.ToolCallUpdate != nil {
		_b, _e := json.Marshal(*u.ToolCallUpdate)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["sessionUpdate"] = json.RawMessage("\"tool_call_update\"")
		return json.Marshal(m)
	}
	if u.TerminalUpdate != nil {
		_b, _e := json.Marshal(*u.TerminalUpdate)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["sessionUpdate"] = json.RawMessage("\"terminal_update\"")
		return json.Marshal(m)
	}
	if u.TerminalOutputChunk != nil {
		_b, _e := json.Marshal(*u.TerminalOutputChunk)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["sessionUpdate"] = json.RawMessage("\"terminal_output_chunk\"")
		return json.Marshal(m)
	}
	if u.PlanUpdate != nil {
		_b, _e := json.Marshal(*u.PlanUpdate)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["sessionUpdate"] = json.RawMessage("\"plan_update\"")
		return json.Marshal(m)
	}
	if u.AvailableCommandsUpdate != nil {
		_b, _e := json.Marshal(*u.AvailableCommandsUpdate)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["sessionUpdate"] = json.RawMessage("\"available_commands_update\"")
		return json.Marshal(m)
	}
	if u.ConfigOptionUpdate != nil {
		_b, _e := json.Marshal(*u.ConfigOptionUpdate)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["sessionUpdate"] = json.RawMessage("\"config_option_update\"")
		return json.Marshal(m)
	}
	if u.SessionInfoUpdate != nil {
		_b, _e := json.Marshal(*u.SessionInfoUpdate)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["sessionUpdate"] = json.RawMessage("\"session_info_update\"")
		return json.Marshal(m)
	}
	if u.UsageUpdate != nil {
		_b, _e := json.Marshal(*u.UsageUpdate)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["sessionUpdate"] = json.RawMessage("\"usage_update\"")
		return json.Marshal(m)
	}
	if u.Other != nil {
		_b, _e := json.Marshal(*u.Other)
		if _e != nil {
			return []byte{}, _e
		}
		return _b, nil
	}
	return []byte{}, nil
}

func (u *SessionUpdate) Validate() error {
	var count int
	if u.UserMessageChunk != nil {
		count++
	}
	if u.UserMessage != nil {
		count++
	}
	if u.AgentMessageChunk != nil {
		count++
	}
	if u.AgentMessage != nil {
		count++
	}
	if u.AgentThoughtChunk != nil {
		count++
	}
	if u.AgentThought != nil {
		count++
	}
	if u.StateUpdate != nil {
		count++
	}
	if u.ToolCallContentChunk != nil {
		count++
	}
	if u.ToolCallUpdate != nil {
		count++
	}
	if u.TerminalUpdate != nil {
		count++
	}
	if u.TerminalOutputChunk != nil {
		count++
	}
	if u.PlanUpdate != nil {
		count++
	}
	if u.AvailableCommandsUpdate != nil {
		count++
	}
	if u.ConfigOptionUpdate != nil {
		count++
	}
	if u.SessionInfoUpdate != nil {
		count++
	}
	if u.UsageUpdate != nil {
		count++
	}
	if u.Other != nil {
		count++
	}
	if count < 1 {
		return errors.New("SessionUpdate must have at least one variant set")
	}
	encoded, err := json.Marshal(*u)
	if err != nil {
		return err
	}
	var decoded SessionUpdate
	if err := json.Unmarshal(encoded, &decoded); err != nil {
		return fmt.Errorf("SessionUpdate: %w", err)
	}
	return nil
}

// Request parameters for setting a session configuration option.
// A ['SessionConfigValueId'] string value ('type: "id"').
type SetSessionConfigOptionId struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The ID of the configuration option to set.
	ConfigId SessionConfigId `json:"configId"`
	// The ID of the session to set the configuration option for.
	SessionId SessionId `json:"sessionId"`
	Type      string    `json:"type"`
	// The value ID.
	Value SessionConfigValueId `json:"value"`
}

// A boolean value ('type: "boolean"').
type SetSessionConfigOptionBoolean struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The ID of the configuration option to set.
	ConfigId SessionConfigId `json:"configId"`
	// The ID of the session to set the configuration option for.
	SessionId SessionId `json:"sessionId"`
	Type      string    `json:"type"`
	// The boolean value.
	Value bool `json:"value"`
}

// Custom or future session configuration option value payload.
//
// Values beginning with '_' are reserved for implementation-specific
// extensions. Unknown values that do not begin with '_' are reserved for
// future ACP variants.
type SetSessionConfigOptionOther = json.RawMessage

type SetSessionConfigOptionRequest struct {
	// A ['SessionConfigValueId'] string value ('type: "id"').
	Id *SetSessionConfigOptionId `json:"-"`
	// A boolean value ('type: "boolean"').
	Boolean *SetSessionConfigOptionBoolean `json:"-"`
	// Custom or future session configuration option value payload.
	//
	// Values beginning with '_' are reserved for implementation-specific
	// extensions. Unknown values that do not begin with '_' are reserved for
	// future ACP variants.
	Other *SetSessionConfigOptionOther `json:"-"`
}

func (u *SetSessionConfigOptionRequest) UnmarshalJSON(b []byte) error {
	*u = SetSessionConfigOptionRequest{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err == nil {
		{
			var disc string
			if v, ok := m["type"]; ok {
				json.Unmarshal(v, &disc)
			}
			switch disc {
			case "id":
				{
					raw, ok := m["configId"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value SessionConfigId
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["sessionId"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value SessionId
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["type"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["value"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value SessionConfigValueId
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v SetSessionConfigOptionId
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Id = &v
				return nil
			case "boolean":
				{
					raw, ok := m["configId"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value SessionConfigId
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["sessionId"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value SessionId
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["type"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["value"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value bool
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v SetSessionConfigOptionBoolean
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Boolean = &v
				return nil
			}
		}
		{
			var v SetSessionConfigOptionOther
			var match bool = true
			{
				raw, ok := m["configId"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value SessionConfigId
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			{
				raw, ok := m["sessionId"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value SessionId
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			{
				raw, ok := m["type"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value string
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			{
				raw, ok := m["value"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value any
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			if match {
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Other = &v
				return nil
			}
		}
	} else {
		if _, ok := err.(*json.UnmarshalTypeError); !ok {
			return err
		}
	}
	var arr []map[string]json.RawMessage
	if json.Unmarshal(b, &arr) == nil {
	}
	return errors.New("no matching variant for union")
}
func (u SetSessionConfigOptionRequest) MarshalJSON() ([]byte, error) {
	if u.Id != nil {
		_b, _e := json.Marshal(*u.Id)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["type"] = json.RawMessage("\"id\"")
		return json.Marshal(m)
	}
	if u.Boolean != nil {
		_b, _e := json.Marshal(*u.Boolean)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["type"] = json.RawMessage("\"boolean\"")
		return json.Marshal(m)
	}
	if u.Other != nil {
		_b, _e := json.Marshal(*u.Other)
		if _e != nil {
			return []byte{}, _e
		}
		return _b, nil
	}
	return []byte{}, nil
}

func (u *SetSessionConfigOptionRequest) Validate() error {
	var count int
	if u.Id != nil {
		count++
	}
	if u.Boolean != nil {
		count++
	}
	if u.Other != nil {
		count++
	}
	if count < 1 {
		return errors.New("SetSessionConfigOptionRequest must have at least one variant set")
	}
	encoded, err := json.Marshal(*u)
	if err != nil {
		return err
	}
	var decoded SetSessionConfigOptionRequest
	if err := json.Unmarshal(encoded, &decoded); err != nil {
		return fmt.Errorf("SetSessionConfigOptionRequest: %w", err)
	}
	return nil
}

// Response to 'session/set_config_option' method.
type SetSessionConfigOptionResponse struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The full set of configuration options and their current values.
	ConfigOptions []SessionConfigOption `json:"configOptions"`
}

func (v *SetSessionConfigOptionResponse) UnmarshalJSON(b []byte) error {
	*v = SetSessionConfigOptionResponse{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["configOptions"]
		if !ok {
			return fmt.Errorf("configOptions is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("configOptions must not be null")
		}
	}
	type Alias SetSessionConfigOptionResponse
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = SetSessionConfigOptionResponse(a)
	return nil
}

func (v *SetSessionConfigOptionResponse) Validate() error {
	if v.ConfigOptions == nil {
		return fmt.Errorf("configOptions is required")
	}
	for i := range v.ConfigOptions {
		if err := v.ConfigOptions[i].Validate(); err != nil {
			return fmt.Errorf("configOptions[%d]: %w", i, err)
		}
	}
	return nil
}

// The state of the agent's foreground work has changed.
//
// Background activity can continue and emit other 'session/update' notifications
// while 'idle'. Those notifications do not change this state.
// Foreground work is in progress.
type StateUpdateRunning struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta  map[string]json.RawMessage `json:"_meta,omitempty"`
	State string                     `json:"state"`
}

// The agent is ready to process a new prompt.
type StateUpdateIdle struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta  map[string]json.RawMessage `json:"_meta,omitempty"`
	State string                     `json:"state"`
	// Indicates why foreground work stopped.
	//
	// Optional. Omitted or 'null' both mean the agent is not reporting a stop reason.
	// Agents SHOULD include this when the idle transition ends foreground work.
	StopReason *StopReason `json:"stopReason,omitempty"`
}

// Foreground work is blocked on user action.
type StateUpdateRequiresAction struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta  map[string]json.RawMessage `json:"_meta,omitempty"`
	State string                     `json:"state"`
}

// Custom or future session state.
//
// Values beginning with '_' are reserved for implementation-specific
// extensions. Unknown values that do not begin with '_' are reserved for
// future ACP variants.
type StateUpdateOther = json.RawMessage

type StateUpdate struct {
	// Foreground work is in progress.
	Running *StateUpdateRunning `json:"-"`
	// The agent is ready to process a new prompt.
	Idle *StateUpdateIdle `json:"-"`
	// Foreground work is blocked on user action.
	RequiresAction *StateUpdateRequiresAction `json:"-"`
	// Custom or future session state.
	//
	// Values beginning with '_' are reserved for implementation-specific
	// extensions. Unknown values that do not begin with '_' are reserved for
	// future ACP variants.
	Other *StateUpdateOther `json:"-"`
}

func (u *StateUpdate) UnmarshalJSON(b []byte) error {
	*u = StateUpdate{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err == nil {
		{
			var disc string
			if v, ok := m["state"]; ok {
				json.Unmarshal(v, &disc)
			}
			switch disc {
			case "running":
				{
					raw, ok := m["state"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v StateUpdateRunning
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Running = &v
				return nil
			case "idle":
				{
					raw, ok := m["state"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v StateUpdateIdle
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Idle = &v
				return nil
			case "requires_action":
				{
					raw, ok := m["state"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v StateUpdateRequiresAction
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.RequiresAction = &v
				return nil
			}
		}
		{
			var v StateUpdateOther
			var match bool = true
			{
				raw, ok := m["state"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value string
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			if match {
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Other = &v
				return nil
			}
		}
	} else {
		if _, ok := err.(*json.UnmarshalTypeError); !ok {
			return err
		}
	}
	var arr []map[string]json.RawMessage
	if json.Unmarshal(b, &arr) == nil {
	}
	return errors.New("no matching variant for union")
}
func (u StateUpdate) MarshalJSON() ([]byte, error) {
	if u.Running != nil {
		_b, _e := json.Marshal(*u.Running)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["state"] = json.RawMessage("\"running\"")
		return json.Marshal(m)
	}
	if u.Idle != nil {
		_b, _e := json.Marshal(*u.Idle)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["state"] = json.RawMessage("\"idle\"")
		return json.Marshal(m)
	}
	if u.RequiresAction != nil {
		_b, _e := json.Marshal(*u.RequiresAction)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["state"] = json.RawMessage("\"requires_action\"")
		return json.Marshal(m)
	}
	if u.Other != nil {
		_b, _e := json.Marshal(*u.Other)
		if _e != nil {
			return []byte{}, _e
		}
		return _b, nil
	}
	return []byte{}, nil
}

func (u *StateUpdate) Validate() error {
	var count int
	if u.Running != nil {
		count++
	}
	if u.Idle != nil {
		count++
	}
	if u.RequiresAction != nil {
		count++
	}
	if u.Other != nil {
		count++
	}
	if count < 1 {
		return errors.New("StateUpdate must have at least one variant set")
	}
	encoded, err := json.Marshal(*u)
	if err != nil {
		return err
	}
	var decoded StateUpdate
	if err := json.Unmarshal(encoded, &decoded); err != nil {
		return fmt.Errorf("StateUpdate: %w", err)
	}
	return nil
}

// Reasons why an agent stops active session work.
//
// See protocol docs: [Stop Reasons](https://agentclientprotocol.com/protocol/v2/prompt-lifecycle#stop-reasons)
type StopReason string

const (
	StopReasonEndTurn         StopReason = "end_turn"
	StopReasonMaxTokens       StopReason = "max_tokens"
	StopReasonMaxTurnRequests StopReason = "max_turn_requests"
	StopReasonRefusal         StopReason = "refusal"
	StopReasonCancelled       StopReason = "cancelled"
)

// String format types for string properties in elicitation schemas.
type StringFormat string

const (
	StringFormatEmail    StringFormat = "email"
	StringFormatUri      StringFormat = "uri"
	StringFormatDate     StringFormat = "date"
	StringFormatDateTime StringFormat = "date-time"
)

// String item schema for multi-select enum properties.
type StringMultiSelectItems struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// Optional. Omitted and 'null' are equivalent and mean no metadata.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Allowed enum values. Must contain at least one value.
	Enum []string `json:"enum"`
}

func (v *StringMultiSelectItems) UnmarshalJSON(b []byte) error {
	*v = StringMultiSelectItems{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["enum"]
		if !ok {
			return fmt.Errorf("enum is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("enum must not be null")
		}
	}
	type Alias StringMultiSelectItems
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = StringMultiSelectItems(a)
	return nil
}

// Schema for string properties in an elicitation form.
//
// When 'enum' or 'oneOf' is set, this represents a single-select enum
// with '"type": "string"'.
type StringPropertySchema struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// Optional. Omitted and 'null' are equivalent and mean no metadata.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Default value.
	//
	// Optional. Omitted and 'null' are equivalent and mean no default value is provided.
	Default *string `json:"default,omitempty"`
	// Human-readable description.
	//
	// Optional. Omitted and 'null' are equivalent and mean no description is provided.
	Description *string `json:"description,omitempty"`
	// Enum values for untitled single-select enums.
	// Must contain at least one value when present.
	// Optional. Omitted and 'null' are equivalent and mean no untitled single-select choices are
	// declared by 'enum'.
	Enum []string `json:"enum,omitempty"`
	// String format.
	//
	// Optional. Omitted and 'null' are equivalent and mean there is no format constraint.
	Format *StringFormat `json:"format,omitempty"`
	// Maximum string length.
	//
	// Optional. Omitted and 'null' are equivalent and mean there is no maximum length constraint.
	MaxLength *uint32 `json:"maxLength,omitempty"`
	// Minimum string length.
	//
	// Optional. Omitted and 'null' are equivalent and mean there is no minimum length constraint.
	MinLength *uint32 `json:"minLength,omitempty"`
	// Titled enum options for titled single-select enums.
	// Must contain at least one option when present.
	// Optional. Omitted and 'null' are equivalent and mean no titled single-select choices are
	// declared by 'oneOf'.
	OneOf []EnumOption `json:"oneOf,omitempty"`
	// Pattern the string must match.
	//
	// Optional. Omitted and 'null' are equivalent and mean there is no pattern constraint.
	Pattern *string `json:"pattern,omitempty"`
	// Optional title for the property.
	//
	// Optional. Omitted and 'null' are equivalent and mean no title is provided.
	Title *string `json:"title,omitempty"`
}

// A display-only reference to an agent-owned terminal.
//
// Terminal state and output are delivered separately through
// ['TerminalUpdate'] and ['TerminalOutputChunk'].
type Terminal struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys. This metadata is scoped to the content reference. Omitted
	// and 'null' are equivalent and mean no item metadata was provided.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The ID of the terminal to display.
	TerminalId TerminalId `json:"terminalId"`
}

func (v *Terminal) UnmarshalJSON(b []byte) error {
	*v = Terminal{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["terminalId"]
		if !ok {
			return fmt.Errorf("terminalId is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("terminalId must not be null")
		}
	}
	type Alias Terminal
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = Terminal(a)
	return nil
}

// Capabilities for terminal authentication methods.
//
// Supplying '{}' means the client can reproduce the configured agent
// invocation in an interactive terminal and supports terminal authentication
// methods.
type TerminalAuthCapabilities struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
}

// Exit information for an agent-owned terminal.
//
// The presence of this object marks the terminal as exited, even when neither
// an exit code nor a signal is known.
type TerminalExitStatus struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys. This metadata is scoped to the exit information. Omitted
	// and 'null' are equivalent and mean no exit metadata was provided.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Process exit code, when known. Omitted and 'null' are equivalent.
	ExitCode *uint32 `json:"exitCode,omitempty"`
	// Signal that terminated the process, when known.
	//
	// Agents should use the conventional platform signal name. POSIX examples
	// include 'SIGTERM', 'SIGKILL', and 'SIGINT'. Other platforms may use a
	// platform-specific name. Omitted and 'null' are equivalent.
	Signal *string `json:"signal,omitempty"`
}

// Unique identifier for an agent-owned terminal within a session.
type TerminalId string

// An authoritative replacement snapshot of terminal output bytes.
type TerminalOutput struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys. This metadata is scoped to the replacement snapshot. Omitted
	// and 'null' are equivalent and mean no snapshot metadata was provided.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Base64-encoded replacement terminal output bytes.
	Data string `json:"data"`
}

func (v *TerminalOutput) UnmarshalJSON(b []byte) error {
	*v = TerminalOutput{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["data"]
		if !ok {
			return fmt.Errorf("data is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("data must not be null")
		}
	}
	type Alias TerminalOutput
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = TerminalOutput(a)
	return nil
}

// A chunk of bytes appended to an agent-owned terminal's output.
type TerminalOutputChunk struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys. This field is chunk-scoped. Omitted and 'null' are
	// equivalent and mean no chunk metadata was provided.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Independently base64-encoded terminal output bytes.
	Data string `json:"data"`
	// The terminal receiving these bytes.
	TerminalId TerminalId `json:"terminalId"`
}

func (v *TerminalOutputChunk) UnmarshalJSON(b []byte) error {
	*v = TerminalOutputChunk{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["data"]
		if !ok {
			return fmt.Errorf("data is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("data must not be null")
		}
	}
	{
		raw, ok := m["terminalId"]
		if !ok {
			return fmt.Errorf("terminalId is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("terminalId must not be null")
		}
	}
	type Alias TerminalOutputChunk
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = TerminalOutputChunk(a)
	return nil
}

// An upsert for the stored state of an agent-owned terminal.
//
// Only ['TerminalUpdate::terminal_id'] is required. Other fields have patch
// semantics: omitted fields leave the stored value unchanged, 'null' clears
// it, and concrete values replace it. When the terminal ID is new, omitted
// fields start unknown.
type TerminalUpdate struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Omitted means no metadata update; 'null' is an
	// explicit clear signal. Implementations MUST NOT make assumptions about values at these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The command being run.
	Command *string `json:"command,omitempty"`
	// The absolute working directory of the command.
	Cwd *AbsolutePath `json:"cwd,omitempty"`
	// Exit information. A concrete object marks the terminal as exited.
	ExitStatus *TerminalExitStatus `json:"exitStatus,omitempty"`
	// An authoritative replacement snapshot of terminal output bytes.
	Output *TerminalOutput `json:"output,omitempty"`
	// Unique identifier for this terminal within the session.
	TerminalId TerminalId `json:"terminalId"`
}

func (v *TerminalUpdate) UnmarshalJSON(b []byte) error {
	*v = TerminalUpdate{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["terminalId"]
		if !ok {
			return fmt.Errorf("terminalId is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("terminalId must not be null")
		}
	}
	type Alias TerminalUpdate
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = TerminalUpdate(a)
	return nil
}

// All text that was typed after the command name is provided as input.
type TextCommandInput struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// A hint to display when the input hasn't been provided yet
	Hint string `json:"hint"`
}

func (v *TextCommandInput) UnmarshalJSON(b []byte) error {
	*v = TextCommandInput{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["hint"]
		if !ok {
			return fmt.Errorf("hint is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("hint must not be null")
		}
	}
	type Alias TextCommandInput
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = TextCommandInput(a)
	return nil
}

// Text provided to or from an LLM.
type TextContent struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Optional annotations that help clients decide how to display or route this content.
	Annotations *Annotations `json:"annotations,omitempty"`
	// Text payload carried by this content block.
	Text string `json:"text"`
}

func (v *TextContent) UnmarshalJSON(b []byte) error {
	*v = TextContent{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["text"]
		if !ok {
			return fmt.Errorf("text is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("text must not be null")
		}
	}
	type Alias TextContent
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = TextContent(a)
	return nil
}

// Text-based resource contents.
type TextResourceContents struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// MIME type describing the encoded media payload.
	MimeType *MediaType `json:"mimeType,omitempty"`
	// Text payload carried by this content block.
	Text string `json:"text"`
	// URI associated with this resource or media payload.
	Uri string `json:"uri"`
}

func (v *TextResourceContents) UnmarshalJSON(b []byte) error {
	*v = TextResourceContents{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["text"]
		if !ok {
			return fmt.Errorf("text is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("text must not be null")
		}
	}
	{
		raw, ok := m["uri"]
		if !ok {
			return fmt.Errorf("uri is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("uri must not be null")
		}
	}
	type Alias TextResourceContents
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = TextResourceContents(a)
	return nil
}

// Items definition for titled multi-select enum properties.
type TitledMultiSelectItems struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// Optional. Omitted and 'null' are equivalent and mean no metadata.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Titled enum options. Must contain at least one option.
	AnyOf []EnumOption `json:"anyOf"`
}

func (v *TitledMultiSelectItems) UnmarshalJSON(b []byte) error {
	*v = TitledMultiSelectItems{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["anyOf"]
		if !ok {
			return fmt.Errorf("anyOf is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("anyOf must not be null")
		}
	}
	type Alias TitledMultiSelectItems
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = TitledMultiSelectItems(a)
	return nil
}

// Content produced by a tool call.
//
// Tool calls can produce different types of content including standard
// content blocks (text, images), file diffs, or display-only terminals.
//
// See protocol docs: [Content](https://agentclientprotocol.com/protocol/v2/tool-calls#content)
// Standard content block (text, images, resources).
type ToolCallContentContent struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The actual content block.
	Content ContentBlock `json:"content"`
	Type    string       `json:"type"`
}

// File modification shown as a diff.
type ToolCallContentDiff struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Structured file changes described by this diff.
	//
	// Clients can use this field without parsing patch text to determine affected paths.
	Changes []DiffChange `json:"changes"`
	// Renderable patch text for some or all of the structured changes.
	//
	// Agents SHOULD provide patch text whenever feasible. Omitted or 'null'
	// means no renderable patch text was provided.
	Patch *DiffPatch `json:"patch,omitempty"`
	Type  string     `json:"type"`
}

// A display-only reference to an agent-owned terminal.
type ToolCallContentTerminal struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys. This metadata is scoped to the content reference. Omitted
	// and 'null' are equivalent and mean no item metadata was provided.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The ID of the terminal to display.
	TerminalId TerminalId `json:"terminalId"`
	Type       string     `json:"type"`
}

// Custom or future tool call content.
//
// Values beginning with '_' are reserved for implementation-specific
// extensions. Unknown values that do not begin with '_' are reserved for
// future ACP variants.
//
// Receivers that do not understand this content type should preserve the
// raw payload when storing, replaying, proxying, or forwarding tool call
// output, and otherwise ignore it or display it generically.
type ToolCallContentOther = json.RawMessage

type ToolCallContent struct {
	// Standard content block (text, images, resources).
	Content *ToolCallContentContent `json:"-"`
	// File modification shown as a diff.
	Diff *ToolCallContentDiff `json:"-"`
	// A display-only reference to an agent-owned terminal.
	Terminal *ToolCallContentTerminal `json:"-"`
	// Custom or future tool call content.
	//
	// Values beginning with '_' are reserved for implementation-specific
	// extensions. Unknown values that do not begin with '_' are reserved for
	// future ACP variants.
	//
	// Receivers that do not understand this content type should preserve the
	// raw payload when storing, replaying, proxying, or forwarding tool call
	// output, and otherwise ignore it or display it generically.
	Other *ToolCallContentOther `json:"-"`
}

func (u *ToolCallContent) UnmarshalJSON(b []byte) error {
	*u = ToolCallContent{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err == nil {
		{
			var disc string
			if v, ok := m["type"]; ok {
				json.Unmarshal(v, &disc)
			}
			switch disc {
			case "content":
				{
					raw, ok := m["content"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value ContentBlock
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["type"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v ToolCallContentContent
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Content = &v
				return nil
			case "diff":
				{
					raw, ok := m["changes"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value []DiffChange
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["type"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v ToolCallContentDiff
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Diff = &v
				return nil
			case "terminal":
				{
					raw, ok := m["terminalId"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value TerminalId
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				{
					raw, ok := m["type"]
					if !ok {
						return errors.New("invalid variant payload")
					}
					if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
						return errors.New("invalid variant payload")
					}
					var value string
					if json.Unmarshal(raw, &value) != nil {
						return errors.New("invalid variant payload")
					}
				}
				var v ToolCallContentTerminal
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Terminal = &v
				return nil
			}
		}
		{
			var v ToolCallContentOther
			var match bool = true
			{
				raw, ok := m["type"]
				if !ok {
					match = false
				}
				if ok && bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
					match = false
				}
				if ok {
					var value string
					if json.Unmarshal(raw, &value) != nil {
						match = false
					}
				}
			}
			if match {
				if json.Unmarshal(b, &v) != nil {
					return errors.New("invalid variant payload")
				}
				u.Other = &v
				return nil
			}
		}
	} else {
		if _, ok := err.(*json.UnmarshalTypeError); !ok {
			return err
		}
	}
	var arr []map[string]json.RawMessage
	if json.Unmarshal(b, &arr) == nil {
	}
	return errors.New("no matching variant for union")
}
func (u ToolCallContent) MarshalJSON() ([]byte, error) {
	if u.Content != nil {
		_b, _e := json.Marshal(*u.Content)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["type"] = json.RawMessage("\"content\"")
		return json.Marshal(m)
	}
	if u.Diff != nil {
		_b, _e := json.Marshal(*u.Diff)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["type"] = json.RawMessage("\"diff\"")
		return json.Marshal(m)
	}
	if u.Terminal != nil {
		_b, _e := json.Marshal(*u.Terminal)
		if _e != nil {
			return []byte{}, _e
		}
		var m map[string]json.RawMessage
		if json.Unmarshal(_b, &m) != nil {
			return []byte{}, errors.New("invalid variant payload")
		}
		m["type"] = json.RawMessage("\"terminal\"")
		return json.Marshal(m)
	}
	if u.Other != nil {
		_b, _e := json.Marshal(*u.Other)
		if _e != nil {
			return []byte{}, _e
		}
		return _b, nil
	}
	return []byte{}, nil
}

func (u *ToolCallContent) Validate() error {
	var count int
	if u.Content != nil {
		count++
	}
	if u.Diff != nil {
		count++
	}
	if u.Terminal != nil {
		count++
	}
	if u.Other != nil {
		count++
	}
	if count < 1 {
		return errors.New("ToolCallContent must have at least one variant set")
	}
	encoded, err := json.Marshal(*u)
	if err != nil {
		return err
	}
	var decoded ToolCallContent
	if err := json.Unmarshal(encoded, &decoded); err != nil {
		return fmt.Errorf("ToolCallContent: %w", err)
	}
	return nil
}

// A streamed item of tool-call content.
//
// Tool-call content chunks append one ['ToolCallContent'] item to the current
// content for the matching ['ToolCallId']. Agents can use
// ['ToolCallUpdate::content'] when they need to replace the whole content
// collection instead.
type ToolCallContentChunk struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys. This field is chunk-scoped.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// A single item of content produced by the tool call.
	Content ToolCallContent `json:"content"`
	// The ID of the tool call this content belongs to.
	ToolCallId ToolCallId `json:"toolCallId"`
}

func (v *ToolCallContentChunk) UnmarshalJSON(b []byte) error {
	*v = ToolCallContentChunk{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["content"]
		if !ok {
			return fmt.Errorf("content is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("content must not be null")
		}
	}
	{
		raw, ok := m["toolCallId"]
		if !ok {
			return fmt.Errorf("toolCallId is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("toolCallId must not be null")
		}
	}
	type Alias ToolCallContentChunk
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = ToolCallContentChunk(a)
	return nil
}

// Unique identifier for a tool call within a session.
type ToolCallId string

// A file location being accessed or modified by a tool.
//
// Enables clients to implement "follow-along" features that track
// which files the agent is working with in real-time.
//
// See protocol docs: [Following the Agent](https://agentclientprotocol.com/protocol/v2/tool-calls#following-the-agent)
type ToolCallLocation struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Optional line number within the file.
	Line *uint32 `json:"line,omitempty"`
	// The absolute file path being accessed or modified.
	Path AbsolutePath `json:"path"`
}

func (v *ToolCallLocation) UnmarshalJSON(b []byte) error {
	*v = ToolCallLocation{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["path"]
		if !ok {
			return fmt.Errorf("path is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("path must not be null")
		}
	}
	type Alias ToolCallLocation
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = ToolCallLocation(a)
	return nil
}

// Permission request details for a tool call.
type ToolCallPermissionSubject struct {
	// Details about the tool call requiring permission.
	ToolCall ToolCallUpdate `json:"toolCall"`
}

func (v *ToolCallPermissionSubject) UnmarshalJSON(b []byte) error {
	*v = ToolCallPermissionSubject{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["toolCall"]
		if !ok {
			return fmt.Errorf("toolCall is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("toolCall must not be null")
		}
	}
	type Alias ToolCallPermissionSubject
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = ToolCallPermissionSubject(a)
	return nil
}

// Execution status of a tool call.
//
// Tool calls progress through different statuses during their lifecycle.
//
// See protocol docs: [Status](https://agentclientprotocol.com/protocol/v2/tool-calls#status)
type ToolCallStatus string

const (
	ToolCallStatusPending    ToolCallStatus = "pending"
	ToolCallStatusInProgress ToolCallStatus = "in_progress"
	ToolCallStatusCompleted  ToolCallStatus = "completed"
	ToolCallStatusFailed     ToolCallStatus = "failed"
	ToolCallStatusCancelled  ToolCallStatus = "cancelled"
)

// Represents an upsert for a tool call that the language model has requested.
//
// Tool calls are actions that the agent executes on behalf of the language model,
// such as reading files, executing code, or fetching data from external sources.
//
// Only ['ToolCallUpdate::tool_call_id'] is required. Other fields have patch semantics:
// omitted fields leave the existing tool call value unchanged, 'null' clears or
// unsets the value, and concrete values replace the previous value. For
// collection fields, concrete arrays replace the previous collection, and both
// 'null' and '[]' clear the collection. When a client receives a tool call ID it
// has not seen before, omitted fields use client defaults.
//
// See protocol docs: [Tool Calls](https://agentclientprotocol.com/protocol/v2/tool-calls)
type ToolCallUpdate struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Omitted means no metadata update; 'null' is an
	// explicit clear signal. Implementations MUST NOT make assumptions about values at these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Content produced by the tool call.
	Content []ToolCallContent `json:"content,omitempty"`
	// The category of tool being invoked.
	// Helps clients choose appropriate icons and UI treatment.
	Kind *ToolKind `json:"kind,omitempty"`
	// File locations affected by this tool call.
	// Enables "follow-along" features in clients.
	Locations []ToolCallLocation `json:"locations,omitempty"`
	// Raw input parameters sent to the tool.
	RawInput any `json:"rawInput,omitempty"`
	// Raw output returned by the tool.
	RawOutput any `json:"rawOutput,omitempty"`
	// Current execution status of the tool call.
	Status *ToolCallStatus `json:"status,omitempty"`
	// Human-readable title describing what the tool is doing.
	Title *string `json:"title,omitempty"`
	// Unique identifier for this tool call within the session.
	ToolCallId ToolCallId `json:"toolCallId"`
}

func (v *ToolCallUpdate) UnmarshalJSON(b []byte) error {
	*v = ToolCallUpdate{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["toolCallId"]
		if !ok {
			return fmt.Errorf("toolCallId is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("toolCallId must not be null")
		}
	}
	type Alias ToolCallUpdate
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = ToolCallUpdate(a)
	return nil
}

func (v *ToolCallUpdate) Validate() error {
	return nil
}

// Categories of tools that can be invoked.
//
// Tool kinds help clients choose appropriate icons and optimize how they
// display tool execution progress.
//
// See protocol docs: [Creating](https://agentclientprotocol.com/protocol/v2/tool-calls#creating)
type ToolKind string

const (
	ToolKindRead       ToolKind = "read"
	ToolKindEdit       ToolKind = "edit"
	ToolKindDelete     ToolKind = "delete"
	ToolKindMove       ToolKind = "move"
	ToolKindSearch     ToolKind = "search"
	ToolKindExecute    ToolKind = "execute"
	ToolKindThink      ToolKind = "think"
	ToolKindFetch      ToolKind = "fetch"
	ToolKindSwitchMode ToolKind = "switch_mode"
	ToolKindOther      ToolKind = "other"
)

// Notification containing a session update from the agent.
//
// Agents can send session updates at any point while the session exists.
//
// See protocol docs: [Agent Reports Output](https://agentclientprotocol.com/protocol/v2/prompt-lifecycle#3-agent-reports-output)
type UpdateSessionNotification struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// The ID of the session this update pertains to.
	SessionId SessionId `json:"sessionId"`
	// The actual update content.
	Update SessionUpdate `json:"update"`
}

func (v *UpdateSessionNotification) UnmarshalJSON(b []byte) error {
	*v = UpdateSessionNotification{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["sessionId"]
		if !ok {
			return fmt.Errorf("sessionId is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("sessionId must not be null")
		}
	}
	{
		raw, ok := m["update"]
		if !ok {
			return fmt.Errorf("update is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("update must not be null")
		}
	}
	type Alias UpdateSessionNotification
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = UpdateSessionNotification(a)
	return nil
}

func (v *UpdateSessionNotification) Validate() error {
	if err := v.Update.Validate(); err != nil {
		return fmt.Errorf("update: %w", err)
	}
	return nil
}

// Context window and cost update for a session.
type UsageUpdate struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Cumulative session cost (optional).
	Cost *Cost `json:"cost,omitempty"`
	// Total context window size in tokens.
	Size uint64 `json:"size"`
	// Tokens currently in context.
	Used uint64 `json:"used"`
}

func (v *UsageUpdate) UnmarshalJSON(b []byte) error {
	*v = UsageUpdate{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["size"]
		if !ok {
			return fmt.Errorf("size is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("size must not be null")
		}
	}
	{
		raw, ok := m["used"]
		if !ok {
			return fmt.Errorf("used is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("used must not be null")
		}
	}
	type Alias UsageUpdate
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = UsageUpdate(a)
	return nil
}

// A user message upsert.
//
// Only ['UserMessage::message_id'] is required. 'content' has patch semantics:
// an omitted field leaves existing message content unchanged, 'null' clears the
// value, and a concrete array replaces the previous value. For a new
// 'messageId', omitted fields use client defaults. 'content' is replaced as a
// whole array; send '[]' or 'null' to clear it.
//
// Message updates and chunks are applied in the order they are received. When
// a 'user_message' update includes 'content', that array replaces any content
// previously accumulated for the message, including content from earlier
// chunks. Later chunks with the same 'messageId' append to the current
// content.
type UserMessage struct {
	// The _meta property is reserved by ACP to allow clients and agents to attach additional
	// metadata to their interactions. Implementations MUST NOT make assumptions about values at
	// these keys. Omitted means no metadata update; 'null' is an explicit clear signal.
	//
	// See protocol docs: [Extensibility](https://agentclientprotocol.com/protocol/v2/extensibility)
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`
	// Complete replacement content for this message.
	Content []ContentBlock `json:"content,omitempty"`
	// A unique identifier for the message.
	MessageId MessageId `json:"messageId"`
}

func (v *UserMessage) UnmarshalJSON(b []byte) error {
	*v = UserMessage{}
	var m map[string]json.RawMessage
	if err := json.Unmarshal(b, &m); err != nil {
		return err
	}
	{
		raw, ok := m["messageId"]
		if !ok {
			return fmt.Errorf("messageId is required")
		}
		if bytes.Equal(bytes.TrimSpace(raw), []byte("null")) {
			return fmt.Errorf("messageId must not be null")
		}
	}
	type Alias UserMessage
	var a Alias
	if err := json.Unmarshal(b, &a); err != nil {
		return err
	}
	*v = UserMessage(a)
	return nil
}

type Agent interface {
	// Request parameters for the 'auth/login' method.
	//
	// Specifies which authentication method to use.
	//
	// Agents MUST support this method when their 'initialize' response advertised
	// at least one valid authentication method. Clients MUST NOT call this method
	// when 'authMethods' was omitted or empty.
	LoginAuth(ctx context.Context, params LoginAuthRequest) (LoginAuthResponse, error)
	// Request parameters for the 'auth/logout' method.
	//
	// Terminates the current authenticated session.
	//
	// Agents MUST support this method when their 'initialize' response advertised
	// at least one valid authentication method. Clients MUST NOT call this method
	// when 'authMethods' was omitted or empty.
	LogoutAuth(ctx context.Context, params LogoutAuthRequest) (LogoutAuthResponse, error)
	// Request parameters for the initialize method.
	//
	// Sent by the client to establish connection and negotiate capabilities.
	//
	// See protocol docs: [Initialization](https://agentclientprotocol.com/protocol/v2/initialization)
	Initialize(ctx context.Context, params InitializeRequest) (InitializeResponse, error)
	// Notification to cancel ongoing operations for a session.
	//
	// See protocol docs: [Cancellation](https://agentclientprotocol.com/protocol/v2/prompt-lifecycle#cancellation)
	Cancel(ctx context.Context, params CancelSessionNotification) error
	// Request parameters for creating a new session.
	//
	// See protocol docs: [Creating a Session](https://agentclientprotocol.com/protocol/v2/session-setup#creating-a-session)
	NewSession(ctx context.Context, params NewSessionRequest) (NewSessionResponse, error)
	// Request parameters for sending a user prompt to the agent.
	//
	// Contains the user's message and any additional context.
	//
	// See protocol docs: [User Message](https://agentclientprotocol.com/protocol/v2/prompt-lifecycle#1-user-message)
	Prompt(ctx context.Context, params PromptRequest) (PromptResponse, error)
}

// AgentSessionCloser defines optional session close support.
type AgentSessionCloser interface {
	// Request parameters for closing an active session.
	//
	// The agent **must** cancel any ongoing work related to the session (treat it
	// as if 'session/cancel' was called) and then free up any resources associated
	// with the session.
	CloseSession(ctx context.Context, params CloseSessionRequest) (CloseSessionResponse, error)
}

// AgentSessionDeleter defines optional session delete support.
type AgentSessionDeleter interface {
	// Request parameters for deleting an existing session from 'session/list'.
	//
	// Only available if the Agent supports the 'session.delete' capability.
	DeleteSession(ctx context.Context, params DeleteSessionRequest) (DeleteSessionResponse, error)
}

// AgentSessionLister defines optional session list support.
type AgentSessionLister interface {
	// Request parameters for listing existing sessions.
	ListSessions(ctx context.Context, params ListSessionsRequest) (ListSessionsResponse, error)
}

// AgentSessionResumer defines optional session resume support.
type AgentSessionResumer interface {
	// Request parameters for resuming an existing session.
	//
	// Resumes an existing session and optionally replays prior conversation
	// history according to 'replayFrom'.
	ResumeSession(ctx context.Context, params ResumeSessionRequest) (ResumeSessionResponse, error)
}

// AgentSessionConfig defines optional session configuration support.
type AgentSessionConfig interface {
	// Request parameters for setting a session configuration option.
	SetSessionConfigOption(ctx context.Context, params SetSessionConfigOptionRequest) (SetSessionConfigOptionResponse, error)
}
type Client interface {
	// Request for user permission to proceed with an operation.
	//
	// Sent when the agent needs authorization before performing a sensitive operation.
	//
	// See protocol docs: [Requesting Permission](https://agentclientprotocol.com/protocol/v2/tool-calls#requesting-permission)
	RequestPermission(ctx context.Context, params RequestPermissionRequest) (RequestPermissionResponse, error)
	// Notification containing a session update from the agent.
	//
	// Agents can send session updates at any point while the session exists.
	//
	// See protocol docs: [Agent Reports Output](https://agentclientprotocol.com/protocol/v2/prompt-lifecycle#3-agent-reports-output)
	SessionUpdate(ctx context.Context, params UpdateSessionNotification) error
}

// ClientElicitation defines optional structured elicitation support.
type ClientElicitation interface {
	// Request from the agent to elicit structured user input.
	//
	// The agent sends this to the client to request information from the user,
	// either via a form or by directing them to a URL.
	// Elicitations are tied to a session (optionally a tool call) or a request.
	CreateElicitation(ctx context.Context, params CreateElicitationRequest) (CreateElicitationResponse, error)
}

// ClientElicitationCompletion defines optional URL elicitation completion notifications.
type ClientElicitationCompletion interface {
	// Notification sent by the agent when a URL-based elicitation is complete.
	CompleteElicitation(ctx context.Context, params CompleteElicitationNotification) error
}
